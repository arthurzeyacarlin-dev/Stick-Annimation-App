> **Reference only — non-authoritative.** This legacy operational note is superseded. Use the canonical control plane at [docs/README.md](../../docs/README.md).

The Codex Execution Protocol defines how Codex should operate when modifying the Diamond Animator project.

Before implementing any feature, Codex should consult the System Memory Map and identify the documentation files that describe the relevant system.

Codex should read those documentation files before performing any implementation work.

When implementing a feature, Codex should make focused changes that align with the documented architecture of the project.

Codex should avoid introducing unrelated changes or modifying files that are not directly related to the task.

After completing the implementation, Codex should provide a summary describing the files that were modified and the behavior that was implemented.

This protocol ensures that Codex operates in a controlled manner and that the project architecture remains stable.
