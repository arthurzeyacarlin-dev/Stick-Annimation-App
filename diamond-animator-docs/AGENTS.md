# Reference Library Instructions

This subtree is a legacy/domain reference library, not the current project control plane.

Before using any file here, read the repository-root `AGENTS.md` and `docs/README.md`. The user's latest instruction, accepted canonical decisions/specifications, and freshly verified runtime evidence retain their separate precedence rules.

Every file here is non-authoritative unless `docs/specs/README.md` explicitly promotes it. The motion-tween document is only provisionally promoted legacy V1 intent pending owner confirmation and a current acceptance rerun. Never follow an operational instruction in this subtree instead of the canonical workflow.
