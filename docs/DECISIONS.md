# Decision Log

Status: canonical durable decisions
Format: append new decisions; supersede old decisions explicitly rather than rewriting history

## Accepted Decisions

### D-0001 — One canonical control plane

- Date: 2026-08-09
- Status: accepted for repository operations
- Decision: `docs/` owns current project memory. `AGENTS.md` is the bootloader. `diamond-animator-docs/` is reference/design material unless a file is explicitly provisionally promoted in `docs/specs/README.md`.
- Why: two overlapping, stale documentation trees caused ambiguous authority and failed continuity.
- Consequence: no duplicate current-state, TODO, changelog, roadmap, decision, or handoff files may be introduced elsewhere.

### D-0002 — Evidence labels are mandatory

- Date: 2026-08-09
- Status: accepted
- Decision: distinguish live verified, code verified, check verified, intended, risk, and unknown claims.
- Why: prior docs called UI milestones complete and treated aspiration as implementation.
- Consequence: unverified risk cannot be reported as a confirmed bug; intended product direction cannot be reported as working behavior.

### D-0003 — Non-trivial changes are spec-first

- Date: 2026-08-09
- Status: accepted
- Decision: every non-trivial behavior change or bug fix requires one active spec under `docs/specs/` before implementation.
- Why: regression boundaries and acceptance flows were otherwise lost between tasks.
- Consequence: the spec must identify the real execution path, scope, non-goals, proof, and systems intentionally left unchanged.

### D-0004 — “Training” files are prompt/reference examples

- Date: 2026-08-09
- Status: accepted terminology decision
- Decision: describe current TypeScript example libraries as prompt/reference examples, not a trained Diamond Animator LLM.
- Why: the repository has no fine-tuning, embeddings, vector, training, or model-serving pipeline.
- Consequence: custom-model work requires a separate approved R&D spec and dataset governance.

### D-0005 — Memory maintenance never mutates Git history automatically

- Date: 2026-08-09
- Status: accepted safety decision
- Decision: control-plane helpers may validate and regenerate a sanitized tree, but must not silently stage, commit, push, reset, clean, or restore.
- Why: the initial worktree has a mixed index and almost all implementation files are untracked.
- Consequence: baseline versioning remains an explicit reviewed task.

### D-0007 — Use the integrated working page for the functional baseline

- Date: 2026-08-09
- Status: accepted repository-preservation decision under Arthur's explicit commit/push authorization
- Decision: use working `app/page.tsx` blob `c24392097af8d578fc1f6cc501dad121ce0cb1fc` in the functional baseline. Preserve the older indexed blob `d44892246c4a8933047c028d2508e194e1ec731a` separately in recovery commit `d35e892bdaabbd66ab36eae4cc32144aa620de44` on `codex/pre-baseline-staged-page-2026-08-09`.
- Why: the working version is the coherent current shell and matches the live-verified Open Project, Drawing, Stick, and Creator flows; the index version is an older monolithic home/new/drawing generation.
- Consequence: the recovery branch is historical and intentionally incomplete, receives no implementation pull request, and must remain until baseline acceptance makes deletion an explicit later decision.

### D-0008 — Use progressive elaboration and risk-calibrated task starts

- Date: 2026-08-10
- Status: accepted project-operating decision under Oliver's explicit sponsor instruction; this accepts no SPEC-0001 owner row
- Decision: make a spec decision-complete and safe for the currently authorized phase, move non-blocking later-phase uncertainty into explicit entry gates or follow-ups, consolidate PM findings into one review round, and require a genuine product/safety/current-phase blocker before another correction round. Start each implementation phase in Codex Plan mode. Use Extra High reasoning by default for PM/spec/normal implementation work, High for small mechanical control-plane or Git work, and Ultra only for genuinely high-risk or conflicting evidence.
- Why: repeated attempts to make all later phases perfect before any implementation consumed a full working day without proportionate product-risk reduction.
- Consequence: this standard does not lower acceptance or regression proof. User-outcome changes, owner choices, material security/privacy/cost/data-loss risk, current-phase feasibility or proof gaps, boundary violations, and protected regressions still block; bounded mechanics and later-phase details do not block earlier safe work.

### D-0009 — Approve SPEC-0001 and authorize Phase 1 only

- Date: 2026-08-11
- Status: accepted by Arthur for `SPEC-0001`
- Decision: approve [`SPEC-0001 — First Reversible AI-Created Stick Animation from Workspace Chat`](specs/0001-first-reversible-ai-stick-animation.md). Accept OD-01 through OD-06 and OD-09 exactly as written. Approve OD-07, OD-08, OD-10, and OD-11 as governing engineering rules and prerequisites. Defer OD-12 through OD-14, including exact provider model/reasoning/numeric policy, retention, cost-risk choice, and optional paid proof, until the separate Phase 7 Policy Gate.
- Why: the spec now defines one narrow, reversible, human/AI-equivalent Stick animation outcome and divides the work into independently provable phases with explicit safety and publication boundaries.
- Consequence at decision time: SPEC-0001 became active and Approved. Phase 1 alone was Authorized but Not started; Phases 2–7 and the separate browser-harness prerequisite remained Unauthorized/Not started. No provider integration, external lookup, search, Supabase request, retention choice, paid/live request, Git publication, or implementation work was authorized by this approval record. Phase 1 could begin only after that approval state was separately published and integrated into canonical `main`, in a new Plan-mode implementation task. Historical status note: Phase 1 was later completed, Verified, published, and integrated at `21a88feb65cf1cc51138c9ad4879b962ee468569`; D-0010 preserves that result as the exception under the former workflow.

### D-0010 — Separate phase execution from control-plane propagation and publication

- Date: 2026-08-13
- Status: accepted by Arthur as permanent project process
- Decision: every future implementation phase uses four sequential authorities: (1) a Spec Executor exclusively implements and technically tests one authorized phase, creates validated technical proof, returns an Implementation Review Packet, and stops without editing the canonical control plane or Git state; (2) Arthur and the Project Manager accept or reject the implementation; (3) after acceptance and complete executor shutdown, a Control Plane Architect takes exclusive ownership of the same worktree, propagates the accepted result into the canonical control plane, validates the technical manifest, completes final tracked-state/Git proof, returns a Control Plane Architect PM Review Packet, and stops; (4) only a later explicit publication instruction authorizes that architect to stage, commit, integrate into canonical `main`, push, and verify.
- Why: the former combined lifecycle let one task implement behavior, write the authoritative project record, and prepare publication, blurring independent review and increasing regression, stale-memory, and accidental-Git-mutation risk.
- Consequence: a Spec Executor may change only its phase-authorized implementation/fixture/technical-test files and ignored proof artifacts. It must not edit `AGENTS.md`, canonical `docs/`, or `project/project_structure.txt`, and must not stage, commit, merge, push, publish, or mutate another worktree. A Control Plane Architect may not enter that worktree until the executor is stopped and the implementation is accepted, and may not alter accepted runtime/fixture/test bytes. Rejection returns to a separately authorized executor correction task. The two roles never edit one worktree concurrently. In the later authorized publication task, the architect stages only the accepted implementation/reviewed control-plane paths, commits on the phase branch, fast-forwards a clean canonical `main`, pushes, and verifies clean `0/0` synchronization; an advanced `main` or changed path is a stop condition, not permission to pull, merge, rebase, force-push, rewrite history, or expand scope. D-0010 supersedes only the combined executor/control-plane mechanics previously described by D-0009/OD-10; sequential phases, Plan-mode starts, human review, separate publication authorization, and canonical-main integration remain required. SPEC-0001 Phase 1 remains a completed Verified/published/integrated historical exception and is not repeated, rewritten, or republished.

### D-0011 — Approve and authorize SPEC-0001 Phase 1.5 only

- Date: 2026-08-14
- Status: accepted by Arthur for `SPEC-0001` Phase 1.5
- Decision: approve the **Phase 1.5 — Permanent Automatic Browser Tester** amendment and authorize Phase 1.5 only as **Approved; Authorized; Not started**. The tester is a permanent reusable developer tool that remains invisible and unreachable to website users: it may add no tester route, page, button, API, asset, or production import; it may make no permanent application font, style, source, or behavior change; and every temporary test/font setup must be restored byte-for-byte. OpenAI, search, Supabase, paid services, and deployment remain forbidden.
- Why: repeatable, fail-closed browser proof is a prerequisite for safely changing the real Stick workspace in Phase 2, while a product-visible or product-mutating test surface would create unacceptable regression and shipping risk.
- Consequence: Phase 1.5 may begin only in a new Plan-mode Spec Executor task/worktree after this approval record is separately reviewed, published, and integrated into canonical `main` under D-0010. This decision does not begin implementation, authorize Git publication, authorize dependency or external acquisition, or authorize Phase 2 or Phases 3–7. Phase 1 remains Verified, published, and integrated. Phases 2–7 remain **Unauthorized; Not started**, and Phase 2 stays blocked until Phase 1.5 is Verified, accepted, propagated, separately published, and integrated.

### D-0012 — Approve the Phase 1.5 protected-Drawing correction ceiling

- Date: 2026-08-15
- Status: accepted by Arthur for the blocked `SPEC-0001` Phase 1.5 correction
- Decision: approve and authorize the Phase 1.5 correction as **Authorized; Not started/resumed**. Temporary diagnosis may occur only in `src/components/workspace/DrawingWorkspace.tsx` and `src/components/workspace/DrawingCanvas.tsx`. No behavior patch is allowed until diagnostics identify the first clearing writer. Any permanent runtime change is limited to the proven smallest subset of those two files. If diagnosis requires a third runtime file, a broad rewrite, or unrelated behavior change, the correction stops without expanding scope.
- Why: the stopped fail-closed tester proved that deterministic Drawing pixels appeared and then disappeared during final AI settlement, while existing evidence cannot distinguish the first clearing authority between the workspace restore/publication path and the canvas reset path. The two-file ceiling permits evidence collection at both known boundaries without pre-authorizing a guessed fix.
- Consequence: the stopped Phase 1.5 result remains blocked, unaccepted, unpublished, and without a valid proof manifest. A separately authorized Plan-mode correction Spec Executor may resume the stopped worktree only after this approval record is reviewed, separately published, and integrated under D-0010. `app/layout.tsx`, application fonts/styles, Drawing contracts/routes/planner/executor, Stick behavior, external services, Phase 2, and all later phases remain outside scope. OpenAI, search, Supabase, paid services, deployment, Git publication, worktree deletion, and recovery-branch use remain unauthorized.

### D-0013 — Approve SPEC-0002 and authorize Phase 1 only

- Date: 2026-08-15
- Status: accepted by Arthur for `SPEC-0002`
- Decision: Arthur explicitly approved: “I approve SPEC-0002. I accept OD2-01 through OD2-08 as written. I authorize Phase 1 only. SPEC-0002 Phase 2 and SPEC-0001 Phase 2 remain unauthorized.”
- Why: the specification defines a bounded, lossless local Drawing persistence foundation, separates the hidden format/storage engine from later real Save/Open wiring, and includes finite capacity, last-good preservation, strict legacy handling, offline proof, and conflict gates.
- Consequence: [`SPEC-0002 — Lossless Local Drawing Save and Reopen`](specs/0002-lossless-local-drawing-save-and-reopen.md) is Approved and active for this bounded work. OD2-01 through OD2-08 are accepted exactly as written. SPEC-0002 Phase 1 is **Authorized; Not started** and may begin only after this approval record is reviewed, separately published, and integrated into canonical `main`, in one new Plan-mode Spec Executor worktree from the then-current canonical-main SHA. SPEC-0002 Phase 2 and SPEC-0001 Phase 2 and later phases remain **Unauthorized; Not started**. The activation audit at `365e68fe98b27e993a1c5645c3e28c7b428c6f33` found clean synchronized canonical `main`, no active SPEC-0001 executor or architect, and a Phase 1 allowlist consisting only of new files with no overlap in SPEC-0001 runtime files or the shared tester core. Any real future overlap is a hard stop. This decision begins no implementation and authorizes no staging, commit, push, merge, publication, deployment, external request, or paid service.

### D-0014 — Authorize SPEC-0002 Phase 2 only

- Date: 2026-08-16
- Status: accepted by Arthur for `SPEC-0002` Phase 2
- Decision: Arthur explicitly authorized SPEC-0002 Phase 2 — Real Save/Open Wiring and Browser Proof as **Authorized; Not started**. This authorization covers only the existing Phase 2 scope and proof boundary in [`SPEC-0002`](specs/0002-lossless-local-drawing-save-and-reopen.md); it does not approve a runtime change in this control-plane task.
- Why: Phase 1 is Verified, published, and integrated, and the fresh audit at canonical `main`/`origin/main` `4835244e522c0f4206af8946a631d9244f2c9945` confirmed clean `0/0` synchronization, an unwired V2 engine, no active Phase 2 executor, no canonical SPEC-0003 file, and no file overlap with SPEC-0001 Phase 2's Stick-only boundary.
- Consequence: SPEC-0002 Phase 2 is **Authorized; Not started**. After this activation packet is reviewed and D-0014 is separately published to canonical `main`, exactly one new exclusive Plan-mode SPEC-0002 Phase 2 Spec Executor task may begin from that published authorization SHA and must refresh the conflict audit before implementation. It must run sequentially: no overlapping executor may touch `app/page.tsx`, Drawing persistence/navigation, or shared browser proof. SPEC-0001 Phase 2 and later phases remain **Unauthorized; Not started**; SPEC-0003 remains absent from canonical `main` and untouched. This decision authorizes no implementation in this task and no staging, commit, push, merge, publication, deployment, external request, paid service, or recovery-branch use.

### D-0015 — Authorize SPEC-0001 Phase 2 only after SPEC-0002 completion

- Date: 2026-08-17
- Status: accepted by Arthur for `SPEC-0001` Phase 2; activation record published in `a85690de9396cf97e3063005cbb6da85f109ae1d`
- Decision: Arthur authorized the dependency-safe sequence **SPEC-0002 Phase 2, then SPEC-0001 Phase 2, then whole SPEC-0003**. SPEC-0002 Phase 2 is now Verified, published, integrated, and durably complete at `af89b26c89d83eb61f77d91b4a50c105b7c12079`. SPEC-0001 Phase 1 and Phase 1.5 remain Verified/published/integrated. SPEC-0001 Phase 2 — Independent per-frame Stick state — is **Authorized; Not started**. Phases 3–7 remain **Unauthorized; Not started**.
- Why: the published SPEC-0002 work completes the earlier Drawing persistence dependency before Stick work begins, while SPEC-0001 Phase 2 is the next bounded foundation required for the human-built three-pose/12-frame wave and later reversible AI execution. The fresh audit found clean synchronized canonical `main` at `af89b26c89d83eb61f77d91b4a50c105b7c12079`, no active executor or dirty worktree owning the exact Phase 2 Stick allowlist, and the separate SPEC-0003 proposal only in its own dirty documentation worktree. SPEC-0002 completed first because it and later SPEC-0003 work both use `app/page.tsx`; no SPEC-0003 byte is copied or activated here.
- Consequence: after this D-0015 activation record is separately reviewed, published, and integrated, exactly one new dedicated Plan-mode SPEC-0001 Phase 2 Spec Executor task/worktree may begin from that activation publication SHA and must repeat the conflict audit. Its scope is exactly SPEC-0001 §10.5: independent per-frame Stick state, the built-in editable figure, the no-AI manual three-pose/12-frame wave, equal manual editability for all 11 joints, the derived line head, and exact transient drag/pointer-up/cancel safety. History, storage, Open Project, AI/chat/API/provider work, SPEC-0003, and later phases remain outside scope. The permanent tester and exact Phase 2 technical-proof contract remain mandatory. This decision authorizes no implementation in this control-plane task, no staging/commit/push/merge/publication, and no external request, OpenAI, search, Supabase, paid service, or deployment.

### D-0016 — Approve and authorize the SPEC-0001 Phase 1.5 proof-compatibility correction

- Date: 2026-08-17
- Status: accepted by Arthur under his explicit autopilot authorization and accepted by the Project Manager
- Decision: approve the exact SPEC-0001 §10.4A **Phase 1.5 Compatibility Correction for Phase 2 Proof** as **Approved; Authorized; Not started**. Its implementation ceiling is exactly the 23 proof-infrastructure paths listed in §10.4A. It changes no user-facing behavior, product outcome, or owner choice. The same immutable version-2 plan must support both dirty-executor and clean-committed proof through exact `dirtyExpectedPaths` and `cleanExpectedPaths`, with state derived from real Git-visible bytes. The correction catalog pre-authorizes only the future Phase 2 paths, schemas, operation families, output root, and §10.5 ceiling; future Phase 2 byte lengths and SHA-256 bindings are established only when Phase 2 creates those files and are not pre-known or pre-recorded by this correction.
- Why: fresh tracing at published activation base `a85690de9396cf97e3063005cbb6da85f109ae1d` proved a genuine proof-infrastructure feasibility blocker: the permanent runner ignores the required plan, loads no later-phase registry/action module, rejects legitimate later-phase dirty sets, and hard-codes a historical lint tuple. The accepted §10.4A correction makes the already-approved Phase 2 proof executable without widening Phase 2 or changing the product.
- Consequence: historical Phase 1 and Phase 1.5 remain Verified, published, and integrated. SPEC-0001 Phase 2 remains **Authorized; Not started; blocked** until this exact correction is implemented by one Plan-mode Spec Executor, technically accepted, propagated under D-0010, separately published/integrated, and followed by a new activation SHA/executor record. Phases 3–7 remain **Unauthorized; Not started**. This control-plane task begins no implementation and authorizes no later phase, external request, OpenAI, search, Supabase, paid service, deployment, staging, commit, merge, push, or publication. The correction itself may begin only after this D-0016 record is separately reviewed, published, and integrated into canonical `main`.

### D-0017 — Approve and authorize SPEC-0001 §10.5A only

- Date: 2026-08-18
- Status: accepted by Arthur for the SPEC-0001 Phase 2 visible-result correction
- Decision: Arthur explicitly rejected the published Phase 2 visible workspace result and approved §10.5A **Phase 2 UI Restoration and Editable Timeline Correction** as **Approved; Authorized; Not started**. The exact accepted outcome is the pre-Phase-2 Stick presentation and prior tools/navigation; one small empty starting keyframe and empty canvas with no automatic figure, wave, or twelve cells; only applicable current Drawing-style frame and layer behavior in the Stick timeline; Insert Frame, Insert Keyframe, Insert Blank Keyframe, copy, paste, remove, held-span, and applicable layer operations; deep-independent editable Stick content per keyframe with holds resolving/editing their owner; unchanged fonts, colors, menus, right panel, toolbar, and canvas presentation; and Creator remaining accessible.
- Why: the published `adbda9dd4f42a103c3c5af41ccc19b110b6825c0` technical result passed its bounded proof but visibly replaced the accepted Stick workspace with a large automatic twelve-cell/figure experience, reduced tools, altered presentation, disabled layer operations, and Creator locking. Fresh historical/current comparison established the exact restoration baseline and the narrow timeline behavior needed without changing the product outside Stick Phase 2.
- Consequence: the original Phase 2 publication remains immutable historical owner-rejected evidence. §10.5A alone is Authorized/Not started and may begin only after this activation record is separately reviewed, published, and integrated; that future publication commit becomes the exact implementation base for one new dedicated Plan-mode Spec Executor worktree. The executor must remain inside §10.5A's exact nine-runtime-file and bounded proof ceiling, stop completely for Arthur's visible desktop/compact review, and perform no control-plane or Git action. Phase 3 and Phases 4–7 remain **Unauthorized; Not started**. SPEC-0002 remains complete/protected; SPEC-0003 remains Proposed/inactive and untouched. No persistence, AI, Drawing change, external request, OpenAI, search, Supabase, paid service, deployment, staging, commit, merge, push, or publication is authorized by this decision-recording task.

### D-0018 — Fix real Stick onion skin inside Phase 3 without authorizing Phase 3

- Date: 2026-08-18
- Status: accepted by Arthur as the exact future SPEC-0001 Phase 3 onion-skin outcome
- Decision: the Onion button restored by §10.5A remains presentation-only in Phase 2 and becomes functional inside Phase 3. While paused and ON, it renders at most one nearest previous distinct visible active-layer Stick state with exact Drawing tint `rgba(92, 63, 158, 0.58)` and at most one nearest next distinct visible active-layer Stick state with exact Drawing tint `rgba(44, 122, 91, 0.56)`. Resolution skips holds/same-visible states, stops at an encountered blank/empty boundary, and suppresses the next overlay when both sides resolve to the same visible state. The current pose stays normal/editable; overlays are transient, non-interactive, suppressed during playback, cleared immediately when OFF, and excluded from canonical project/history/digest/storage/AI bytes. Fresh human-authored, later AI-authored, fixture-mounted, and reopened Stick projects use the same renderer path. Drawing runtime and tint behavior remain byte-for-byte unchanged.
- Why: Arthur explicitly chose Drawing-consistent onion skin for the existing Stick control. It makes frame-to-frame posing readable without introducing tweening, automatic motion, a separate phase, a second renderer, or any new persisted state.
- Consequence: SPEC-0001 Phase 3's authorized-file proposal and proof contract now include only the existing Stick workspace/canvas work necessary for this transient renderer plus deterministic and browser proof of exact count/tints/alpha, neighbor resolution, blank/hold/duplicate/multi-layer boundaries, playback/OFF suppression, non-interactivity, origin independence, zero canonical mutation, and Drawing regression. Onion skin is removed from the later tween/interpolation deferral; tweening, automatic in-betweens, easing, and motion frames remain deferred. Phase 3 remains **Unauthorized; Not started** and may not be planned or implemented until separately authorized after §10.5A closeout/publication. This decision authorizes no runtime/test/proof edit, Drawing change, external request, stage, commit, push, publication, or later phase.

### D-0017 closeout note — §10.5A accepted and technically Verified in its worktree

- Recorded: 2026-08-18
- Result: after the Spec Executor stopped and Arthur plus the Project Manager accepted the visible/technical result, the Control Plane Architect took exclusive ownership of the dedicated correction worktree at exact base/HEAD `1f9746a6188e5e13aca1fbd62bea2e1e27bba627`. The exact 20 accepted implementation/proof paths remain byte-bound to technical-manifest SHA-256 `edda3028b9fbe2759e31059455f16cc3ee02ac9b242149107454071dae62de90`; the index remains empty.
- Proof disposition: the registered correction plan passed 15/15 actions, two compact screenshots, and five protected groups. Deterministic proof records 311 timeline assertions and 631 Phase 1 regressions; TypeScript, focused/measured lint, and diff checks pass. The historical SPEC-0002 command is `not-applicable`, never passed, because its obsolete frozen runner hash differs and the fail-closed audit proves zero protected Drawing/SPEC-0002/Open/storage/`app/page.tsx` or relevant shared-dependency runtime-byte change. The published SPEC-0002 record, Arthur's human Drawing Save verification, and current protected groups remain the replacement protection. Two alternate harness attempts are diagnostics only.
- Finalizer repair: after separate Spec Executor repair and Arthur/Project Manager acceptance, `scripts/finalizeSpec0001ProofBundle.ts` at SHA-256 `e6795e9f686117d89824bea4f9de3ed766077ba9a8d00c51c27b074708c396f9` adds the explicit `--purpose=phase-2-ui-restoration-correction` mode without changing the historical/default mode. The correction mode binds the exact 29-path tracked state, accepted technical manifest and 20/20 hashes, 16/16 protected zero diffs, sole obsolete SPEC-0002 runner-hash mismatch, registered browser evidence, empty index, and correction-specific ignored closeout. Historical SPEC-0002 remains `not-applicable`, never passed.
- Consequence: §10.5A is **Verified and accepted in the dedicated worktree; pending separate publication/integration**. This closeout does not stage, commit, merge, push, publish, authorize Phase 3, or change D-0018. Phase 3 remains **Unauthorized; Not started**.

### D-0019 — Authorize SPEC-0001 Phase 3 after §10.5A integration

- Date: 2026-08-18
- Status: accepted by Arthur for `SPEC-0001` Phase 3
- Decision: Arthur explicitly instructed that onion skin remains inside Phase 3, that the already accepted D-0018 outcome must be included unchanged, and that Phase 3 should be prepared for implementation but not started in this task. The §10.5A correction is now **Verified, published, and integrated** in canonical `main` at `edfb3dea023119b91336e6e5da645d4982a9f068`. SPEC-0001 Phase 3 — **Atomic history and minimal local persistence**, including the exact D-0018 Stick onion-skin contract — is therefore **Authorized; Not started**. The rest of Phase 3's approved scope, proof, non-goals, and protected regressions remain unchanged.
- Why: the sole Phase 3 entry prerequisite is now durably satisfied, and Arthur has explicitly selected Phase 3 as the next bounded implementation phase without splitting onion skin into separate work.
- Consequence: after this authorization record is reviewed, published, and integrated, exactly one new dedicated Plan-mode Phase 3 Spec Executor task may begin from this activation publication's final canonical-main SHA and implement only SPEC-0001 §10.6. This record begins no Phase 3 runtime/test/proof work, authorizes no Phase 4–7 work, and authorizes no external request, OpenAI, search, Supabase, paid service, deployment, or broader product change. Phases 4–7 remain **Unauthorized; Not started**.

### D-0020 — Accept the Phase 3 tester-registration and Creator-preservation correction

- Date: 2026-08-18
- Status: accepted by Arthur as the exact corrected SPEC-0001 Phase 3 implementation and proof boundary
- Decision: keep D-0018 exact and Phase 3 **Authorized; Not started** until this accepted correction record is separately published and integrated. Phase 3 implements only Stick Undo/Redo, local Save/Open, and D-0018 onion on the published §10.5A compact shell/manual timeline. Preserve Stick Figure Creator presence and usability before and after edits, Undo/Redo, Save, Open, reload, fixtures, and terminal failures; add no Creator lock/copy or history/storage/document/digest/request/checkpoint lock field, and do not broaden Creator Save/library behavior. Accept exactly the nine named shared tester paths in corrected §10.6 for an additive, fail-closed `phase-3/v1` registration/port while preserving the legacy no-plan, Phase 1.5 synthetic, and Phase 2 meanings and modes. Add no redesign, fat frames, automatic figure/wave/twelve cells, new colors or bright-green state, animation/glow, font/size/spacing/layout/tab/menu/toolbar/panel change, removed control, or Drawing change.
- Why: the stopped first Phase 3 executor changed no file and proved at exact clean canonical `45504214dffb528e930194b93fc2ab78dc730ebc` that the two-entry tester and its closed schemas/types/runner/recorder/validator cannot register or prove Phase 3. The accepted correction resolves that feasibility blocker without widening the product outcome or weakening protected older tester modes.
- Consequence: GIT-019 must publish/integrate only this reviewed seven-document correction and report the resulting exact clean canonical-main SHA. Only then may one replacement dedicated Plan-mode Phase 3 Spec Executor implement corrected §10.6 and stop after its technical proof/Implementation Review Packet. This decision implements nothing, authorizes no staging/commit/merge/push/publication by itself, and leaves Phases 4–7 **Unauthorized; Not started**.

### D-0019 closeout note — Phase 3 accepted and technically Verified in its worktree

- Recorded: 2026-08-19
- Result: after the proof-only Spec Executor stopped, Arthur completed visible human review and explicitly accepted the Phase 3 result. The Control Plane Architect then took exclusive ownership of `/Users/arthurcarlin/.codex/worktrees/cdb2/stick-animation-app`, verified exact base/HEAD `54234b7c7b95201e274975a804859fa9c36806a1`, an empty index, and the exact 30-path accepted implementation/fixture/test set, and independently revalidated `output/spec-0001/phase-3/proof-manifest.json` at SHA-256 `07bd09c413a28ffcc706be85d00d1d4466cde1e336daceab23d9a1617153b675` without changing accepted bytes.
- Proof disposition: the fresh manifest binds 11 successful receipts and 63 artifacts. It proves the bounded async Drawing-memory validator correction, Stick history/persistence/onion behavior, TypeScript, base/result lint at 5 errors and 72 warnings with zero changed-line/new-file findings, diff checks, registration self-test, inherited tester run, and a 71-action/7-checkpoint/4-screenshot two-viewport browser result with all five protected groups, zero non-loopback/provider requests, and complete cleanup. After the first closeout exposed a missing Phase 3 record-path context, a separately accepted proof-only correction changed exactly `scripts/spec0001-browser/browserTesterExtensionContract.ts` and `scripts/validateSpec0001ProofBundle.ts` to enforce the exact 30-technical-plus-eight-record closeout set; it changed no accepted product outcome or other accepted technical byte. Arthur's human review accepted the visible Undo/Redo, layer, onion, playback, and preserved-shell result.
- Consequence at closeout: Phase 3 was **accepted and technically Verified in its dedicated worktree; pending separate publication/integration**. This note changed no product choice and did not stage, commit, merge, rebase, push, publish, deploy, authorize Phase 4, or change D-0018/D-0019/D-0020. Current superseding status is recorded by D-0022: Phase 3 is published/integrated at `3fe3a5487389647b67216e9466121e00f1a73856`; Phase 4 is Authorized/Not started after D-0022 publication; Phases 5–7 remain Unauthorized/Not started.

### D-0021 — Require a separate review copy before accepting visible implementation changes

- Date: 2026-08-19
- Status: accepted by Arthur as a permanent implementation-review rule
- Decision: for every future implementation that changes visible application behavior, the stopped Spec Executor must leave the exact worktree result available for human review. Before accepting that result or authorizing control-plane propagation/publication, the Project Manager must give Arthur a separate review copy through a current worktree-served loopback app URL, current screenshots from that exact worktree, or both, and explain in plain language what to review. Arthur reviews the unpublished worktree result rather than testing canonical `main`. Publication waits for Arthur's explicit human acceptance. If Arthur rejects the visible result, it returns to a separately authorized Spec Executor correction task; no propagation or publication follows the rejected result.
- Why: Arthur's Phase 2 and Phase 3 reviews proved that automated checks can pass while a visible result is still wrong for the intended product, and that a separate worktree preview lets him evaluate the real change without placing unaccepted bytes on canonical `main`.
- Consequence: this human review copy is an additional product-acceptance gate, not a replacement for deterministic or browser regression proof. It creates no production route, public tester, deployment, external upload, or requirement to copy unpublished bytes into `main`. Invisible implementation changes may use their exact technical evidence when no visible app surface exists. The existing Spec Executor → Arthur/Project Manager → Control Plane Architect → separate publication lifecycle remains unchanged; Phase 3's completed visible acceptance satisfies this rule. Phase 4 was Unauthorized/Not started when D-0021 was recorded; D-0022 later authorizes it without changing this review rule.

### D-0022 — Authorize SPEC-0001 Phase 4 with the exact tester-permission correction

- Date: 2026-08-19
- Status: accepted by Arthur for `SPEC-0001` Phase 4
- Decision: Phase 3 is Verified, published, and integrated in exact 38-path canonical-main commit `3fe3a5487389647b67216e9466121e00f1a73856`, parent `b8d8406ca6b51c3b83adb29fa20f20160c871c54`. Arthur approved Phase 4 — **Validated editor command transaction** — as **Authorized; Not started**, together with the narrow proof-feasibility correction in SPEC-0001 §10.7. That correction authorizes exactly 22 tracked technical paths: the original 12 Phase 4 product/fixture/action paths plus exactly 10 additive catalog/schema/runner/contract/recorder/validator/finalizer paths needed to register, run, record, independently validate, and close out authorization ID `phase-4/v1`. New Phase 4 schemas live under `scripts/fixtures/spec0001-browser/v4/`; accepted v2/v3 schemas and all older tester entries, meanings, modes, hashes, registered paths, source-restoration rules, network denial, cleanup, and production-exclusion behavior remain exact.
- Why: the current permanent tester recognizes only the already integrated Phase 1.5, Phase 2, and Phase 3 modes. The former §10.7 simultaneously required a new Phase 4 plan/registry/action module and prohibited the shared tester from registering, dispatching, recording, validating, or finalizing that new mode. The exact additive ceiling fixes this internal authorization contradiction without changing what Phase 4 does for the user.
- Consequence: this decision changes no product outcome and implements nothing. Phase 4 may begin only after this reviewed record is separately published and integrated into canonical `main`, in one new dedicated Plan-mode Spec Executor worktree from that exact publication SHA. The executor may change only §10.7's 22 technical paths plus ignored Phase 4 proof artifacts, must prove the unchanged older tester modes as well as strict `phase-4/v1`, and must stop after its Implementation Review Packet. It may not edit canonical control-plane files or stage, commit, merge, push, publish, deploy, call an external service, or begin Phase 5. Phases 5–7 remain **Unauthorized; Not started**.

### D-0023 — Accept the exact SPEC-0001 Phase 4 technical result without authorizing Phase 5

- Date: 2026-08-19
- Status: accepted by Arthur for the stopped SPEC-0001 Phase 4 implementation
- Decision: Arthur explicitly accepted the exact unpublished Phase 4 worktree after reviewing Play, atomic Undo/Redo, manual joint drag, and Creator/Back. The accepted result is the exact 22-path technical projection from base/HEAD `62f046adff7418d2e644365fc04bd5d6312dcca9`, bound by technical-manifest SHA-256 `d34d589373713ac7930984aeb3ef0640539316c00e1f510cf8d798aee931bf46` and browser-runner SHA-256 `353f4937972d30b1959f1c7aee3075f5ebafb308d04a778db69372969e7b906f`.
- Why: the implementation satisfies D-0022's validated transaction outcome and the separate D-0021 visible-review gate. Its proof records 12 successful receipts, 61 artifacts, 138 deterministic transaction assertions, inherited Stick/Drawing regressions, TypeScript, zero lint regression, 105 browser actions, 31 checkpoints, 2 screenshots, five protected groups, zero non-loopback traffic, and complete cleanup.
- Consequence at acceptance: Phase 4 was **Verified and accepted in its dedicated worktree; pending separate publication/integration**. The Control Plane Architect could propagate and close out the accepted bytes but could not change them; that acceptance alone did not authorize staging, commit, merge, push, publication, deployment, any external/provider operation, or Phase 5.
- Historical publication note: Arthur later explicitly authorized publication. The exact accepted 22 technical paths and eight reviewed canonical record paths were committed as `71841e96499f7627139c53d87114bba65e19d29d`, parent `62f046adff7418d2e644365fc04bd5d6312dcca9`, message `Implement SPEC-0001 Phase 4 command transaction`, then fast-forwarded and normally pushed to canonical `main`. The Phase 4 branch, local `main`, local `origin/main`, and live GitHub `main` matched cleanly at `0/0`. At that publication point, Phase 4 was **Verified, published, and integrated** while Phases 5–7 remained **Unauthorized; Not started**; D-0024 later supersedes only Phase 5's authorization status.

### D-0024 — Authorize SPEC-0001 Phase 5 server raw-dispatch and deterministic mock boundary only

- Date: 2026-08-19
- Status: Accepted by Arthur.
- Decision: Arthur explicitly authorized SPEC-0001 Phase 5 only. Phase 4 remains **Verified, published, and integrated** in `71841e96499f7627139c53d87114bba65e19d29d`; at this decision's start, clean canonical `main`/`origin/main`/live GitHub `main` were synchronized at post-publication record commit `617aa61a81b9c16b781e6d333255fdd5b65fced2`. Phase 5 becomes **Authorized; Not started** with exactly section 10.8's provider-free server raw-dispatch, strict marked-request parsing, coarse availability, deterministic mock-envelope, Drawing fallthrough, no-egress, and proof boundary. This decision authorizes no writable Stick chat or UI, OpenAI/provider/search/Supabase/paid request, deployment, Phase 6, or Phase 7 work.
- Human review requirement: after the dedicated Phase 5 Spec Executor completes technical proof and stops, the Project Manager must give Arthur the exact unpublished Phase 5 app copy for one review before technical acceptance. Because Phase 5 intentionally adds no visible Stick UI, that review confirms that the existing app still looks and works the same; the hidden mock route is proved by the automated technical evidence, not by a new visible feature.
- Consequence at decision time: implementation could begin only after this reviewed activation record was separately published and integrated into canonical `main`; the executor had to remain inside section 10.8, return its Implementation Review Packet, and stop before control-plane propagation or publication. Phases 6 and 7 remained **Unauthorized; Not started**. This decision authorized no staging, commit, push, publication, external request, or deployment.
- Historical publication note: the exact seven-document activation was later published as canonical-main commit `245ac7e8bceeca76b522ff21c4327fa0219a3805`, message `Authorize SPEC-0001 Phase 5`. The fresh executor booted from that exact SHA and stopped before edits on the separately recorded D-0025 proof-feasibility blocker.

### D-0025 — Correct SPEC-0001 Phase 5 permanent-tester permission without changing the product phase

- Date: 2026-08-19
- Status: accepted control-plane correction; pending separate publication/integration
- Evidence: D-0024's exact seven-document activation is published in canonical-main commit `245ac7e8bceeca76b522ff21c4327fa0219a3805`. A fresh dedicated Phase 5 Spec Executor completed the required boot from that exact clean SHA and stopped before edits. Live tracing proved §10.8 requires registered `phase-5/v1` real-route proof and a Phase 5 proof bundle, while `tester-extension-authorizations.json` contains exactly four entries through `phase-4/v1`; the v4 schemas require exactly four; `browserTesterExtensionContract.ts`, `runSpec0001BrowserProof.ts`, `recordSpec0001ProofBundle.ts`, `validateSpec0001ProofBundle.ts`, and `finalizeSpec0001ProofBundle.ts` accept/derive/close only the earlier modes. The runner's current `/api/ai` behavior is intercepted Drawing proof and explicitly asserts zero real-route requests. The stopped executor changed nothing.
- Decision: preserve D-0024's exact Phase 5 product outcome and original 14 product/fixture/validator paths, and authorize exactly 10 additive shared tester paths: `scripts/fixtures/spec0001-browser/v2/tester-extension-authorizations.json`; four new v5 authorization/plan/registry/result schemas under `scripts/fixtures/spec0001-browser/v5/`; `scripts/runSpec0001BrowserProof.ts`; `scripts/spec0001-browser/browserTesterExtensionContract.ts`; `scripts/recordSpec0001ProofBundle.ts`; `scripts/validateSpec0001ProofBundle.ts`; and `scripts/finalizeSpec0001ProofBundle.ts`. The complete Phase 5 technical ceiling is therefore exactly 24 tracked paths. No other tester, runtime, fixture, proof, package, dependency, environment, application, or control-plane path is authorized for Phase 5 implementation.
- Route-only tester contract: append exactly one deferred `phase-5/v1` catalog entry after the byte/meaning-preserved first four. It binds the authorized v5 plan, uses the already-authorized `stick-ai-raw-route-cases.json` as its v5 registry, returns route-only v5 evidence, and has only `guarded-http` and `runner-environment` operation families. The permanent runner sends exact fixture bytes through guarded Node loopback HTTP to the real `/api/ai` route and records exact status/header/body/log/network/cleanup evidence for marked availability GET, marked raw Stick POST, and marker-free Drawing-fallthrough POST. Phase 5 has no action-adapter file, workspace port, in-page driver operation, pointer target, visible role/test ID, screenshot requirement, route/page/control/asset, browser-mocked route response, or writable Stick surface.
- Compatibility and closeout rule: the no-plan Phase 1.5 v1 mode and Phase 1.5 synthetic/Phase 2/Phase 3/Phase 4 v2-v4 meanings, entries, schemas, bindings, hashes where materialized, Git-state behavior, source restoration, egress denial, cleanup, and production exclusion remain exact. Phase 5 dirty mode requires exact base HEAD, empty index/no hidden flags, and an observed set equal to the canonical technical projection; clean mode requires a strict descendant and exact base-to-HEAD projection. The two existing contracts in the 24-path ceiling may be dirty only when a Phase 5 fixture proves a required strict-validation correction; all 22 other technical paths are mandatory. Closeout permits only that exact technical projection plus the eight established canonical record paths.
- Consequence: Phase 5 remains **Authorized; Not started**. The no-edit executor from base `245ac7e8bceeca76b522ff21c4327fa0219a3805` is superseded only as a stopped feasibility attempt. A replacement Phase 5 Spec Executor may begin only after this correction is separately reviewed, published, and integrated, from that correction-publication SHA, and may implement only corrected §10.8. Phases 6 and 7 remain **Unauthorized; Not started**. This correction changes no product outcome, raw-byte/parser rule, availability/mock behavior, Drawing fallthrough, no-editor/history/storage/UI rule, no-egress rule, or external/live prohibition; it authorizes no implementation in this task and no staging, commit, merge, push, publication, deployment, provider, OpenAI, search, Supabase, or paid request.

### D-0026 — Accept SPEC-0001 Phase 5 provider-free server/mock implementation

- Date: 2026-08-20
- Status: Accepted by Arthur; Verified, published, and integrated.
- Decision: Arthur reviewed the exact unpublished Phase 5 app copy, reported that everything looked normal, and explicitly approved Phase 5. The accepted implementation stays at exact base/HEAD `a2b4f3e0fc492df9cd63bda32554e382a344cdb6` with exactly 22 Git-visible technical paths from D-0025. It implements only §10.8's hidden first-operation raw Stick dispatch, strict captured-input rejection, safe coarse availability, deterministic provider-free mock, and original unread Drawing fallthrough. It adds no visible UI, workspace/editor/history/storage mutation, provider call, search, Supabase, paid request, or deployment.
- Proof and packaging correction: initial publication preflight exposed one extra terminal LF in each of four new v5 schemas, `stick-ai-availability-cases.json`, and `stickFigureAiMockServer.ts`. A stopped correction executor removed exactly those six bytes, changed no logic, left the other 16 technical and eight inherited record files byte-identical, and reran the complete isolated proof. The corrected 22-path technical projection has executor-reported aggregate `sha256:78e00dd35e1be1bc97ea2498abad6b09042216d57765ff6bbaec0a7e19e7d55c`; its 12-receipt/53-artifact manifest independently binds the corrected bytes at SHA-256 `f9024a5d86cc1febbac7df3d4219348cd8289d17723406bac5d90929d4cf5c0f`. Source-direct proof passed 362 assertions over 31 exact raw cases; registered guarded-loopback proof executed 31 real-route requests; all named Phase 1–4 and Drawing regressions, TypeScript, measured lint non-regression, diff, registration, EOF/UTF-8/whitespace, zero-egress, and cleanup gates passed. Browser/server/child/runner non-loopback attempts and provider/editor/history/storage calls were zero. Arthur's visible review is separate human evidence. A production build attempt was not a Phase 5 gate and did not pass: it stopped on the pre-existing untouched AI-cost dashboard `PageProps` type error.
- Consequence: Phase 5 is **Verified, published, and integrated** in exact 30-path commit `9bbcc1df2fe4c79c0947601d0ea6274a85732d85`, parent `a2b4f3e0fc492df9cd63bda32554e382a344cdb6`, message `Implement SPEC-0001 Phase 5 raw Stick dispatch`. The Phase 5 branch, local `main`, local `origin/main`, and live GitHub `main` matched cleanly at `0/0` after a normal non-force push. Phases 6 and 7 remain **Unauthorized; Not started**. This publication does not authorize Phase 6, a real OpenAI/provider/search/Supabase/paid request, or deployment.

### D-0027 — Accept typo-friendly deterministic intent for SPEC-0001 Phase 6

- Date: 2026-08-20
- Status: Accepted by Arthur and the Project Manager; published at `f46ed3b13e6bca3a09c9b2926c972bea8c331f2c`. Historical activation status was **Approved; Authorized; Not started**. D-0028 supersedes the current Phase 6 lifecycle/presentation after Arthur's rejected first review.
- Decision: Phase 6 must not require the exact canonical sentence. It accepts only SPEC-0001 section 4.3's versioned closed V2 grammar: bounded meaning-equivalent phrasing, safely implied three poses and 12 FPS, grammatically singular one-figure wording, an audited unique typo map including `wwave`, `animaton`, and `onne`, and Arthur's exact `122 FPS` repeated-second-digit correction as the only accepted numeric typo. Every accepted form resolves to one canonical server-authoritative intent: one stick figure, one safe three-pose wave, 12 displayed frames at 12 FPS. Preview must plainly state that interpretation and that no change has been made before explicit Apply.
- Safety boundary: There is no general fuzzy matching. Exact accepted vocabulary wins first; only the fixed typo-map sources correct, with validated token-length distance limits, at most two corrected tokens, total distance at most two, and ambiguity rejection. Digit runs never enter letter correction. Explicit `13`, `24`, `30`, other FPS/counts/poses, multiple figures, different real nouns/actions such as clown/fish/jump/fight/walk, props, backgrounds, clauses, Unicode/confusables, excessive input, or unconsumed content reject before mock/provider work, preview, or mutation. Missing values default only after a complete supported singular-wave parse.
- Version/lifecycle boundary: Published Phase 5 `promptIntentVersion: 1` exact-sentence behavior, fixtures, proof, history, and exact publication `9bbcc1df2fe4c79c0947601d0ea6274a85732d85` remain immutable. Phase 6 may add request/manifest/envelope/intent V2 beside V1 in the exact 26-path technical ceiling recorded in §10.9, including the live-code-required shared contract/mock changes and additive `phase-6/v1` V6 tester registration. The real route/raw-reader/classifier/availability path stays read-only. Phase 7 later consumes the same canonical server intent and still requires its separate owner policy gate; raw user wording/correction metadata is never sent to the provider.
- Consequence at activation: Phase 6 alone became **Approved; Authorized; Not started** after separate publication. The activation changed no runtime, UI, test, fixture, script, package, dependency, configuration, environment, database, provider, or deployment state. Its later publication allowed one dedicated Plan-mode Phase 6 Spec Executor to start from `f46ed3b…`; D-0028 now records rejection/correction of that first unpublished result. Phase 7, an external lookup, OpenAI/search/Supabase, a paid request, and deployment remained unauthorized.

### D-0028 — Reject the first Phase 6 review copy and correct its visible outcome

- Date: 2026-08-21
- Status: Accepted directly by Arthur and accepted by the Project Manager as the corrected Phase 6 owner outcome; published in exact seven-document canonical-main commit `f19955c336be43b19b2c4cc13d16abcc7ded7247`, parent `f46ed3b13e6bca3a09c9b2926c972bea8c331f2c`, message `Record D-0028 Phase 6 correction`. D-0029 records the later accepted corrected implementation.
- Rejection: D-0027's seven-document activation was published at exact canonical-main commit `f46ed3b13e6bca3a09c9b2926c972bea8c331f2c`. Arthur then rejected the first unpublished Phase 6 implementation review copy. It showed timeline creation without the required visible built-in figure and changed the initial Stick AI panel away from the original presentation by exposing pre-action eligibility/guidance, a suggestion shortcut, and a visibly text-labeled Preview control. That separate worktree result is not accepted, Verified, published, or present on canonical `main`.
- Corrected visible result: before the user sends, retain the original bottom-right AI Animator helper/header style and source-authored `Generate frames • Clean drawings • Animate faster`, exact placeholder `Chat here`, and original white 28×28 circular checkmark send button. Show only transparent/unboxed muted text exactly `Ask the assistant for help with your stick figure.` Show no readiness/eligibility warning, suggestion chip, shortcut explanation, preview, Apply, or Cancel. The user acts first; sending requests the preview. Reasoning/Task controls are deferred and may remain absent.
- Apply/Cancel/onion result: the visible §10.5A starter remains one blank cell/empty canvas. Preview says exactly what the server understood and mutates nothing. Cancel remains an exact no-op and creates no figure. Apply uses the unchanged Phase 4 atomic transaction and must visibly render the real approved built-in figure through the existing editable `structureGraph` on Frames 1/5/9 and all held frames. Paused Onion on Frame 5 must visibly show the previous complete figure in exact purple `rgba(92, 63, 158, 0.58)` and the next complete figure in exact green `rgba(44, 122, 91, 0.56)`.
- Proof/boundary: real-browser acceptance must bind canonical/render-input digests to live SVG geometry and clipped-stage pixels, including zero pre-Apply/Cancel figure geometry, three distinct key poses, hold equality, exact onion sides/tints/neighbor geometry/color pixels, and Onion OFF cleanup. Exact-base/result evidence at `1440×900` and `1024×768` rejects any unrelated font/color/size/spacing/layout/panel dimension/button size/timeline/canvas shell/right-panel/workspace/navigation or fat-frame change. The existing 26-path Phase 6 authorization ceiling remains sufficient; its exact final dirty subset is bound. `StickFigureCanvas.tsx`, the Phase 3 onion resolver/tints, Phase 4 transaction semantics, Phase 5 V1/history/proof, Drawing, Creator, dependencies, config, environment, database, and Phase 7 policy remain unchanged.
- Consequence: this is a correction inside Phase 6, not a new phase. The first review copy returns to a separately authorized Spec Executor correction task only after this accepted record is separately published/integrated under the existing lifecycle. Phase 5 remains **Verified, published, and integrated** at `9bbcc1df2fe4c79c0947601d0ea6274a85732d85`; Phase 7 remains **Unauthorized; Not started**. This documentation closeout performs no runtime/test/fixture/script/proof/package/config/environment/database/external/Git publication action and does not inspect or modify the rejected executor worktree.

### D-0029 — Accept the corrected SPEC-0001 Phase 6 implementation and same-root Creator continuity

- Date: 2026-08-21
- Status: Accepted directly by Arthur after reviewing the corrected app; **Verified, published, and integrated** in exact 33-path canonical-main commit `caa6c2d946780f384d0a8c58f4ea75a771483bcd`, parent `f19955c336be43b19b2c4cc13d16abcc7ded7247`, message `Implement SPEC-0001 Phase 6 deterministic Stick chat`.
- Decision: Arthur accepted the corrected Phase 6 result in the exact stopped executor worktree at base/HEAD `f46ed3b13e6bca3a09c9b2926c972bea8c331f2c`. The original Stick AI presentation is preserved until the user sends: `Generate frames • Clean drawings • Animate faster`, placeholder `Chat here`, the original white circular checkmark sender, and idle copy `Ask the assistant for help with your stick figure.` The bounded V2 grammar produces one non-mutating preview; Cancel is a no-op; one explicit Apply installs the canonical one-figure, three-pose, 12-frame, 12-FPS wave as one atomic transaction. The complete 11-joint/10-limb figure, Frames 1/5/9, held frames, and exact purple/green onion neighbors must remain visibly projected into the current responsive stage.
- Creator continuity: Creator remains a separate screen, but Creator → Back returns to the same mounted Stick project/history/storage/selection/view root rather than creating a fresh workspace. The remounted stage observer must bind to the current canvas so a viewport change cannot clip still-present content. After Back, Undo removes the complete AI wave and Redo restores it; manual-edit history and Save/Open continue to work.
- Evidence: the accepted exact dirty subset is 25 paths inside D-0028's maximum 26-path ceiling; `WorkspaceAiPanelShell.tsx` is the intentionally unchanged 26th path. Technical-manifest SHA-256 is `c0d13c9234087855784ad805410667e9d583ac0f03dbe847257db778fea39297` (48,147 bytes), browser-runner SHA-256 is `b1be9c83951f027687e2b50fa7df702ffa09b2e8213a7c30b6c7b0e9fa6220bd`, and the framed 25-path aggregate is `3bf3290f09f72787fa28db4f1a96003ab5f13e846358361e5805812904078aa2` over 1,065,268 bytes. All 15 receipts and 88 artifacts validate; 338 matcher/UI assertions, 15 accepted visible cases, 36 guarded rejections, 13 visible rejections, exact geometry/pixel/onion checks, Creator → Back at `1440×900 → 1024×768`, atomic Undo/Redo, Save/Open, TypeScript, lint non-regression, protected Phase 1–5/Drawing behavior, zero non-loopback traffic, and cleanup pass. The final proof-only correction makes closeout use the manifest-bound 25-path subset while retaining the strict 26-path maximum ceiling.
- Root causes: the first corrected review's missing figure/onion came from drawing canonical `960×594` rig coordinates 1:1 into a smaller responsive SVG without a `viewBox`, clipping the figure. The later Creator → Back disappearance came from the stage `ResizeObserver` remaining attached to the pre-Creator detached canvas, so the remounted canvas kept stale dimensions after viewport change and clipped the still-present figure/history.
- Consequence: the corrected Phase 6 result is accepted, technically Verified, published, and integrated. Canonical `main`, local `origin/main`, live GitHub `main`, and the publication branch matched the exact commit cleanly at `0/0`. Phase 7 and its Policy Gate remain **Unauthorized; Not started**. Arthur and the Project Manager must separately discuss and explicitly accept the deferred model, cost, retention, and optional paid-proof decisions before any Phase 7 implementation; no OpenAI/provider/search/Supabase/paid request or deployment occurred.

### D-0030 — Approve SPEC-0003 Tutorials and Cleaner Home Screen and authorize its one implementation

- Date: 2026-08-25
- Status: Accepted directly by Arthur and published/integrated in exact canonical-main commit `4cd1a98b2f0b53c89d50ede453d1ab14bccbc9c7`; SPEC-0003 is **Approved and active** and its one product implementation is **Authorized; Not started**.
- Decision: implement exactly one polished full-screen Tutorials page opened from the existing Home Tutorials card. The Home header is absent inside Tutorials. One Back control returns to Home and restores focus to the Tutorials card. The only `h1` is exactly `Welcome to Diamond Animator`. One large dominant blue-outlined static `Start Here` placeholder appears above exactly three smaller static placeholders in this order: `Create Your First Animation`, `Create with AI`, and `Finalize Your Animation`. All four show exactly `COMING LATER` and are non-interactive. Remove the inert Home `AI Credits` card.
- Boundaries: no Quick Help, Jump In, workspace selector, real tutorial media/player/delivery, unrelated font/Home/New/Open/Drawing/Stick/Creator/timeline/AI-panel/menu/notification/Dashboard behavior, provider/service/deployment work, or product change to SPEC-0001/SPEC-0002. The product implementation ceiling is the exact ten runtime/style/fixture/proof paths in SPEC-0003. D-0031 supersedes only D-0030's original assumption that the permanent tester could remain byte-identical before execution; it does not change this visible product decision.
- Consequence: the fresh executor at the published activation base correctly stopped before editing on D-0031's proven tester blocker. D-0031's separate prerequisite is now accepted and technically Verified pending publication/integration. The one product implementation remains Authorized/Not started and may begin only after that package is published and its corrected clean committed tester passes. The product executor then implements and proves the one authorized SPEC-0003 result, leaves the exact unpublished app available for Arthur's review, returns its packet, and stops.

### D-0031 — Authorize the narrow SPEC-0003 pre-implementation permanent-tester compatibility prerequisite

- Date: 2026-08-25
- Status: Accepted directly by Arthur and published as an authorization record at `57ef6ff5ff9d2da7ca3ab1e154aac9f506cc6b81`. The exact seven-path prerequisite is now accepted and **technically Verified pending publication/integration**. SPEC-0003 remains **Approved and active**, and its one product implementation remains **Authorized; Not started** but entry-gated on prerequisite publication and the corrected clean committed tester.
- Proven blocker: on clean published base `4cd1a98b2f0b53c89d50ede453d1ab14bccbc9c7`, before any SPEC-0003 tracked edit, `npm run test:spec0001-browser` failed during the first protected Stick flow with `Unexpected /api/ai request outside Drawing proof.` Mounted `StickFigureAiPanel` performs the already-integrated Phase 6 same-origin availability `GET /api/ai`; the older no-plan tester rejects every `/api/ai` request outside its Drawing step. This is a stale tester rule, not a Tutorials or Phase 6 product defect.
- Decision: authorize one separate invisible prerequisite with the exact seven-path ceiling in SPEC-0003 §8.1. It may change only the shared runner's no-plan availability handling and add its dedicated contract/fixture/recorder/validator/proof paths. It must recognize and test-fulfill exactly four ordered loopback `GET /api/ai` requests—two in `stick-1440x900`, then two in `stick-1024x768`—with exact Stick workspace header, no body/query/hash, and exact provider-free unavailable response. Those requests never reach the real route.
- Preserved meaning: no-plan proof remains 40 operations, 13 screenshots, 4 driver messages, 37 historical negative cases, and exactly one deterministic Drawing POST. Every other `/api/ai` request still fails; real-route and non-loopback counts stay zero; existing v1–v6 catalogs/schemas/contracts/action adapters/recorders/validators/finalizers and Phase 3–6 registration meanings remain byte-identical. Every product/runtime, package, dependency, configuration, provider, and deployment byte remains unchanged.
- Accepted evidence: the stopped prerequisite executor changed exactly the authorized seven paths. The independently revalidated 14,344-byte manifest at `output/spec-0003/permanent-tester-prerequisite/proof-manifest.json` has SHA-256 `adc3ad5534027470e436d92c0cbeaa6bc51fd5821303bb008f771b57da91edb3`, 11 receipts, and 29 artifacts. It records 40 operations, 13 screenshots, 4 driver messages, all 37 historical negatives, exactly four expected Stick availability GETs, exactly one mocked Drawing POST, and zero real API-route/non-loopback/provider attempts.
- Lifecycle consequence: Arthur/PM accepted the technical result and the stopped executor transferred exclusive ownership to the Control Plane Architect for this propagation. A later separate task must publish the exact seven technical paths plus reviewed records and rerun the corrected clean permanent tester. Only that synchronized publication SHA may start the fresh ten-path SPEC-0003 product executor. No Tutorials implementation, staging, commit, push, deployment, provider, external, or paid action is authorized by this closeout task.

### D-0032 — Accept the exact SPEC-0003 Tutorials implementation

- Date: 2026-08-31
- Status: Accepted directly by Arthur after reviewing the unpublished app copy; **Verified, published, and integrated** in exact canonical-main commit `9fa1b819aacc7823711af5838b79e70921469a93`.
- Decision: accept the exact ten-path SPEC-0003 result from base/HEAD `2cd25fd0bdfb8a775370641ffd65db315cc94532`. Home Tutorials opens the approved full-screen dark-navy/blue Tutorials page without the Home header. The page has the sole heading `Welcome to Diamond Animator`, one dominant static `Start Here` card, and exactly three smaller static cards—`Create Your First Animation`, `Create with AI`, and `Finalize Your Animation`—all marked `COMING LATER`. Back returns Home and restores focus to the Tutorials card. The inert Home AI Credits card is removed.
- Evidence: the fresh 3,084-byte manifest at `output/spec-0003/single-implementation/proof-manifest.json` independently validates at SHA-256 `4b63e1dc171cf9536aecbed067f271793dffc17137200afc8d136e1072d04d6d`, with six receipts and four artifacts. The browser result covers `1440×900`, `1024×768`, and `390×844`, three screenshots, 14 assertion groups, exact hierarchy/order/copy/static semantics, Back/focus, no overflow, zero console/page errors, and zero API/external requests. TypeScript, focused lint, and diff checks pass; full lint retains the accepted 5-error/72-warning baseline with no accepted changed-path finding.
- Proof recovery boundary: the original accepted manifest/screenshots were erased. The regenerated source result is exact replay of the accepted archived ten-path patch stream and the visible result is unchanged, but the fresh manifest and screenshots are not claimed byte-identical to the historical manifest whose SHA-256 began `1059c0…`.
- Consequence: SPEC-0003's one product implementation and reviewed records were published in exact 20-path product commit `9fa1b819aacc7823711af5838b79e70921469a93`, parent `2cd25fd0bdfb8a775370641ffd65db315cc94532`, message `Implement SPEC-0003 Tutorials screen`. The clean committed permanent tester passed 40 operations, 13 screenshots, 4 driver messages, all 37 historical negatives, four ordered provider-free Stick availability GETs, one deterministic Drawing POST, zero real-route/non-loopback/provider requests, and complete cleanup; result SHA-256 is `8c3647aecec11f2660c5bc47b2e656da6fc1b19b816c9a317c709d759f661412` and browser-network-ledger SHA-256 is `514d163f272a938a7babb374cda98ea5f362a36f5566ee0e34afbab4db1130ac`. Immediately after product publication, the publication branch, canonical local `main`, local `origin/main`, and live GitHub `main` matched at that SHA with clean `0/0` synchronization. The later closeout containing this decision changes exactly eight canonical record documents and no product/proof byte; its own SHA and the repository's present refs must be read directly from Git. SPEC-0003 is fully Verified, published, and integrated. No tutorial media/playback, AI Assistant, Export, AI Project Finalizer, My Project behavior, SPEC-0001 Phase 7, provider/external/paid request, deployment, or next feature is authorized.

### D-0033 — Retire SPEC-0001 Phase 7 and open SPEC-0004

- Date: 2026-08-31
- Status: Accepted directly by Arthur; documentation transition only; not implementation authorization
- Decision: keep SPEC-0001 and preserve Phases 1–6 exactly as completed history, including the accepted Phase 6 pretend/deterministic single-stick-figure wave feature. Retire the former SPEC-0001 Phase 7, supersede it with [`SPEC-0004 — One-Time Stick Figure AI Animator`](specs/0004-future-real-ai-animator-requirements.md), and prohibit implementation of old Phase 7 as written.
- Product boundary: app users do not choose `off`, `mock`, or `live` Mode; those remain hidden development/server concepts. This initial retirement did not import partial examples or old Phase 7 content as final requirements; D-0034 records Arthur's later complete Proposed direction.
- Historical boundary: D-0009's accepted OD-01–OD-06/OD-09 and approved OD-07/08/10/11 remain intact for completed SPEC-0001. OD-12–OD-14 were deferred and never accepted; their former Phase 7 Policy Gate is retired rather than completed. All old Phase 7 file lists, commands, policy values, cost arithmetic, live-proof mechanics, acceptance criteria, and proof instructions remain readable only as superseded history with zero implementation authority.
- Consequence: this retirement changes no Drawing or Stick behavior, no Phase 1–6 runtime/fixture/proof byte, and no provider/API/key/cost/privacy/production state. It authorizes no external lookup, paid request, deployment, Spec Executor, implementation phase, staging, commit, merge, push, or publication. D-0034 preserves those prohibitions while making Phase 1 decision-complete for later separate authorization.

### D-0034 — Record the complete Proposed SPEC-0004 direction

- Date: 2026-08-31
- Status: Accepted directly by Arthur as product/spec direction; **not implementation authorization**
- Decision: SPEC-0004 is Stick Figure Workspace only and is the full eight-phase replacement for old SPEC-0001 Phase 7. It creates one new animation only on an eligible fresh Stick project. Preview never mutates; Apply commits the complete creation as one undoable action; Redo restores exact accepted bytes; manual editing, Save/Open, Undo/Redo, the white background, one existing layer, and derived horizontal-line head remain available. A persistent project-bound latch consumes only at Apply and thereafter every AI message returns “AI editing comes later; use manual tools.” with no local/provider call or mutation. Follow-up AI editing belongs to SPEC-0005.
- Routing/UI decision: Pretend AI is a $0 versioned parameterized local recipe system, not RAG or a claim of human-equivalent understanding. It requires a complete high-confidence match, never emits a partial result, recognizes tested defaults/casual wording/negation/ambiguity, and otherwise returns needs-real-AI/unavailable unless the later Terra policy allows handoff. Generate Frames is the only functioning Stick task. Low/Medium/High/Extra High Reasoning may be visibly selectable but is inert. No user-visible Mode exists, and there is no Luna/Sol auto-switch.
- Intended original path: GPT-5.6 Terra is the intended server-only original-creation model. It returns strict plan/tool data for the same manually editable project executor, never raw executable code or a locked format. No real model configuration/request, API key, official-source lookup, price, retention assertion, spend, deployment, or paid proof is authorized by this decision.
- Quality/release decision: the product claim is measured, not presumed: record editable first-preview time, mechanical/body/timeline checks, representative free/original prompts, rendered/playback review, and Arthur's final side-by-side acceptance before claiming faster/better than his normal roughly week-long manual process or one-day completion. High FPS alone is not a smoothness claim; Phase 2 provides meaningful in-betweens. The private development/owner dashboard is not claimed to track Stick/Terra yet and the removed Home AI Credits card stays removed.
- Phase boundary: Phase 1 alone is decision-complete and provider-free: fixed wave/jump/bow/dodge plans through a strict single-figure/one-layer 8–24-frame, 12-or-24-FPS executor with Preview/Apply/Cancel, stale/no-op/history/storage/manual-edit proof. It has no natural-language matcher, Terra, smooth motion, multiple figures, layer/background changes, working Task/Reasoning, or post-Apply editing. Phases 2–8 are sequential and require their own later authorizations.
- Named later owner gates: Phase 3 selects the final compatible duration/frame/FPS cap and multi-figure limits; Phase 6 rechecks same-day official Terra alias/pricing/retention/privacy/budget and records explicit live-request authority; Phase 7 sets the benchmark/quality/comparison and one-repair gate; Phase 8 accepts cost/credits/auth/rate-limit/privacy/retention/logging/release policy. No task may invent a closed gate.
- Consequence: this is documentation/control-plane work only. It changes no product/runtime/fixture/proof byte, provider/API/key/cost/privacy/production state, other worktree, staging, commit, merge, push, or publication. A later explicit Phase 1 Spec Executor authorization remains required.

### D-0035 — Approve and authorize SPEC-0004 Phase 1 only

- Date: 2026-09-01
- Status: Accepted directly by Arthur; **Approved for Phase 1 only — Authorized; Not started.** This decision also authorizes the reviewed docs-only activation commit and safe local canonical-main fast-forward, but no GitHub push.
- Decision: Arthur accepts the complete SPEC-0004 direction and authorizes exactly Phase 1, **Safe One-Time Animation Builder**. The exact Phase 1 command language, small 8–24-frame at 12-or-24-FPS proof bound, fixed wave/jump/bow/dodge fixtures, transaction/history/storage/manual-edit proof, and exact allowlist in SPEC-0004 remain unchanged. Its single-figure, one-existing-layer, white-background, derived-horizontal-line-head, fixed-fixture boundary is accepted exactly.
- Preserved exclusions: this authorization adds no natural-language matcher, Terra or other provider, API key/request, paid work, smooth-motion engine, multiple figures, layer creation/removal, background or color generation, working Task/Reasoning UI, user-visible Mode, post-Apply AI editing, model routing, Drawing change, package/config/environment/dashboard change, deployment, or live/provider proof. It does not revive or authorize old SPEC-0001 Phase 7; SPEC-0001 Phases 1–6 and the Phase 6 deterministic wave remain completed protected history.
- Lifecycle: after this docs-only activation is committed and locally integrated into a clean canonical `main`, one dedicated Phase 1 Spec Executor starts in Plan mode. Before implementation it must re-run the required boot, confirm the then-current canonical SHA, empty index, exclusive worktree ownership, exact Phase 1 allowlist, and no scope conflict. It may implement only Phase 1, return its Implementation Review Packet, and stop. Phases 2–8 remain unauthorized and each require a later explicit authorization after the prior phase is accepted, integrated, and recorded.
- Consequence: this activation changes only the eight reviewed control-plane/spec paths. It authorizes no other worktree mutation and no push, provider, external, paid, production, or product action.

### D-0036 — Accept the exact SPEC-0004 Phase 1 engine

- Date: 2026-09-01
- Status: Accepted directly by Arthur after reviewing the unpublished private app copy; **Verified, published, and integrated** in exact commit `086420e6b0cbe683adbb8f0024e65a2fc1d68d6d`.
- Decision: accept the exact 17-path Phase 1 result from activation base/HEAD `9fae072359f3c0d10f1ed2bcee8da9ebc11d54ec`. It adds the strict action-neutral `set_timing` / `create_key_pose` / `hold_pose` / `finish` plan, one shared executor for fixed wave/jump/bow/dodge fixtures, an isolated candidate, one atomic Apply, a durable project-bound consumed latch, exact Undo/Redo and V1/V2 Save/Open behavior, post-Apply manual-tools response, and normal manual editability of every generated joint.
- Review-box boundary: the blue `PRIVATE REVIEW` fixture picker was injected by the dedicated browser-proof script into an isolated temporary copy only. It is not imported or loaded by the product. Its identifying tokens are absent from `app`, `src`, and `public`; the temporary copy/server were removed after review. The proof script may remain as developer-only technical evidence on GitHub, but the floating box must never appear in the real app.
- Evidence: the 15,683-byte technical manifest `output/spec-0004/phase-1/proof-manifest.json` passed 126 independent checks at SHA-256 `ee3a92edf8f4227dfa91ec3b84de3599fa158d5fb5f3df83155ab5192c076e4a`. It binds 14 receipts and 27 artifacts, 707 assertions, four valid fixtures, 26 invalid-plan rejections, 12 real-browser flows, 11 screenshots across both required viewports, TypeScript, focused lint, unchanged full-lint baseline, diff checks, manual edit/history/storage/Creator/Drawing regressions, and zero external/API/provider requests.
- Preserved exclusions: no natural-language matcher, Terra/provider/API/key/paid request, smooth interpolation, multiple figures, layer/background/color change, working Task/Reasoning control, user-visible Mode, post-Apply AI editing, Drawing runtime change, package/config/environment/dashboard/deployment change, or Phase 2 behavior is accepted here.
- Lifecycle consequence: the stopped executor transferred exclusive ownership to the Control Plane Architect, which propagated the result and later published the exact 17 technical paths plus 11 reviewed control-plane/tree paths in 28-path commit `086420e6b0cbe683adbb8f0024e65a2fc1d68d6d`, parent `9fae072359f3c0d10f1ed2bcee8da9ebc11d54ec`, message `Implement SPEC-0004 Phase 1 animation builder`. The required clean tester passed 40 operations and 13 screenshots with zero real API/nonloopback/provider requests; publication branch, local `main`, local `origin/main`, live GitHub `main`, and GitHub API matched with clean `0/0` synchronization. The blue private review box was not published. D-0037 later authorizes Phase 2 only without changing the accepted Phase 1 result.

### D-0037 — Approve and authorize SPEC-0004 Phase 2 only

- Date: 2026-09-01
- Status: Accepted directly by Arthur; **Approved for Phase 2 only — Authorized; Not started.** Implementation remains entry-gated on separate publication/integration of this docs-only activation record.
- Decision: authorize exactly Phase 2, **Pose and Smooth Motion Engine**, as the next one-day milestone. One deterministic local engine accepts the unchanged fixed Phase 1 wave/jump/bow/dodge plans, normalizes their important 11-joint poses against the built-in humanoid segment lengths, creates cubic-eased shortest-turn in-betweens, and bakes every output slot into an independent ordinary Stick keyframe before Preview. The same hidden engine is intended for future Pretend AI recipes and future Terra structured plans, but Phase 2 connects neither one.
- Human-ownership rule: no generated `hold`/`tween` owner span, persistent controller, hidden AI data, green tween range, lock, regeneration, or snap-back may exist. Every important, in-between, and repeated-looking frame owns unique IDs/content and is manually editable. Editing one applied frame must persist and leave every unrelated frame digest unchanged. Generation/body-length rules end at Apply and may never override a later human edit.
- Exact bounds: preserve one figure, one existing layer, white background, derived horizontal line head, 8–24 frames, 12/24 FPS, and all Phase 1 Preview/Cancel/Apply/latch/stale/concurrency/idempotency/no-op protections. Exact input/output body, spacing, 480-pixel hip, 170-degree angular-turn, bounds, cubic easing, arc, materialization, proof-manifest, eight-path implementation allowlist, and private-review rules are in SPEC-0004 Phase 2.
- Review-box decision: Arthur must not see the former blue private review box again, even in the unpublished Phase 2 copy. The executor must provide four disposable normal-looking loopback copies/links, one preloaded sample each, using only the normal Stick canvas/timeline/AI transaction/manual controls. There is no visible tester picker/overlay/control/query/route/asset/import, and no review helper is published.
- Preserved exclusions: no natural-language matcher or new recipe, Task/Reasoning behavior, Terra/provider/API/key/paid request, multiple figures, duration/frame/FPS expansion, background/layer work, Drawing or workspace-integration change, dashboard/cost implementation, post-Apply AI editing, package/config/environment/deployment work, or Phase 3–8 implementation.
- Lifecycle: after this exact activation is reviewed, published, and integrated into clean canonical `main`, one new dedicated Phase 2 Spec Executor starts in Plan mode from that activation SHA with exclusive worktree ownership and the spec's exact eight-path ceiling. It returns technical proof and stops before control-plane edits or Git publication. This decision does not itself authorize stage, commit, push, or another phase.

### D-0038 — Preserve future Terra economics and unified-workspace ideas without authorizing them

- Date: 2026-09-01
- Status: Accepted planning direction only; **not implementation or paid-request authorization**.
- Terra economics note: a later Terra path should send compact important-pose/timing plans and generate in-betweens locally, use a free local recipe first only on a safe full match, allow at most one separately budgeted bounded pre-commit repair inside the isolated candidate, measure real tokens/cost in the private dashboard, enforce monthly budgets/credits, and never offer unlimited Terra use. Exact current official model alias, pricing, retention/privacy terms, and request policy must be rechecked from official sources at the later paid-provider gate; no price or paid call is accepted now.
- Unified workspace note: Arthur may later replace the separate Drawing/Stick choice with one Animation Workspace that has one project, timeline, canvas, Save/Open, history, and AI surface with typed Stick and Drawing layers. This likely needs its own later specification after Phase 2 and before broad real-AI UI because project data, editing tools, and regression boundaries would change. Phase 2 performs no workspace integration, Drawing change, layer change, or migration.
- Consequence: these notes must survive future Project Manager handoff and inform later specs, but they create no current executor, route/model/client/dashboard/UI/storage/migration/provider/external/paid/deployment action and do not widen D-0037.

### D-0039 — Accept the exact SPEC-0004 Phase 2 smooth-motion engine

- Date: 2026-09-02
- Status: Accepted directly by Arthur after reviewing the four ordinary unpublished samples; **Verified, published, and integrated** through GIT-035.
- Decision: accept the exact eight-path result from activation base/HEAD `70bf7b0799bcff8d703525bcb50c378b8a122ebf`. It adds one hidden local `phase-2-baked-motion` materializer that normalizes the fixed 11-joint/10-segment humanoid, uses deterministic cubic smoothstep and shortest signed turns between important poses, and bakes every important, in-between, and repeated-looking slot into one complete independently owned ordinary keyframe. The Phase 1 held-frame materializer remains the default unless the explicit Phase 2 option is selected.
- Human-ownership result: accepted output has no hold/tween/controller/hidden-AI ownership, lock, regeneration, or snap-back. Every frame and pose has distinct identity/content; a later normal joint edit stays on that one frame and the engine has no authority after Apply. Preview/Cancel/failure are no-ops; Apply remains one history action; Undo/Redo and Save/Open preserve exact accepted editable data.
- Evidence: the exact 11,493-byte technical manifest `output/spec-0004/phase-2/proof-manifest.json` has SHA-256 `a6e656d930781b589a3350abec62000818fade6553638ada0899ec7183b24d3f`. Executor-time independent validation passed 210 checks while four temporary review servers were live. It references 10 technical receipts within 19 unique artifacts and records 1,734 deterministic assertions, four valid fixed plans, 20 invalid-motion rejections, 40 browser flows, eight screenshots across two viewports, TypeScript/focused-lint/diff proof, unchanged 5-error/72-warning full-lint baseline with zero Phase 2 findings, protected Stick/Drawing behavior, and zero external/API/provider requests. CPA closeout later revalidated all 19 unique durable artifacts after server cleanup and preserved every accepted technical hash.
- Review and cleanup: Arthur saw only four ordinary no-blue-box loopback samples. No fixture picker, query flag, product route, public asset, visible tester control, or permanent product import exists. The temporary review servers are stopped and the four isolated copy directories are removed. That cleanup makes the live-only validator fail at its expected process-liveness check; the preserved successful 210-check receipt and complete offline hash/scope cleanup proof remain valid.
- Preserved exclusions: no normal chat connection, natural-language matcher, new recipe, Task/Reasoning behavior, Terra/provider/API/key/paid request, multiple figures, FPS/duration/frame expansion, background/layer/color/custom-shape work, Drawing/workspace integration, dashboard, deployment, post-Apply AI edit, Phase 2.5 app code, or Phase 3–8 work is accepted here.
- Publication result: GIT-035 published the exact accepted result in 20-path commit `e3ec6a33438c2f3d2e075b6477f18b8eb1b58e24`, parent `70bf7b0799bcff8d703525bcb50c378b8a122ebf`, message `Implement SPEC-0004 Phase 2 motion engine`. The clean permanent tester passed 40 operations, 13 screenshots, four driver messages, all 37 historical negatives, one deterministic mocked Drawing POST, zero real-route/non-loopback/provider requests, and complete cleanup. Branch/local-main/origin/live-GitHub/GitHub-API equality and clean `0/0` synchronization were verified.
- Lifecycle consequence: Phase 2 is closed. GIT-036 is the next docs-only task: reconcile and separately publish the Phase 2.5 Action Timing and Spacing activation onto current main before one new Phase 2.5 executor starts. No Phase 2.5 app code is authorized by this publication record.

### D-0040 — Approve and authorize SPEC-0004 Phase 2.5

- Date: 2026-09-02
- Status: Accepted directly by Arthur; **Approved for Phase 2.5 only — Authorized; Not started; GIT-036 activation published.**
- Decision: insert **Phase 2.5 — Action Timing and Spacing Engine** between Phase 2 and Phase 3. It is shared local infrastructure for later $0 Pretend-AI recipes and later Terra original plans, but connects neither route. One strict `stick.action-timing/v1` sidecar binds to the exact animation plan and selects one supported profile for every adjacent important-pose transition. Natural callers use explicit `ease_in`, `ease_out`, `ease_in_out`, paired `impact`/`recovery`, with `ease_in_out` as the required safe natural default policy. `constant` is valid only under an explicit structured `mechanical_explicit` intent. Phase 2.5 interprets no user language.
- Exact math: `ease_in = u²`, `ease_out = 1-(1-u)²`, `ease_in_out = 3u²-2u³`, `constant = u`, `impact = u³`, and `recovery = 1-(1-u)³`. Wider consecutive baked-frame position gaps mean faster movement; tighter gaps mean slower movement. The timing progress drives Phase 2 hip and shortest-turn segment interpolation before the unchanged canonical-length rebuild/round/bounds checks.
- Human-ownership rule: important poses → one timing choice per adjacent transition → local in-betweens → every result baked as a complete independent ordinary Stick keyframe before Preview. No live tween, shared hold owner, timing controller, lock, regeneration, snap-back, AI-only data, or post-Apply overwrite is allowed. Preview/Cancel/failure no-op, one atomic Apply, manual editing, Undo/Redo, Save/Open, white background, one layer, derived line head, and every Phase 2 body/frame-ownership protection remain unchanged.
- Fixed proof direction: preserve timing sidecars for the exact Phase 1/2 wave/jump/bow/dodge plans; the bow must use deliberately unequal profiles. Add one technical-only 24-frame/12-FPS detailed jump with stand → crouch → launch → peak → landing contact → knee-bend compression → recovered stand and profiles that prove fast launch, slow near the peak, accelerating fall, impact, and recovery. It is not a new chat recipe or the Phase 3 timeline-cap choice. A three-second run, variable FPS/duration/final frame cap, and multiple figures remain Phase 3/4/6 matters after their later gates.
- Cross-phase links: Phase 4 free recipes must emit important poses plus the complete supported timing sidecar and own any natural-language decision for robotic/constant pace. Phase 6 Terra must emit the same strict contract for original supported Stick scenes. Phase 7 must visibly check realistic action timing/spacing as well as body integrity and readability. Dictated “Tara” means Terra.
- Preserved future scope: backgrounds, colors, custom stick shapes/rigs, accurate non-humanoid dolphin/dinosaur bodies, layers, AI onion control, post-Apply AI editing, Drawing/workspace integration, dashboard/provider/API/paid/deployment work, and Phase 3–8 implementation remain outside Phase 2.5 and need later specs/authorizations.
- Exact later executor ceiling: `src/lib/ai/stickFigureMotionEngine.ts`, `src/lib/ai/stickFigureCommandExecutor.ts`, `scripts/fixtures/spec0004-stick/v3/phase25-timing-cases.json`, `scripts/spec0004-stick/phase25BrowserProof.ts`, `scripts/spec0004-stick/recordPhase25Proof.ts`, `scripts/spec0004-stick/validatePhase25Proof.ts`, and `scripts/validateStickFigureActionTiming.ts`, plus ignored `output/spec-0004/phase-2.5/**`. Recommended executor is `gpt-5.6-sol` at `xhigh`. The later private review uses four disposable normal-looking non-3000 loopback copies only; no blue box, query, picker, overlay, special control, product route/import/asset, or permanent review surface may exist.
- Current-evidence boundary: GIT-034 published Phase 2's activation at exact docs-only commit `70bf7b0799bcff8d703525bcb50c378b8a122ebf`; D-0039 accepted the exact result; and GIT-035 published it in exact 20-path commit `e3ec6a33438c2f3d2e075b6477f18b8eb1b58e24`. Phase 2 is Verified, published, and integrated.
- Publication/lifecycle: GIT-036 published this exact docs-only activation in commit `a755f892d7737c6a10d9c381ec59c1e2fdba4d47`, parent `2d94cd79f0af16bfa3010c6f3dfc74fd13f7471c`, message `Authorize SPEC-0004 Phase 2.5 timing engine`, with exactly 11 reviewed documents and no app/runtime/fixture/test/proof byte. One separate new Plan-mode Phase 2.5 executor may now begin from the final clean record SHA. No other phase is authorized by D-0040.

### D-0041 — Accept Phase 2.5 as the timing primitive and authorize final Phase 2.6 foundation

- Date: 2026-09-02
- Status: Accepted directly by Arthur. **Phase 2.5 is accepted and technically Verified in its dedicated worktree, pending GIT-037 publication/integration. Phase 2.6 is Approved/Authorized/Not started and entry-gated on GIT-037.**
- Phase 2.5 decision: accept the exact seven-path result from base/HEAD `f131e75aafccec0d1b8ecb717e2d95b518355d39` only as the shared `stick.action-timing/v1` timing/spacing primitive. It provides exact plan/project/transaction binding, the six bounded profiles `ease_in`, `ease_out`, `ease_in_out`, explicit-mechanical `constant`, paired `impact`/`recovery`, timing-bound deterministic IDs, and complete independent ordinary keyframes through the existing Phase 2 geometry/transaction path. It connects neither Pretend AI nor Terra and adds no normal UI, route, provider, API, paid request, or stored timing controller.
- Evidence: the accepted 14,601-byte manifest at `output/spec-0004/phase-2.5/proof-manifest.json` has SHA-256 `783e6396cf994ce48fb9d7c94dc58674594dd545f888a9c25fe3c1f654a788d1`. Its 21 bound artifacts, 12 technical receipts, 1,609 unit assertions, 30 invalid timing cases, 20 inherited Phase 2 cases, 40 browser flows, eight screenshots, TypeScript/focused-lint/full-lint-non-regression/diff/permanent-browser evidence, and zero external/API/provider requests revalidated at 250 independent checks before exact review cleanup. Every accepted technical source hash remains frozen.
- Human-naturalness boundary: the four old wave/jump/bow/dodge samples remain rejected as natural-action evidence. The jump inherited a raised wave hand and floated near the apex; the bow compressed its readable action into roughly `0.33s` halves without a settling beat; and the dodge reversed at maximum speed while both feet slid. Those failures came from action plans, poses, frame allocations, contacts, weight, and balance—not from the accepted timing formulas. No control-plane statement may call those samples natural, accepted natural quality, professional motion, or final action evidence.
- Phase 2.6 decision: approve and authorize exactly the reviewed **Phase 2.6 — Natural Action Planning, Weight, and Intent Guard** amendment as the final shared motion-foundation correction; there is no Phase 2.7. Its one `stick.action-foundation/v1` contract is shared by later free/Pretend-AI recipes and future Terra plans and binds exact requested action, action-pure beats/poses, contact/support/weight/balance/landing recovery, the accepted timing sidecar, and the animation plan. It covers wave, jump/hop, bow, dodge, walk, run, and the technical `reach_up` structural case, including fail-closed wave-with-clap/hop, jump-with-wave, and hop-with-head-nod/shake negatives while allowing only mechanically tied balance motion.
- Fixed Phase 2.6 boundary: all required positive samples use exact 12 FPS allocations; output remains ordinary independent editable keyframes; both pre-bake and post-bake validation fail closed before Preview; permanent browser proof must pass before Arthur receives six ordinary non-3000 review links; and no blue box, query/picker/overlay/special control, provider/API/Terra call, chat or recipe routing, Phase 3/UI/background/layer/color/custom-rig/multiple-figure/post-Apply work is allowed. The reviewed one-file source SHA-256 before lifecycle/Git-ID reconciliation is `dc7f968b0aab4a44bd3f70557a1b0e708a974cd2dd1c9050775ec56cfed83e56`.
- Lifecycle: the Phase 2.5 executor is stopped. This Control Plane Architect owns the accepted worktree sequentially, revalidated and froze the exact technical bytes, imported the reviewed Phase 2.6 spec, updated only canonical records/tree, cleaned only the four manifest-recorded review process groups/ports/copies, and stops with an empty index. GIT-037 is the next separate publication task and may publish only the accepted seven technical paths plus the 12 reviewed control-plane/tree paths. Only after that clean canonical-main integration may one fresh Plan-mode Phase 2.6 executor begin under D-0041. This decision authorizes no staging, commit, merge, push, publication, deployment, external/provider/paid action, or Phase 2.6 implementation in the current task.

### D-0042 — Close GIT-037 and require separate Phase 2.6 implementation authorization

- Date: 2026-09-02
- Status: Accepted through Arthur's latest explicit publication instruction. **Phase 2.5 is Verified, published, and integrated for timing/spacing only. Phase 2.6 is decision-complete/Not started; implementation authorization is pending.**
- Publication decision: GIT-037 published the exact seven accepted Phase 2.5 technical paths plus 12 reviewed control-plane/tree paths in commit `16799539fb7db31e345a878aa892d4485115188b`, parent `f131e75aafccec0d1b8ecb717e2d95b518355d39`, message `Publish SPEC-0004 Phase 2.5 timing and Phase 2.6 foundation`. Clean canonical `main` was fast-forwarded and pushed normally without force; publication branch, local main, origin, live GitHub, and GitHub API matched at `0/0` before the separate record-only reconciliation.
- Proof decision: all seven accepted technical hashes and the 14,601-byte manifest SHA-256 `783e6396cf994ce48fb9d7c94dc58674594dd545f888a9c25fe3c1f654a788d1` remained exact. Offline validation matched all seven source and 21 artifact bindings. The clean publication tester passed 40 operations, 13 screenshots, four driver messages, all 37 historical negatives, one deterministic mocked Drawing POST, zero real API/non-loopback/provider requests, and complete cleanup. Its result SHA-256 is `de043f7b3daa647961b952bc48890c79a8f8a858f6ffc9d9afc52623bdf636b3` (84,507 bytes); browser-network-ledger SHA-256 is `683ea5224642a884a94eda5e1a0aaf146d6ebe1bd58787694903789d351e87cf` (23,017 bytes); server-network-ledger SHA-256 is `b8f6ad815ef3b6cb95e7ddf7c0115f3db54ed27096a80912a1de6c41111fc50b` (5,082 bytes).
- Lifecycle clarification: D-0041's reviewed `stick.action-foundation/v1` contract and exact eight-path ceiling remain decision-complete and unchanged. This latest instruction supersedes only D-0041's automatic executor-start wording: no Phase 2.6 Spec Executor is authorized or active, and Arthur must separately authorize implementation before a fresh Plan-mode executor starts from the then-current clean canonical `main`.
- Boundary: the Phase 2.5 publication does not accept its old samples as natural-action evidence. Jump's inherited wave hand/floating, the over-compressed bow, and dodge's instant reversal/sliding feet remain binding Phase 2.6 inputs. This decision starts no Phase 2.6 code, review copy, provider/API/Terra work, deployment, Phase 3 work, or Phase 2.7.

### D-0043 — Supersede rejected SPEC-0004 Phase 2.6 with the professional shared motion-engine spec

- Date: 2026-09-04
- Status: Accepted directly by Arthur as the original product/spec direction. **Its Phase 1 decision remains accepted/published; D-0046 supersedes only this decision's former seven-phase numbering/structure after the first former SPEC-0005 Phase 2 result was rejected.**
- Rejected-result decision: one SPEC-0004 Phase 2.6 executor ran in the separate `/Users/arthurcarlin/.codex/worktrees/8de8/stick-animation-app` worktree from canonical `4c1da7fa4ea14ed82af950f7ed748b86387a7e0a` and produced the former exact eight-path result with an empty index. Its ignored proof manifest had SHA-256 `900f1faf0fbdddbccd8301a62628cd6889990e1a3c75ca2977fdcd56995a8d5e` and reported green schema, binding, deterministic, browser, editability, and zero-egress checks. Arthur rejected the visible motion after human review. No implementation byte is accepted, propagated, published, integrated, or reusable by this decision.
- Root-cause decision: the rejected design validated action-specific full joint-coordinate recipes instead of authoring coordinated motion from action-independent intent. Its browser flow proved only Play entered `playing` before an immediate Pause, its frozen expected frames were regenerated through the same implementation path, and its still screenshots could not prove playback quality. Technical green status, exact labels/counts/digests, and self-generated goldens do not establish professional motion.
- New product contract: create [`SPEC-0005 — Professional Shared Stick Motion Engine`](specs/0005-professional-shared-stick-motion-engine.md) with exactly seven sequential phases: accepted reference/full-playback quality proof; action-independent whole-body key-pose creation; support/weight/balance/ground mechanics and required-pose insertion; natural paths/timing/gravity/follow-through; distinct real walk/run; semantic wave/jump/hop/bow/dodge rebuilds; and one provider-free Pretend-AI/future-Terra planning door/final gate. Both planner types emit the same strict `stick.movement-goal/v1` intent and use the same local engine; planners do not return raw code, raw joint coordinates, or a second animation format.
- Naming clarification: this decision supersedes D-0034's placeholder sentence that reserved the name SPEC-0005 for follow-up AI editing. SPEC-0005 is now the shared motion engine. Follow-up editing/recoloring/refinement/continuation remains a separate unnamed future specification and is not authorized here.
- Quality decision: protect the published SPEC-0001 12-frame wave as the older accepted readability reference. Full-playback proof must observe ordered `0 → … → last → 0` within bounded real elapsed time, use independently authored accepted-reference and bad-motion catalogs, record time-based playback evidence, and require Arthur's ordinary-app action review. A Play/Pause toggle, compilation, labels/counts, numeric proxies, or exact implementation-generated golden output cannot be the sole quality gate.
- Human-ownership decision: all output remains complete ordinary independent editable Stick keyframes. Preserve Preview/Apply/Cancel, one atomic Apply, exact Undo/Redo and Save/Open, manual tools, onion, Creator round trip, fixed limb lengths, derived line head, one-shot/no-post-Apply behavior, and no live tween/shared hold/controller/hidden AI data/lock/regeneration/snap-back.
- Preserved scope: SPEC-0004 Phases 1, 2, and timing-only Phase 2.5 remain Verified/published/integrated. Phase 2.6 is rejected and superseded, not completed. SPEC-0004 is paused before Phase 3 until all seven SPEC-0005 phases are accepted/published/integrated. No Drawing, background, layer, multiple-figure, color, custom-rig, sound, Task/Reasoning behavior, follow-up AI, dashboard, deployment, provider/client/API/key/paid request, or SPEC-0004 real-provider phase is moved or authorized.
- Lifecycle: this task may change only the dedicated specification/control-plane worktree and returns a Spec Architect PM Review Packet with empty index. A later explicit instruction must review and publish/integrate this docs-only activation. Only then may one new Plan-mode SPEC-0005 Phase 1 Spec Executor start from that publication SHA under the exact six-path ceiling. Each later phase gets one new executor/worktree, independent manifest, visible human review where applicable, sequential CPA takeover, and separate publication before the next phase.

### D-0044 — Accept SPEC-0005 Phase 1 historical-wave quality gate

- Date: 2026-09-04
- Status: Accepted directly by Arthur and the Project Manager; **Verified, published, and integrated under completed GIT-039 at `2436a9414221e8ee7ef40151284cb8f4e069e828`.**
- Decision: Arthur accepts the published SPEC-0001 12-frame/12-FPS three-pose wave as SPEC-0005 Phase 1's protected readability floor after the ordinary-app review. This decision accepts the proof gate, not a runtime motion improvement: the historical held wave is not claimed to be the later professional smoothness ceiling, and no motion-engine/runtime byte changed.
- Exact accepted result: the stopped Spec Executor started from exact published activation/base/HEAD `2b4f00e7a122c196b2c0600144cd638b461bbb2f`, retained an empty index, and added exactly the six Phase 1 fixture/proof paths authorized by SPEC-0005. The accepted manifest is exactly 10,011 bytes at SHA-256 `af287680b7ae73fd4c543edf8076d9fbb7fb65474a5d4508f8b17dde56174e84`; its six source hashes remain frozen and exact.
- Technical evidence: the independent reference catalog binds the published request, command, and independently authored manual applied project; 13 required bad-motion mutations fail closed. Browser proof observed complete ordered `0..11..0` playback at `1440×900` in 1,262.4 ms and `390×844` in 1,085.0 ms, with 26 timestamped body samples, six ready/inward/outward landmark checks, 260 ten-limb checks, four ordinary/time-based screenshots, and zero external/API/provider requests or actionable browser errors. TypeScript, scoped lint, Git/diff/scope, the 11-case manifest-mutation contract, and the clean permanent tester passed; that tester recorded 40 operations, 13 screenshots, zero real API/non-loopback/provider traffic, and complete cleanup.
- Validation and cleanup: the preserved independent validation receipt passed 102 checks against the exact manifest before review cleanup. Later successful local review GETs appended 200 bytes to `review-server.log`, and normal process termination appended a seven-byte terminal-reset sequence, leaving 501 bytes total; the original 294 manifest-bound bytes remain an exact prefix at SHA-256 `637d481e2da0de2923489e85b4c8cd35db6456360d20e86c275a9aeb67a392ae`, while all other 12 artifacts and all six source files match complete bound hashes/sizes. The Control Plane Architect stopped only manifest-recorded PID/PGID `90076`, verified port `58451` closed, removed only the exact recorded disposable copy, and preserved durable ignored proof. No accepted source/proof byte was rewritten.
- Preserved rejection: the separate `/8de8/` SPEC-0004 Phase 2.6 result remains rejected, unaccepted, unpropagated, unpublished, and unintegrated. SPEC-0004 remains paused before Phase 3, and Phase 2.6 receives no correction/reuse authority.
- Publication result: GIT-039 published the exact six Phase 1 paths plus 14 reviewed control-plane/tree paths in commit `2436a9414221e8ee7ef40151284cb8f4e069e828`, parent `2b4f00e7a122c196b2c0600144cd638b461bbb2f`, message `Publish SPEC-0005 Phase 1 quality gate`. Local `main`, local `origin/main`, live GitHub `main`, and the clean publication branch matched at `0/0`. The required clean permanent tester passed 40 operations, 13 screenshots, four driver messages, three negative cases, zero non-loopback attempts, zero real API-route requests, zero policy violations, zero production leaks, zero console errors, and cleanup. Its 84,506-byte result has SHA-256 `bc7d5c978926b1b55babc8aee92827a4baef94adbca9570741b5dcc0e88f881d`.
- Consequence: Phase 1 is durably closed. This decision authorizes no provider/API/paid request, deployment, SPEC-0004 Phase 3, or edit to `/8de8/`.

### D-0045 — Authorize SPEC-0005 Phase 2 whole-body key-pose maker only

- Date: 2026-09-04
- Status: Accepted directly by Arthur and published/integrated under completed GIT-040 at `b5ddd5e3f4fb3b19e5c7c2be8a1bd35b0f8d6070`; **the resulting first Phase 2 implementation was visually rejected and D-0046 supersedes this implementation authorization without rewriting the historical decision.**
- Decision: after GIT-039 completed Phase 1 publication, Arthur authorized only SPEC-0005 Phase 2 — Action-Independent Whole-Body Key-Pose Maker. It must turn strict semantic body goals into strong grounded whole-body important poses: connected limbs and the body cooperate, limb lengths remain fixed, planted feet remain grounded, balance responds to the requested movement, and the existing timing/baker/transaction path produces complete independent editable Stick frames.
- Exact scope: one new dedicated Plan-mode Spec Executor is limited to the nine paths already frozen in SPEC-0005 Phase 2: `src/lib/ai/stickFigureMovementGoal.ts`, `src/lib/ai/stickFigurePoseEngine.ts`, `src/lib/ai/stickFigureMotionEngine.ts`, `src/lib/ai/stickFigureCommandExecutor.ts`, `scripts/fixtures/spec0005-stick/v2/whole-body-pose-cases.json`, `scripts/spec0005-stick/phase2BrowserProof.ts`, `scripts/spec0005-stick/recordPhase2Proof.ts`, `scripts/spec0005-stick/validatePhase2Proof.ts`, and `scripts/validateStickWholeBodyPoseEngine.ts`.
- Lifecycle: no executor starts from this unpublished record. After separate review, publication, and integration of this activation, one new Spec Executor starts in Plan mode from that exact publication SHA, refreshes the conflict audit, implements and proves only Phase 2, returns its Implementation Review Packet, and stops before control-plane propagation or Git publication.
- Boundaries: Phase 2 does not add Phase 3 weight/support/required-pose insertion, Phase 4 acceleration/deceleration/arcs/gravity, Phase 5 gait, Phase 6 final action recipes, or Phase 7's shared planner door. It adds no UI, provider, Terra, API, paid request, external request, deployment, Drawing/workspace integration, or SPEC-0004 change. SPEC-0004 remains paused; the rejected `/8de8/` Phase 2.6 result remains rejected, unpublished, and untouched. This control-plane task begins no implementation and authorizes no staging, commit, push, merge, or publication.

- Historical publication/result note: GIT-040 published this 12-document activation as exact canonical-main commit `b5ddd5e3f4fb3b19e5c7c2be8a1bd35b0f8d6070`, message `Authorize SPEC-0005 Phase 2 whole-body poses`. One later executor produced a technically green unpublished result, but Arthur rejected its visible motion. Its code/proof bytes were never accepted, propagated, committed, or published, and its disposable review app/worktree was removed. STICK-017 is closed as rejected/superseded, not completed.

### D-0046 — Restructure SPEC-0005 around a permanent body-safety foundation

- Date: 2026-09-04
- Status: Accepted directly by Arthur for the specification/control-plane correction and published/integrated under completed GIT-041 at `46b97556ec4e9c9249a1ff354c546f7b1c32ea4d`. Its original publication-stop status is historical; D-0048 now accepts the corrected Phase 2 result as technically Verified pending publication. Phases 3–8 are Unauthorized; Not started.
- Rejection/root-cause decision: the rejected pose solver had no single authoritative safe/natural body frame. It selected two-bone elbow/knee branches mostly by a fixed screen-side sign instead of enumerating safe candidates and using prior-pose/intent continuity; `recover` did not converge toward rest; unspecified arms could form large W movement; global-angle interpolation had no anatomical gate after interpolation, rounding, or contact repair; and its oracle allowed bad-but-test-legal branches, near-locked moving knees, unwanted motion, overshoot, unsafe in-betweens, and loop snaps. Green automation cannot replace Arthur's visible acceptance.
- Eight-phase decision: keep accepted/published Phase 1 unchanged. New Phase 2 is **Planner-Independent Body Safety Gate and Foundation**. The former Phase 2 whole-body key-pose maker becomes Phase 3 and must be rebuilt fresh on the accepted safety foundation. Former Phases 3–7 become Phases 4–8 in the same order: mechanics/weight/contact and required-pose insertion; paths/timing/gravity/in-betweens; walk/run; semantic core actions; and one shared provider-free Pretend-AI/future-Terra planning door. SPEC-0005 has no Phase 2.5, Phase 2.6, Phase 2A/2B, or second corrective spec.
- Permanent pipeline: `movement goal → enumerate whole-body pose candidates → important-pose safety gate → mechanics/required-pose insertion → path/timing bake → post-rounding and post-contact-repair safety gate → final whole-animation semantic/continuity gate → Preview`. The safety kernel is shared and planner-independent. Planner identity, action name, fixture ID, prompt text, alternate materializer, or trust metadata cannot bypass it. No safe complete sequence means fail closed before Preview.
- Body-safety decision: the exact built-in `humanoid-11-v1` 11-joint/10-segment topology, top-left/`+x` right/`+y` down sign convention, signed-bend math, fixed-length rules, elbow/knee corridors and narrow straight-neutral knee exception, torso/head limits, crossing/clearance/contact/continuity/overshoot/loop constraints, and `relax`/`recover`/`balance` semantics are fixed in SPEC-0005 §§3.2–3.3. Both two-bone IK solutions are enumerated; unsafe candidates are removed; deterministic full-sequence selection uses semantic target accuracy, prior-pose branch/turn continuity, recovery direction, support/balance, unrequested motion, transported neutral, joint travel, and a canonical tie-break. The built-in humanoid has no custom/unusual-motion escape flag.
- New Phase 2 boundary: exactly eight tracked paths are authorized: `src/lib/ai/stickFigureBodySafety.ts`; `src/lib/ai/stickFigureMotionEngine.ts`; `src/lib/ai/stickFigureCommandExecutor.ts`; `scripts/fixtures/spec0005-stick/v2/body-safety-cases.json`; `scripts/spec0005-stick/recordPhase2SafetyProof.ts`; `scripts/spec0005-stick/validatePhase2SafetyProof.ts`; `scripts/validateStickBodySafety.ts`; and `scripts/validateStickBodySafetyIntegration.ts`, plus ignored `output/spec-0005/phase-2/**`. The two existing runtime files may change only for the one closed safety-qualified SPEC-0005 seam and bypass rejection. Every component/UI/existing fixture/project/history/storage/provider/package/config/control-plane path is read-only to the future executor.
- Review-delivery clarification (2026-09-05): Phase 2 remains technical-only and creates no new generated motion, normal product/UI change, blue box, picker, route, test control, injected fixture, preloaded new action/project, query/hash flag, or visible safety claim. After every technical gate passes, Arthur explicitly authorizes and requests exactly one fresh ordinary regression-review app instance served unchanged directly from the exact unpublished executor worktree at one loopback-only non-`3000` root URL. The independently validated manifest must bind that URL/port, launch command, PID/process group, worktree path, exact base/HEAD/index/eight-path dirty source identity and hashes/sizes, startup/network logs, purpose `existing_app_regression_smoke_only`, explicit false safety/new-motion claims, and exact cleanup instructions. Arthur's check concerns only whether existing app behavior still looks and works normally; it cannot demonstrate or accept Phase 2 safety because no normal route invokes the new seam. Phase 3 remains the first human review of new safety-gated motion.
- Proof decision: the independent oracle may not import the runtime safety/motion/executor modules. At least 10,000 fixed-seed valid/invalid property candidates plus mirrored counterparts must cover the exact rejected reach branch, impossible reach, zero/rounded-short/stretched segments, elbow/knee flips and singularity, moving-knee near-lock/dead-corridor/inversion, head/torso inversion/kink/snap, unsafe in-between with safe endpoints, rounding-only and contact-repair-only faults, recover-away-from-rest, unspecified W arms, unsolicited hip lowering, crossings, unsupported contact and foot slide/lift, unbalanced grounded poses, overshoot/loop teleport, invalid mirror asymmetry, unreachable no-safe candidate, and alternate-materializer/planner bypass. Current live-tree focused validators reprove SPEC-0005 Phase 1 and accepted SPEC-0004 Phase 1/2/2.5 outputs/defaults plus editability/transaction/history/storage/onion/Creator/zero-egress protections; the permanent tester protects unrelated app behavior from an isolated clean D-0046-publication clone. Historical manifests/receipts retain their published hashes but their old activation-HEAD/dirty-set/live-process checks are not falsely claimed as rerun after approved shared-source extension.
- Lifecycle consequence: GIT-041 separately published D-0046 at `46b97556…`. One executor then implemented the exact eight-path technical Phase 2 and stopped; D-0047 governs its publication-stop correction. The former rejected implementation may not be reused. Phase 3, SPEC-0004 Phase 3, provider/API/paid work, Drawing/workspace integration, deployment, staging, commit, merge, push, or publication remain unauthorized.

### D-0047 — Correct the Phase 2 pre-bake API and Phase 4 airborne ceiling

- Date: 2026-09-05
- Status: Accepted directly by Arthur for this bounded specification/control-plane correction only. The correction is ready for independent PM review; runtime/proof correction, acceptance, publication, and Phases 3–8 are not authorized by this record.
- Publication-stop truth: the distinct body-safety executor in `/Users/arthurcarlin/.codex/worktrees/7dfc/stick-animation-app` stopped at detached GIT-041 base `46b97556ec4e9c9249a1ff354c546f7b1c32ea4d` with empty index and exactly eight Phase 2 paths. Its 17,826-byte manifest SHA-256 is `ea7ab9c5a142843b109ad49924f4709660347265099740312a666eca07972cd4`. Those bytes are implemented-but-publication-blocked historical technical evidence, not accepted, Verified, published, rejected visible motion, or the older removed pose-maker.
- Root cause: its only exported completion API privately selected important poses after callers already supplied final frames/document, so Phase 3 could not safely perform the required checked selection before baking without duplicating IK/selection. Separately, Phase 2 correctly rejected airborne support, but Phase 4 was required to create takeoff/airborne/contact while its nine-path ceiling excluded the safety module and its validators.
- Two-stage API decision: Phase 2 exposes exactly two callable safety operations. `selectStickBodySafetyImportantPoses` validates the captured base binding and returns a deep-frozen checked canonical important-pose selection before baking; its request contains no final frames or candidate document. A straight-neutral-knee stationary exception at selection is derived only from a bounded rest endpoint or geometrically identical selected important-pose interval with independently checked rest semantics, never a trusted stationary flag, and completion rechecks every baked frame. `finalizeStickBodySafetyCandidate` receives the original request, exact checked selection, final rounded/post-repair frames, and candidate document; it recomputes/revalidates binding, request and selection, important-frame correspondence, every final frame, and whole-animation semantic continuity. Selection digests are comparison evidence, never trusted tokens. All unchecked enumeration/IK/validation helpers remain private. `stickFigureMotionEngine.ts` owns exactly one final SPEC-0005 candidate door, and only its successful final result may reach executor Preview.
- Non-bypassable decision: no trusted/skip/checked boolean, planner identity, action label, fixture ID, materializer choice, guessed document, caller-selected legacy path, exported unchecked helper, or selection-only result may expose Preview or mutate document/history/storage/latch. Current SPEC-0004 materializers remain historical compatibility routes only. Pretend AI and future Terra use the same body safety regardless of 2D direction.
- Proof decision: the existing exact eight-path ceiling remains feasible. The correction proof must execute `checked selection → provisional test-local in-betweens from returned poses → final completion/revalidation`, without implementing the Phase 3 pose maker or new app motion. It preserves at least 10,000 fixed-seed valid/invalid property cases plus at least 10,000 mirrors and adds tampered-selection, stale-binding, unsafe-rounded-frame, unsafe-post-repair-frame, selection-to-Preview, exported-helper, legacy-materializer, trusted/skip/action/fixture bypass negatives. Safe left/right translations, role-swapped mirrors, and grounded rise/lower with fixed contact prove 2D placement/body safety only, not travel, jumps, locomotion, or natural-action quality. Numerical anatomy rules do not change.
- Phase 4 ceiling decision: Phase 4 has exactly twelve tracked paths, adding `stickFigureBodySafety.ts`, `validateStickBodySafety.ts`, and `validateStickBodySafetyIntegration.ts` to its prior nine. Those paths may change only for mechanics-qualified grounded/anticipation/takeoff/airborne/contact/compression/recovery behavior. Phase 2 continues to reject airborne. Phase 4 selection accepts it only when the complete ordered important-pose/mechanics sequence proves the transition without final frames; completion then revalidates the full baked sequence. Both passes require no planted anchor during flight, grounded contact before/after, no penetration, and unchanged bend/crossing/clearance/continuity/manual-ownership rules. There is no caller `allowAirborne` or other bypass flag, and the v4 fixture owns Phase 4 cases.
- Unusual-body boundary: the built-in humanoid has no hyperextension/reversed-bend override. A future separate custom/unusual-body specification must choose performer-anatomical versus viewer/facing left/right meaning and define deliberate ranges, display, and safety ownership. Current manual post-Apply freedom and ordinary independently editable frames remain unchanged.
- Lifecycle consequence: this task changes only the same twelve canonical documentation paths and stops before runtime/proof or Git mutation. After independent PM review, a separate docs publication must land from canonical `46b97556…`. Only a later explicit instruction may transfer exclusive correction ownership to the preserved `/7dfc/` worktree, verify its stopped/index-empty/exact-eight-path/historical-manifest state and the existing `http://127.0.0.1:57795/` process, then reconcile its detached basis to the docs publication without changing any of the eight dirty bytes. The correction stays inside those eight paths and writes a new manifest without overwriting `ea7ab9c5…`. It may reuse the exact old process or, if exited/invalid, verify/stop it before starting one replacement in the same worktree; old and replacement may never overlap and no second app copy is allowed. It returns a packet and stops. Any path overlap, hash drift, extra/staged path, or concurrent/mismatched process is a stop condition. Phase 3 remains unauthorized.

### D-0048 — Accept and technically verify corrected SPEC-0005 Phase 2 body safety

- Date: 2026-09-06
- Status: Accepted by Arthur and the Project Manager; technically Verified; publication and integration pending.
- Decision: accept the stopped executor's exact eight-path D-0047 correction on base `1861db92e8f599afa811b0ab6fdd46cc475f9f1c`. The two-stage safety route is now implemented: checked important-pose selection occurs before any provisional bake, final completion independently rechecks the final rounded/post-repair frames, and only one final motion-engine candidate door can reach Preview. The correction is planner-independent infrastructure and creates no visible motion or Phase 3 behavior.
- Evidence: correction manifest `output/spec-0005/phase-2/d0047-correction/proof-manifest.json` is 17,520 bytes at SHA-256 `e8a3fbbbb1a1cdf63e5d3648422def4736a4a1bf212ecc27335d2aa842db7874` and passed 168 independent checks; the PM revalidated it again on 2026-09-06 and confirmed the ordinary review root returned HTTP 200 before closeout. Runtime integration passed 20,305 assertions, 81 named negatives, and 10,000 generated cases plus 10,000 mirrors. The independent oracle imported none of the runtime safety/engine/executor modules and separately passed 10,000 cases plus 10,000 mirrors and 68 negatives. Normal and mirrored upper-bend witnesses correctly reject elbow bends above 150 degrees and knee bends above 125 degrees; torso faults, branch flips, intersections, and unsafe rounded/post-repair output fail closed. Inherited validators, TypeScript, scoped lint, full-lint non-regression, permanent tester, diff/scope/index, and cleanup gates passed. The permanent tester recorded 40 operations and 13 screenshots with zero external/provider traffic. The earlier manifest `ea7ab9c5a142843b109ad49924f4709660347265099740312a666eca07972cd4` remains immutable historical non-acceptance evidence.
- Human-review boundary: Arthur's review of the ordinary unchanged app at `http://127.0.0.1:57795/` was accepted only as an existing-app smoke check. It is not proof of body safety or new motion. The manifest-recorded review process was stopped and the port was verified closed after acceptance.
- Consequence: Phase 2 is accepted and technically Verified but remains unpublished and unintegrated until separate GIT-043 publication. Phase 3 and Phases 4–8 remain Unauthorized; Not started. No provider, Drawing, persistence, package/configuration, deployment, unusual-body override, or new motion is authorized by this decision.

### D-0049 — Record SPEC-0005 Phase 2 publication and authorize Phase 3 whole-body poses

- Date: 2026-09-06
- Status: Accepted by Arthur. Phase 2 is Verified, published, and integrated; Phase 3 alone is Approved, Authorized, and Not started pending publication of this authorization record.
- Publication record: GIT-043 published the exact accepted 21-path Phase 2 package in canonical-main commit `e52454354c39b962ac2710a8602a5306ffd62ad5`, parent `1861db92e8f599afa811b0ab6fdd46cc475f9f1c`, message `Implement SPEC-0005 Phase 2 body safety`. Canonical `main`, local `origin/main`, and live GitHub `main` matched cleanly at `0/0`. The required clean tester passed 40 operations, 13 screenshots, four messages, all 37 negatives, exactly one deterministic mocked Drawing POST, zero real API-route/non-loopback/provider requests, and cleanup; result SHA-256 is `c2887be734fe073191aa7d81fc9ffcf2cd6009da0bd188b67ddd9ad77aaebb63`. The accepted correction manifest remains `e8a3fbbbb1a1cdf63e5d3648422def4736a4a1bf212ecc27335d2aa842db7874`; historical manifest `ea7ab9c5a142843b109ad49924f4709660347265099740312a666eca07972cd4` remains historical non-acceptance evidence.
- Phase 3 decision: authorize only the existing SPEC-0005 Phase 3 contract, **Action-Independent Whole-Body Key-Pose Maker**. It is limited to four visible grounded non-locomotion micro-movements—reach-high-right, centered compression, left support shift, and neutral recovery—and must use the published Phase 2 checked important-pose selection before bake and final completion afterward. Its exact nine-path ceiling and ignored `output/spec-0005/phase-3/**` proof boundary remain unchanged.
- Boundary: Phase 3 is not Pretend-AI chat, Terra/provider work, walk/run, mechanics/weight/contact insertion, professional path/timing/gravity work, final core actions, Drawing/persistence work, or a broad action-language system. Walk, run, and no-crab verification remain Phase 6. Phases 4–8 remain Unauthorized; Not started.
- Lifecycle consequence: GIT-044 must separately publish this record before implementation. Only that future publication commit may become the base for one fresh dedicated Plan-mode Phase 3 Spec Executor worktree/app copy. This control-plane task starts no executor, app copy, server, implementation, external request, paid service, deployment, staging, commit, merge, push, or publication.

### D-0050 — Correct Phase 2 body-local safety and rewrite rejected Phase 3 natural poses

- Date: 2026-09-07
- Status: Accepted directly by Arthur for the specification/control-plane correction. **Phase 2 body-local natural-safety correction is Approved/Authorized/Not started pending GIT-045. Corrected Phase 3 is decision-complete but Unauthorized/Not started until corrected Phase 2 is accepted, published, and integrated.**
- Publication/rejection truth: GIT-044 published D-0049 at exact canonical-main SHA `269ac82335ee4576cb471bd9dffc8f7ce9bdec0f`. The resulting separate Phase 3 executor output was rejected after visible review. No rejected runtime, fixture, test, proof, app-copy, or review byte is accepted, propagated, or reusable. This decision does not read or mutate that worktree.
- Root cause: GIT-043 Phase 2 v1 checks finite segment lengths, broad elbow/knee magnitude corridors, screen-space branch sign, crossings, clearance, support/contact, and continuity. It does not understand performer facing, limb plane/depth projection, anatomical bend direction, comfort band, line of action, silhouette, or prompt-exact naturalness. A visibly wrong Reach High Right elbow near `57°` passed `8°..150°`; its alternate two-circle branch was also numerically valid, and v1's closest-screen continuity/sign-flip rules could retain the wrong-looking projection because screen sign is not anatomy. The rejected Phase 3 goal omitted facing, joint guide/plane, comfort band, root/torso/support staging, required action landmark, and forbidden movement; generated too few whole-body choices; ranked target/continuity/travel rather than naturalness; and turned about eight guide samples into easing-restart key poses. Neutral Recovery started raised, Reach High Right was not a wave, and green math/editability/playback checks did not prove human-looking poses.
- Formal Phase 2 correction: preserve GIT-043 and manifest `e8a3fbbbb1a1cdf63e5d3648422def4736a4a1bf212ecc27335d2aa842db7874` as published v1 history. Add explicit v2 selection/result/completion contracts with performer-facing body-local axes, allowed frontal/profile plane, semantic elbow/knee guide, exact safe flexion bands, controlled declared projected branch transitions, backward/ambiguous bend rejection, and final rounded-frame recomputation. V2 hard safety returns a qualified candidate graph rather than choosing the natural winner. The sole SPEC-0005 Preview door rejects v1 after correction; all normal app/manual behavior and SPEC-0004 compatibility routes remain unchanged. Unsafe, unclear, tampered, unsupported, or unprovable output produces no Preview and no state mutation.
- Phase 3 rewrite: every no-coordinate goal must declare facing, grounded support, root and torso/head staging, all four active/passive/support/recover limb roles, hand/foot target region, limb plane, joint guide, flexion band, required action landmark, forbidden extra movement, and sparse landmark kinds. It generates bounded whole-body alternatives, sends them through v2 hard safety, and deterministically selects among safe sequences lexicographically by prompt match, joint comfort, line of action, balance/support, silhouette/readability, continuity, minimum unrequested movement, then canonical bytes. Pass-through guides cannot become key endpoints or easing stops; contact/impact stay Phase 4, continuous paths/timing Phase 5, gait Phase 6, semantic wave/punch/jump/run-then-wave recipes Phase 7, and the shared planner door Phase 8.
- Quality/proof decision: `SPEC0005-HUMANOID-NATURALNESS-v1` is an independent human-written oracle/mutation set covering grounded pose probes or expected fail-closed cases for wave, walk, run, run-then-wave, punch, jump, left/right mirrors, near limits, and extra-action rejection. Hard safety/prompt/landmark/forbidden/editability/rejection gates pass `100%`; each supported visible case scores at least `85/100`, the mean at least `90/100`, and Arthur must accept every required visible sample. This proves only the named built-in 11-joint/10-segment humanoid set, not universal perfection. Future creatures require separate declared anatomy contracts.
- Fresh Phase 2 executor: after GIT-045, use one new exclusive Plan-mode worktree/`codex/` branch from its exact SHA and exactly the eight paths in SPEC-0005 plus ignored `output/spec-0005/phase-2-natural-safety-correction/**`. Preserve the historical v1 fixture/manifest, run the independent `10,000 + 10,000` v2 property/mirror floor, inherited validators, TypeScript/lint/diff/permanent-browser/zero-egress gates, independently validate the correction manifest, and stop with an empty index. Do not enter any rejected or other task worktree. Phase 3 and Git publication remain separate later authorizations.
- Scope boundary: exactly eight phases remain. No layers, backgrounds, colors, custom bodies, AI editing, functioning Reasoning selector, provider/API/paid request, outside service, Drawing/persistence, deployment, staging, commit, merge, push, or publication is authorized here.

### D-0051 — Correct SPEC-0005 to one bounded semantic movement sequence before Phase 2 v2 execution

- Date: 2026-09-07
- Status: Accepted directly by Arthur for this final specification/control-plane correction. GIT-045 is published at exact 13-document commit `a4589664b6857eb10a828189e177e0e7d47e3f69`. The corrected Phase 2 v2 contract is Approved but **Not implementation-authorized/Not started** until separate GIT-046 publication; corrected Phase 3 and Phases 4–8 remain Unauthorized/Not started.
- Root cause: D-0050 correctly established body-local safety, but its single whole-goal facing/support/limb state could not encode per-landmark turns, foot/hand/pelvis contact release or acquisition, a non-neutral bound base, simultaneous requested parts, sequential handoffs, or `run → jump+wave → land → continue run`. Treating that interim shape as permanent would force later phases to fork the contract or hide mechanics/composition in labels.
- Contract correction: `stick.movement-goal/v1` is one bounded ordered semantic sequence with an exact base-pose binding; one through eight action-independent requested parts; two through twelve complete per-landmark states; exactly adjacent transition records; per-landmark facing, support/contact set, four limb obligations, active-part IDs, required outcomes, and forbidden extras; and exact `hold_last`, `return_to_base`, or `continue_sequence` completion. Raw coordinates, angles, executable code, implicit parts, forged context, and single-state alternatives remain invalid.
- Permanent safety and proof: Phase 2 v2 is action-, planner-, style-, wording-, and sequence-position-independent across all facings, final frames, bases, roles, and future contact/composition contexts. Unsafe anatomy never becomes valid because injury, deformation, or unusual motion was requested; depicting those outcomes requires a future separate approved spec. Human-authored examples are primary; self-generated goldens are secondary. Every owner phase requires `100%` hard expected accepts/rejects with zero false accepts and zero false rejects, the named visible threshold of at least `85/100` per sample and `90/100` mean, and Arthur acceptance of every required visible sample.
- Eight-phase ownership remains exact: Phase 2 owns action-independent safety/classification through every context/facing/final frame; Phase 3 owns constant-contact grounded compound key poses from neutral or non-neutral bases; Phase 4 owns foot/hand/pelvis contact changes and takeoff/airborne/landing; Phase 5 owns permanent motion law with `natural_smooth` default and explicit mechanical styles only; Phase 6 owns real left/right walk/run, turns, direction changes, and continuation; Phase 7 owns full actions and compositions including squat-plus-wave, push-up-plus-wave, and run-plus-jump-plus-wave-plus-land-plus-continue; Phase 8 sends the identical bounded sequence through Pretend AI and a provider-free Terra-shaped adapter.
- Lifecycle: GIT-046 must separately publish this exact reviewed control-plane correction. Only that future publication SHA may activate one fresh Plan-mode Phase 2 Spec Executor using the unchanged exact eight-path allowlist and proof root. D-0050/GIT-045 remain valid published history; only their incomplete schema/start gate is superseded. Phase 3 requires later Phase 2 acceptance, sequential CPA closeout, publication/integration, and explicit owner authorization.
- Scope boundary: this task changes documentation only. It authorizes no runtime, fixture, technical test, proof artifact, review app/process, provider/API/paid request, deployment, stage, commit, merge, push, or publication. Exactly eight phases and every unrelated accepted system remain unchanged.

### D-0052 — Define front-view knee projection without weakening humanoid safety

- Date: 2026-09-07
- Status/authority: Accepted for the latest explicitly authorized narrow Spec Architect correction and its docs-only normal Git publication, GIT-047. GIT-046 is complete at `55b05e2daad649ac1e0027722a75762a9714d281`, parent `a4589664b6857eb10a828189e177e0e7d47e3f69`, message `Publish D-0051 bounded movement sequence`. Phase 2 reported a specification blocker; its runtime/proof/acceptance state is not verified by this task. Phase 3–8 remain unauthorized.
- Conflict/root cause: §3.2 mapped profile forward but allowed front only in the frontal plane while requiring every moving knee to guide anatomically forward. No front-knee guide existed. Inventing a stage sign would violate body-local/mirror invariance; blanket `facing_projection` would reject required safe front compression/support; `unsupported_owner_phase` would falsely defer Phase 2's own safety duty. Current canonical v1 checks bend magnitude/sign and has no v2 facing projection; intended and implemented behavior are explicitly different.
- Exact decision: SPEC-0005 §3.2.1 is authoritative. The existing front/frontal leg `forward` guide derives the stylized `front-outward-v1` direction from that frame's torso and anatomical role: left `+r`, right `-r`, with `r=(-up.y,up.x)`. Remove the guide component parallel to the hip–foot chord, normalize the remainder, and measure the knee's signed perpendicular clearance. Profile forward stays unchanged. This is a declared flat-rig convention, not hidden 3D anatomy and not an injury diagnosis.
- Closed outcomes: indistinguishable guide `length(p)<=1e-9` fails `facing_projection`; wrong side fails `backward_bend`; zero/tangent fails existing `branch_singularity`; positive clearance below `0.015H` fails new `joint_guide_clearance` with the existing distance epsilon. Only the already-permitted explicit near-extension band waives the clearance minimum and still requires a positive nonsingular guide. All non-exempt flexed knees, including stationary support/base knees, qualify; the exact straight-neutral exception and every bend, length, crossing, contact, balance, continuity, transition, and final-rounding rule remain.
- Proof boundary: independent front compression, asymmetric support, flexed non-neutral base/return, permitted near-extension, both IK branches/roles, tilted/translated examples, mirrors, thresholds, degenerate guides, wrong sides, forged context, and rounded/post-repair mutations are mandatory inside the unchanged Phase 2 eight-path proof scope. Guide-correct knee seeds and analytical invariance are design evidence only; full-body correctness, actual runtime rejection/acceptance, visible naturalness, and professional quality remain unproved here.
- Ownership/lifecycle: no second executor or worktree takeover. After publication, PM gives the blocked Phase 2 executor the exact GIT-047 spec SHA and an explicit continuation/base instruction. Preserving its GIT-046 implementation base requires separately binding the exact new spec commit/bytes in proof; a genuinely fresh executor starts from GIT-047 in Plan mode. This task neither moves nor edits that worktree. Phase 2 remains qualification/final safety; Phase 3 generation/ranking; Phase 4 mechanics; Phase 5 paths; Phase 6 gait/turns; Phase 7 recipes/composition; Phase 8 planners.
- Unchanged/prohibited: no runtime, fixture, technical test, proof file/artifact, app/browser process, package/config, AGENTS, custom creature, injury/deformation escape hatch, UI, Drawing/persistence, provider/API/paid request, deployment, or accepted/rejected implementation mutation. Exactly eight phase allowlists remain `6/8/10/12/9/10/7/8`. Only the reviewed canonical docs may be staged, committed, fast-forwarded to clean main, and pushed normally; stop if canonical main advances or scope differs.

### D-0053 — Accept SPEC-0005 Phase 2 v2 body-local safety; no Phase 3 activation

- Date: 2026-09-08
- Status: Accepted by Arthur/Project Manager for the bounded technical result; technically Verified; publication/integration pending GIT-048.
- Authority and meaning: Arthur accepted the ordinary existing-app smoke; PM accepted the technical evidence and explicitly transferred exclusive `/8673/` ownership after the executor stopped. This accepts hard body safety/fail-closed routing, not generated motion, naturalness scores, or universal anatomical correctness. D-0050–D-0052 contract substance and all historical rejection/publication evidence remain unchanged.
- Accepted boundary: exactly eight Phase 2 technical paths from `5d0299a00459d39d6c4bff5eeb345f72c5abde4e`, on `codex/spec0005-phase2-v2-fresh-safety`; no accepted technical/proof byte changed during CPA propagation. Manifest `a8c8db1bc81dbc6191975b7544027139f581a7493345e3b9e8bfeba6ecc29c65` is 30,333 bytes, with 15 receipts and 49 unique bindings. CPA independently passed all 744 material-field mutation checks before stopping the exact review process group 63034/listener 63035 and verifying port 54945 closed.
- Evidence: independent natural oracle 20,047 assertions and 10,000+10,000 cases/mirrors; retained v1 10,000+10,000 floor/68 negatives; integration 40,658 assertions, 81 named negatives, 80 band checks, 41 transaction attacks and stationary-hold Preview/Apply; inherited suites, TypeScript, scoped lint zero, full lint unchanged 5/72, and diff/index/scope checks. The clean exact-base permanent tester passed 40 operations/13 screenshots, zero real API/non-loopback/provider traffic and cleanup; it is baseline protection, while focused suites prove dirty v2 safety. Arthur's smoke is separate human evidence.
- Closeout/publication boundary: no dedicated Phase 2 v2 tracked-state finalizer exists; preserve successful live validation and all accepted bindings, revalidate durable evidence offline after cleanup, and use exact combined-path/memory/link/lifecycle/diff/index/hidden-flag/process checks. Do not rerun or claim success for the live eight-path validator against propagated records/a stopped server. Separate GIT-048 publication must precede later authorization. After successful clean publication, the owner's later task removes only obsolete completed Phase 2 app-copy worktrees/branch under policy, preserving proof/recovery material, canonical main and active PM worktree.
- Unchanged: Phase 1/v1 published history, accepted SPEC-0001–0004 behavior, manual tools/editability/history/storage, Drawing/UI, packages/configuration, providers/external services/deployment. Corrected Phase 3 and Phases 4–8 remain Unauthorized/Not started. No new executor or publication starts here.

### D-0054 — Require review-copy cleanup and authorize only corrected SPEC-0005 Phase 3

- Date: 2026-09-08
- Status: Accepted directly by Arthur; permanent process rule and Phase 3 approval/authorization recorded; implementation Not started; GIT-049 publication pending.
- Publication prerequisite: GIT-048 publication is complete at `05faa59195c6d5f2d8a11ebcc9ab77fe1b6fdf14`, parent `5d0299a00459d39d6c4bff5eeb345f72c5abde4e`, message `Implement SPEC-0005 Phase 2 v2 body-local safety`, exactly 22 paths (eight technical, thirteen Markdown records, one generated tree). Main/local origin/live GitHub matched cleanly at `0/0` on this task's fresh check. Preserved technical proof is under `output/spec-0005/git-048/05faa59195c6d5f2d8a11ebcc9ab77fe1b6fdf14/accepted-technical-proof/`; the clean tester result is in its sibling `clean-publication-browser/result.json`. Preserved technical manifest SHA-256 remains `a8c8db1bc81dbc6191975b7544027139f581a7493345e3b9e8bfeba6ecc29c65` (30,333 bytes). Clean publication tester result SHA-256 is `9c264119f61f8ff43defc51239ba6467ed8b786b1f5ec65d5000ac480802746e` (84,802 bytes): PASS, 40 operations, 13 screenshots, four messages, 37 negatives, four marked Stick GETs, one mocked Drawing POST, zero real API/non-loopback/provider requests, and cleanup. These are preserved publication results, not new browser runs in this docs-only task.
- Owner decision: after an accepted review result is propagated, separately published/integrated, and verified clean, stop its review server and remove the obsolete executor worktree/local branch while preserving required proof. After rejection and selection of a correction/replacement path, stop the executor, back up and verify any unique dirty bytes, then remove the rejected server/worktree/obsolete local branch before the next fresh executor. Keep only one active review app copy per milestone. Never remove canonical main or the active PM worktree; PM V3 and successors must enforce this rule. Exact process/path/branch identity, ownership, backup hashes/inventory, and safe branch recovery checks are mandatory. Rejected backups are not implementation sources. D-0010 role separation and separate Git-publication authority remain unchanged.
- Cleanup evidence: Cleanup is complete: exactly canonical `/Users/arthurcarlin/Projects/stick-animation-app` and active PM `/Users/arthurcarlin/.codex/worktrees/ef4e/stick-animation-app` remain registered. The owner/PM cleanup report records seven obsolete worktrees and six obsolete local branches removed after backup of unique rejected/superseded work at `/private/tmp/diamond-animator-old-copy-backups.xlkF6g`. That private recovery archive is not an app copy and grants no rejected-byte reuse. This task verifies the two-worktree inventory and backup presence; it performs no removal.
- Phase 3 decision: Arthur explicitly authorizes SPEC-0005 Phase 3 only, **Approved; Authorized; Not started**. Preserve the existing corrected §6 Phase 3 contract: action-independent whole-body key-pose creation for bounded grounded, in-place, constant-facing/constant-contact semantic sequences, using Phase 2 v2 as-is. The exact ten-path allowlist, independent oracle, candidate/scoring rules, 100% hard expected accepts/rejects, 85-per-case/90-mean quality threshold, permanent tester, eight two-viewport playback flows, manual ownership regressions, and Arthur's acceptance of every required case remain mandatory. No rejected code/proof is reusable.
- Fresh start: only after GIT-049 separately publishes this record may one new dedicated Plan-mode Spec Executor/worktree begin from that exact future publication SHA. Refresh base/source/proof/conflict checks; Astra is allowed for hard engine work (development model only). Supply one real loopback review URL and clear clickable case instructions in the PM message, loading cases sequentially in the same ordinary copy without a new product/review picker. No second review app copy may overlap.
- Boundary: no full wave/run/gait/action recipes, contact changes/airborne mechanics, new smooth motion law, Terra/provider AI, Drawing/UI/persistence change, or later phase. SPEC-0005 Phases 4–8 remain Unauthorized/Not started; SPEC-0004 remains paused before its Phase 3. This task performs documentation only: no executor, app copy/server, implementation, dependency, provider/API/paid/deployment operation, staging, commit, or push. Next step is PM review and separately authorized docs-only publication, not implementation from this unstaged state.

### D-0055 — Preserve completed work and supersede unfinished motion plans

- Date: 2026-09-09
- Authority/status: Accepted directly by Arthur, relayed by the Project Manager for this records-only transition. This decision changes work direction and closes old execution authority; it does not implement, approve a new spec, authorize publication, or permit network/provider work.
- Completed results: SPEC-0004 Phases 1, 2, and timing-only Phase 2.5 remain Verified, published, and integrated. Phase 2.6 remains rejected, unpublished, and superseded historical evidence. Under D-0055, unfinished Phases 3–8 are Superseded by future SPEC-0008, inactive, and must not be implemented. SPEC-0005 Phase 1 and Phase 2 v1/v2 remain accepted, Verified, published, and integrated. Existing contracts, proof, publication hashes, and earlier accepted history are preserved; no rollback or revalidation of accepted product code is performed.
- Actual Phase 3 history: GIT-049 is complete at `de54aed275c2f6da6e7c3f4a7f65091e8d5370c0`, parent `05faa59195c6d5f2d8a11ebcc9ab77fe1b6fdf14`, message `Record GIT-049 cleanup rule and authorize SPEC-0005 Phase 3`, exactly 14 Markdown paths. The publication was reported clean/synchronized; this records-only task independently verifies local main and local origin/main at that SHA with `0/0`, without refreshing the live remote because network access is outside this task. Arthur/PM report the post-GIT-049 rejected Phase 3 attempt in `/Users/arthurcarlin/.codex/worktrees/5200/stick-animation-app`, branch `codex/spec0005-phase3-whole-body-poses`, base/HEAD `de54aed275c2f6da6e7c3f4a7f65091e8d5370c0`, empty index and ten dirty technical paths. Reported ignored manifest: `output/spec-0005/phase-3/proof-manifest.json`, 55,941 bytes, SHA-256 `1420012f2213dcbf6c31483c700879f69e39881fd2bda8594fae1ec5d29947f3`. This is attributed rejected evidence, not independently revalidated proof or acceptance. The CPA does not open, modify, validate, recover, reuse, or remove that worktree or its proof in this task. The ten reported paths comprise the existing command executor/motion engine, a new v3 fixture, three Phase 3 proof scripts, two pose validators, and the movement-goal/pose-engine modules. The result ran and was visibly rejected; it must not be labeled Not started, completed, accepted, or reusable.
- Supersession: SPEC-0005 Phase 3 and unfinished Phases 4–8 are Superseded by future SPEC-0008, inactive, with no implementation authority. SPEC-0004 unfinished Phases 3–8 have the same status. This supersedes the unfinished phase dispatch and finish-SPEC-0005-before-resuming-SPEC-0004 dependency only, including D-0054's Phase 3 authorization. Old allowlists, technical contracts, commands, and gates remain historical reference, not permission to execute or automatic requirements for the replacement.
- Planning only: Planning order only: future SPEC-0006 unifies the Drawing and Stick workspace; future SPEC-0007 improves human/manual Stick tools in that unified workspace, including later colors and shape/head options with exact scope to be decided with Arthur; future SPEC-0008 replaces the unfinished AI Animator motion plan with temporary motion-video → body/rig tracking → ordinary editable animation frames. No SPEC-0006, SPEC-0007, or SPEC-0008 file is created, approved, active, or implementation-authorized by this record. Arthur designs SPEC-0006 with the PM next. The temporary-video direction selects no provider, model, library, price, credential, training pipeline, retention policy, or final implementation architecture. Those choices require the relevant future spec and explicit safety/cost/privacy decisions.
- Permanent protections: D-0054's review-copy cleanup rule and D-0010's role separation/separate publication gate remain unchanged. No active PM/rejected worktree is entered or changed, no review server is stopped, no recovery artifact is reused, and no worktree/branch/evidence is deleted in this turn.
- Closeout and next gate: Next step: PM review of this unstaged D-0055 transition packet, then a separate publication-only instruction for GIT-050. Publish only the reviewed record paths from the verified canonical base after fresh local/live Git checks under that later authorization. Arthur and the PM next design SPEC-0006; do not create or dispatch an executor, resume either superseded spec, or implement SPEC-0006–0008. All record changes remain unstaged. GIT-050 tracks only their later separately authorized publication, not implementation or cleanup.

### D-0056 — Record the Proposed unified Animation Workspace architecture

- Date: 2026-09-09
- Authority/status: Arthur supplied the exact owner outcome for specification architecture. [`SPEC-0006`](specs/0006-unified-animation-workspace.md) is **Proposed; Unauthorized** pending Arthur/Project Manager review. This records product direction and a reviewable design; it approves no phase and grants no implementation or publication authority.
- Product decision: New Project will open one neutral `Untitled Project`; Open Project will show one collection; one Animation Workspace will own one project, stage, timeline, playback/onion clock, Undo/Redo order, Save/Save As/Open path, and AI Animator placement. The visible direction preserves Stick Figure Tools, Properties, Library, Assets, the existing functional Drawing tools along the bottom, and AI Animator at bottom right.
- Data decision: the unified document uses typed Drawing and Stick layers. Drawing bitmap/text/sound/position-tween data remains Drawing data; Stick figure/rig/joint/limb/pose data remains structured and editable. Valid Drawing V1/V2 and Stick V1/V2 sources open through strict read-only migration. First Save creates a new canonical identity and never overwrites/deletes the legacy source. One canonical root/dispatcher/history/repository becomes the only normal writable truth.
- Phase decision: the proposal contains exactly seven sequential phases—contract/migration; shell/New/Open; stage/compositing; timeline/ownership/playback/onion; existing manual tools/panels/Creator/layout; history/persistence/recovery; and old-path retirement plus full acceptance. Every phase requires separate later authorization, executor, human review, CPA propagation, and publication before the next.
- Boundaries: SPEC-0006 adds no AI capability, provider/model/prompt, motion video, body tracking, paid call, external request, package/service, cloud/auth, deployment, new Stick color/head/shape tool, or legacy deletion. Future SPEC-0007 owns later manual Stick expansion only after SPEC-0006's exact completion gate. Future SPEC-0008 owns the later motion-video → tracking → editable-frame design. Neither future spec is created or implemented here.
- Evidence/limits: the proposal is based on fresh code tracing and ordinary loopback review of the current split New/Open/Drawing/Stick flows. It defines exact non-destructive migration obligations, a realistic mixed fixture using the accepted two `4563×3302` Drawing rasters, desktop/compact latency and memory bounds, accessibility gates, protected regressions, negative cases, and one-review-copy handling under D-0054.
- Consequence: P-0001 and P-0009 are resolved for intended architecture by this owner direction; current runtime remains split until separately authorized work is accepted and published. GIT-050 remains pending and must not be silently widened or published by this Spec Architect task. Next step is Arthur/PM review of the complete unstaged record set, not an executor.

### D-0057 — Approve SPEC-0006 and authorize Phase 1 only

- Date: 2026-09-09
- Owner authority: Arthur explicitly reviewed SPEC-0006, approved its exact seven-phase design, and authorized implementation of Phase 1 only in a brand-new review worktree, as relayed by the Project Manager. SPEC-0006 is Approved and active; Phase 1 is Authorized; Not started. Phases 2–7 remain Unauthorized; Not started. This supersedes D-0056's proposal-only lifecycle, not its architecture or D-0055's preservation/supersession decision.
- Accepted design: preserve the proposal's exact §§1–14 contract, seven phases, limits, migration obligations, proof/regression/human-review gates, and eleven-part completion gate. The proposal preimage SHA-256 is `87e19725316f046d1a6d27adf0685d11b670e58a6d1f933d47bf98bd81977898`; activation changes lifecycle/record text only, not that technical design.
- Authorized Phase 1: strict unified project contract and deterministic read-only Drawing V1/V2 and Stick V1/V2 migration adapters, independent fixtures/oracles/validators/proof, within the existing 18-path technical ceiling. Exact paths are frozen in the fresh Plan-mode plan before edits. Existing parsers may expose only the already-permitted essential narrow read adapter. No component, page, existing store-write, shell/stage/timeline/tool/history/repository/UI, package/configuration, or API change; no source project writes or deletion.
- Sole specification owner: SPEC-0006 alone owns the complete workspace unification through its seven phases. Do not create a spec-helping-spec chain or move its contract, migration, tester, shell, or integration responsibility into another prerequisite spec. Bounded corrections belong in SPEC-0006 under the existing amendment/review rules; material changes return to Arthur without silently expanding a phase.
- Start and publication boundary: Next step: complete this CPA review packet, then obtain the separate publication-only instruction for the exact reviewed combined D-0055/SPEC-0006/D-0057 record set. After those records are published and canonical main is clean/synchronized, one brand-new dedicated SPEC-0006 Phase 1 Spec Executor/review worktree starts in Plan mode from that exact publication SHA under Arthur's existing Phase 1 authorization. Freeze its exact paths within the 18-path ceiling and satisfy the existing Phase 1 entry/one-copy gates. Do not start from this dirty pre-publication checkout or reuse a rejected copy. No executor, review app, or publication is created by this CPA turn. Phase 1 does not need another owner authorization once those existing publication/entry gates are satisfied; its accepted implementation still requires Arthur review, stopped-executor exclusive CPA transfer, and separate publication. No later phase starts automatically.
- Preserved history/systems: completed SPEC-0001–0005 results, D-0055's rejected/unpublished/non-reusable Phase 3 history and superseded unfinished phases, D-0054 cleanup, D-0010 role separation, saved projects, existing proofs, AGENTS.md, other worktrees and recovery material remain unchanged. SPEC-0007/0008 remain absent and unauthorized.
- This task: canonical control-plane approval records only. No runtime/test/fixture/proof/dependency/config/environment/database edit, executor/review app, provider/API/search/Supabase/paid/network/deployment action, cleanup, staging, commit, merge, push, or publication. Local main/origin remain at `de54aed275c2f6da6e7c3f4a7f65091e8d5370c0`; live remote is not refreshed in this records-only turn.

### D-0058 — Accept SPEC-0006 Phase 1 contract and read-only migration result

- Date: 2026-09-09
- Status: Accepted by Arthur and the Project Manager; technically Verified in the dedicated worktree; pending separate GIT-051 publication/integration.
- Activation/base: GIT-050 is complete in exact 16-path commit `3b784cc6a68ff6f10fa390d96b81376b46e54b44`, parent `de54aed275c2f6da6e7c3f4a7f65091e8d5370c0`, message `Publish GIT-050 unified workspace activation records`. Canonical `main`, local `origin/main`, and accepted detached base/HEAD match that SHA; canonical local synchronization is `0/0`, and the accepted index is empty.
- Decision: accept exactly the 11 new Phase 1 technical paths under `src/lib/animation/`, `scripts/spec0006-unified/`, and `scripts/fixtures/spec0006-unified/v1/`. They add one strict `diamond-animation-project/v1`/document contract, deterministic source-bound canonical IDs/digests, content-addressed Drawing raster/audio assets, structured Stick payload/latch preservation, source-space display transforms, and read-only migration adapters for Drawing V1/V2 and Stick V1/V2. No existing runtime file, component, page, store-write path, package/configuration, API route, AI behavior, or ordinary visible flow changes.
- Evidence: accepted technical manifest `output/spec-0006/phase-1/proof-manifest.json` is exactly 9,257 bytes at SHA-256 `83614635c02f22d81205c441c46de3bff3a75f1c948661a1670a36eca75dbb29`. CPA independently replayed 160 manifest checks and the migration suite: 56 valid cases, 124 invalid/tamper cases, 1,000 repeated seeded mappings, 3,516 assertions, all four source kinds, stable typed failures, no implementation import in the oracle, and zero source writes. TypeScript, focused lint, Drawing V1/V2 contract/repository validators, Stick history/timeline validators, source hashes, exact scope, both diff checks, and empty-index checks pass. Full lint remains the pre-existing 5-error/72-warning baseline with zero Phase 1 finding.
- Human/regression evidence: Arthur accepted the unchanged ordinary-app review. The bound browser result records 10 operations and five screenshots covering Home, the existing New split, Drawing, Stick, Creator/Back, and the existing Open type tabs; it has four deterministic Stick availability requests, zero real API/external requests, zero console/page errors, and no review-only UI. Review URL `http://127.0.0.1:56261/` is historical evidence; port `56261` is closed and no persistent process owns the accepted worktree.
- Lifecycle consequence: Phase 1 is **accepted and technically Verified; pending GIT-051 publication/integration**. This decision and CPA propagation do not stage, commit, merge, push, publish, deploy, or authorize Phase 2. A later exact GIT-051 instruction may publish only the accepted 11 technical paths plus the reviewed control-plane/tree allowlist, after which clean synchronization and D-0054 accepted-copy cleanup are mandatory. Phase 2 and Phases 3–7 remain **Unauthorized; Not started**.
- Preserved systems: current split Home/New/Open/Drawing/Stick UI and runtime ownership; all accepted SPEC-0001–0005 behavior/proof; D-0055 rejected/superseded history; legacy source bytes/stores; Drawing/Stick write paths; AI/provider/search/Supabase/model configuration; packages/dependencies; database/auth/billing/export/deployment; other worktrees and recovery material remain unchanged.

### D-0059 — Accept corrected SPEC-0006 Phase 2 shell and unified New/Open result

- Date: 2026-09-09
- Status: Accepted by Arthur and the Project Manager; technically Verified in the dedicated worktree; pending GIT-052 publication/integration.
- Published prerequisite: GIT-051 is complete in exact 27-path commit `804ff39dc73c88d4799570cce2ef18987745a0be`, parent `3b784cc6a68ff6f10fa390d96b81376b46e54b44`, message `Implement SPEC-0006 Phase 1 unified contract`. Canonical `main`, local `origin/main`, and live remote `main` were freshly verified at that SHA and `0/0`; its Phase 1 contract and migration hashes remain frozen.
- Decision: accept exactly the corrected 15-path Phase 2 technical result in `/Users/arthurcarlin/.codex/worktrees/e4f3/stick-animation-app` from detached base/HEAD `804ff39dc73c88d4799570cce2ef18987745a0be`, with empty index. New Project creates **Untitled Project** directly through one `AnimationWorkspace`. Open Project keeps the established dark Projects screen, Back/centered heading/spacing/card treatment, removes the Drawing/Stick selector, combines all four supported source kinds without type badges or new wrapper chrome, disables invalid entries, and mounts exactly one inherited compatibility editor at the top of the viewport.
- Evidence: accepted manifest `output/spec-0006/phase-2/proof-manifest.json` is 16,649 bytes at SHA-256 `ebfeb699c0d9bcedd5b6b7c90d5cb4b71a3fe3ea89326a2820b86882e2c27cb7`. CPA replay passed its 15 source bindings, 19 receipts, 38 artifacts, 18 negative mutation cases, frozen Phase 1 hashes, and empty-index check. Browser proof passed 40 operations/13 screenshots at desktop and compact across New, collection, Drawing V1/V2, Stick V1/V2, invalid/missing/stale/storage failures, with zero source writes, real API/external requests, page errors, console errors, or review-only product surface. Arthur's acceptance occurred after the immutable executor manifest was written, so its historical `humanAcceptance: pending Arthur` field remains unchanged while this decision records the later acceptance. Production compile/generate, TypeScript, lint non-regression, inherited validators, scope, and diff checks pass.
- Lifecycle consequence: Phase 2 is **accepted and technically Verified; pending GIT-052 publication/integration**. A later already-authorized publication task may stage only the accepted 15 technical paths plus this reviewed control-plane/tree allowlist, commit, fast-forward unchanged clean canonical main, push, verify `0/0`, and complete D-0054 cleanup. Phase 3 and Phases 4–7 remain **Unauthorized; Not started**.
- Preserved boundary: compatibility editors remain separate and keep their existing Drawing Save/Save As and Stick Save behavior. No mixed compositor, unified timeline/tools/history, canonical Save/repository, legacy deletion/rewrite, new AI/provider/model/prompt, external/paid request, dependency/configuration, cloud/auth, export, or deployment is accepted or implemented.

### D-0060 — Reconcile GIT-052 publication and authorize SPEC-0006 Phase 3 stage/compositing

- Date: 2026-09-10
- Authority/status: Arthur explicitly authorized any required Git/GitHub bookkeeping before Phase 3 and explicitly authorized SPEC-0006 Phase 3 implementation. GIT-052 is now recorded as Verified, published, and integrated. Phase 3 is **Approved; Authorized; Not started**. Phases 4–7 remain **Unauthorized; Not started**.
- Verified publication fact: canonical `HEAD`, local `main`, local `origin/main`, GitHub's live `main` ref, and `git ls-remote origin refs/heads/main` were freshly verified equal at exact GIT-052 commit `d2096109900cc50a0a4dae2f603bd74b7b4a3427`, parent `804ff39dc73c88d4799570cce2ef18987745a0be`, message `Implement SPEC-0006 Phase 2 unified shell`, with `0/0` divergence, clean worktree, and empty index. That 29-path commit contains exactly the accepted 15 Phase 2 technical paths plus the 14 previously reviewed control-plane/tree paths. The accepted manifest remains 16,649 bytes at SHA-256 `ebfeb699c0d9bcedd5b6b7c90d5cb4b71a3fe3ea89326a2820b86882e2c27cb7`.
- D-0054 cleanup fact: the former accepted Phase 2 executor worktree `/Users/arthurcarlin/.codex/worktrees/e4f3/stick-animation-app` is no longer registered, PID `72206` is absent, and port `56362` has no listener. The accepted manifest identity and technical evidence remain preserved in the canonical D-0059/GIT-052 records; the ignored manifest file is not present in this canonical worktree, so this reconciliation did not rerun or alter it. No recovery or unrelated worktree was entered or changed.
- Authorized Phase 3: implement only §12 Phase 3's unified stage, renderer, and mixed Drawing/Stick compositing result. The existing plain outcome, execution path, 16-tracked-technical-path ceiling, non-goals, entry gate, human review, acceptance, negative tests, regressions, proof, and stop boundary remain unchanged. No Phase 4 timeline mutation UI, unified tools/history/persistence, legacy retirement, AI/provider/model/video/tracking, dependency/configuration, deployment, or external/paid operation is authorized.
- Exact base and records binding: per Arthur's explicit instruction, the Phase 3 technical/runtime diff starts from exact published GIT-052 runtime base `d2096109900cc50a0a4dae2f603bd74b7b4a3427`. This D-0060 reconciliation is a later records-only successor. The new dedicated Spec Executor must start in Plan mode, bind the exact published D-0060 authorization commit and current SPEC-0006 bytes in its plan/proof, confirm the runtime parent/base is `d2096109900cc50a0a4dae2f603bd74b7b4a3427`, freeze its exact path list within the 16-path ceiling, and satisfy the existing mixed-fixture/reference-render/coordinate-transform/parity entry gate before editing.
- Lifecycle: this task performs only canonical control-plane/tree reconciliation and its explicitly authorized normal Git publication. It does not create the Phase 3 executor/worktree/app copy, implement Phase 3, alter any runtime/fixture/technical-test/package/config/proof byte, deploy, or contact a provider/external/paid service. The future executor returns an Implementation Review Packet and stops; Arthur/PM acceptance, CPA propagation, and Phase 3 publication remain separate later steps.

### D-0061 — Accept SPEC-0006 Phase 3 stage/compositing and transfer to CPA

- Date: 2026-09-10
- Status: accepted by Arthur after visible app-copy review and accepted for sequential CPA takeover by the Project Manager. Phase 3 is Verified in `/Users/arthurcarlin/.codex/worktrees/c6b0/stick-animation-app`; unpublished/not integrated. GIT-054 publication remains a separate turn.
- Exact bases: runtime parent GIT-052 `d2096109900cc50a0a4dae2f603bd74b7b4a3427`; authorization/HEAD GIT-053 `916a4d913c6fdf8340b67bcc88dcea184d67cd32`; detached HEAD and empty index. Canonical main/local origin remained clean at `916a4d913c6fdf8340b67bcc88dcea184d67cd32`, 0/0, on CPA takeover. The live remote was not refreshed by this records-only turn.
- Ownership: executor turn `01a0894c-2c1f-7b20-a2af-4af65bf280e7` in task `01a0894c-2a06-7bd2-9f0f-257fdaee9915` completed before CPA turn `01a08975-038f-79a3-84ef-1ae395466cd5` began. Arthur's acceptance and the PM's explicit delegation authorize exclusive sequential ownership of the same worktree for propagation only. No concurrent executor remains active there.
- Accepted scope: exactly the 16 technical paths listed in SPEC-0006 §15. One owner resolver, typed Drawing/Stick adapters and ordered compositor render the seeded mixed scene at fixed 1920×1080 backing; presentation resize preserves authored coordinates. The existing Stick owner still handles gestures/history; canonical mixed persistence and other owners are not added.
- Fixture and AI boundary: Every visible element in `MIXED-REALISTIC-01` is manually seeded review content, not AI output. Its four independently owned Stick keyframes at indices 0/12/24/36 have identical neutral geometry and 44 holds; only the existing Drawing position tween moves. No AI model, prompt, generation, pose/motion engine, video, tracking/reconstruction, provider, or API behavior changed.
- Evidence: The accepted technical manifest is `output/spec-0006/phase-3/proof-manifest.json`, exactly 22,897 bytes at SHA-256 `207aea2a7d664e49d28dd6b8c4704e50da9fae8d5197d9164ccbac871db4fcc0`: 16 source bindings, 20 receipts, and 60 artifacts. CPA reran the unchanged validator before propagation: PASS, 3,725 assertions and 18 negative manifest cases. All accepted technical and proof bytes remain unchanged. The accepted proof passed 9,145 contract assertions; desktop 1440×900 DPR 1 and compact 390×844 DPR 2 rendering; eight mixed and four type-isolated pixel comparisons per profile with zero differences; five measured full 0…47→0 playback loops per profile after warmup; joint drag, held-owner editing, independent-owner preservation and exact Undo/Redo; resize/DPR/zero-size/failure recovery; and the stage performance/memory bounds. Compact stage is 352×198 with no page overflow. Production compile/generate, TypeScript, focused lint, all 16 inherited validators, and the inherited 40-operation/13-screenshot New/Open/Drawing/Stick browser suite passed. Full lint remains the exact inherited 5-error/72-warning baseline, with zero new findings. Real API/external requests, source-store writes, and render-triggered document/history writes were zero.
- Recovery provenance: the former uncommitted review worktree was removed. The replacement replayed the original patch history including its final neutral-fixture correction. Its source/document fixture hashes match the prior final result; regenerated browser/build receipts and manifest are fresh recovery evidence, not claimed byte-identical to erased historical output.
- Immutable evidence and closeout: CPA closeout is preserved under `output/spec-0006/phase-3/cpa/`: `takeover.json`, `ownership-transfer.json`, `pre-propagation-validation.log`, and `closeout-manifest.json`. The strict executor validator was run before records changed. After propagation, independently revalidate all accepted source/receipt/artifact hashes and sizes, the 157 original protected bindings, and all five frozen bindings. Resolve the original spec and original `docs/AI_SYSTEM.md` snapshot at the bound authorization commit; the other 156 protected files remain byte-identical, and no protected runtime/test byte changes. Do not rewrite the accepted manifest or claim its strict live 16-dirty-path/current-spec CLI passes after CPA records are added. Its historical pending-Arthur/executor-ownership/publication fields describe the stopped executor snapshot; D-0061 records subsequent acceptance and sequential CPA ownership.
- Limits: Physical-phone performance and native/GPU memory remain unproven; the compact result is a desktop Chrome browser profile. Existing compact title clipping remains. No unified timeline/playback/onion owner (Phase 4), tools/panels integration (Phase 5), shared history/canonical Save/Open/recovery (Phase 6), or legacy retirement (Phase 7) is implemented or authorized by this acceptance. The accepted full-loop evidence exercises composition through the existing Stick clock; it does not complete Phase 4. No natural-motion or AI-output quality claim is made.
- Review-copy preservation: loopback `http://127.0.0.1:56463/` → Open Project → MIXED-REALISTIC-01 remains running at this closeout, listener PID 331, cwd `output/spec-0006/phase-3/review-app` inside c6b0. Seeding/instrumentation is confined to that ignored source copy. Do not remove this accepted server/worktree before separate publication/integration/synchronization and authorized D-0054 cleanup.
- Lifecycle consequence: acceptance and CPA propagation do not stage, commit, publish, integrate, push, deploy, or authorize later phases. Phases 4–7 and future SPEC-0007/0008 remain unauthorized. All protected runtime/test bytes, ordinary navigation, Drawing editor, legacy stores, AI/motion/provider systems, dependencies/configuration, other worktrees, and recovery material remain unchanged. The original 157 protection bindings are revalidated: 156 current files are identical; only the AI overview lifecycle paragraph is a CPA record update, with its original snapshot bound at the authorization commit.

### D-0062 — Reject typed-layer Phase 4 and require visible same-cell neutral correction

- Date: 2026-09-11
- Authority: Arthur's explicit rejection and neutral-layer outcome, relayed by the PM; PM's subsequent direction requires visible corrected Phase 4 in the same app copy and accepts the two internal-gate structure. This is architecture correction authority, not CPA acceptance of rejected runtime or Git publication.
- Intended result: layers are neutral containers. The same layer and owner frame/cell contains Drawing raster strokes/shapes, editable text, bitmap symbol instances and structured Stick rigs/figures. No layer-kind field/hidden lanes/default kind or `· Drawing`/`· Stick` label remains in the V2 architecture. Manual sword/fireball placement on that same cell is required; automatic attachment/motion following is not added. End-of-spec one project/document/stage/timeline/playback/onion/library/tools/panels/history/Save/Save As/Open/recovery combines existing capabilities. “Share” means common project capabilities, not cloud/social Share.
- Supersession: D-0056/D-0057 typed-layer, separate-kind targeting, timeline-before-history and deferred-all-persistence assumptions are superseded only where they conflict with corrected SPEC-0006 §§1–14/16. Published Phases 1–3 and D-0061 accepted evidence remain historical. Fresh local HEAD/main/origin are GIT-054 `e11f6c453f13772ee9bd4b172a17bc68e1de65b9`, parent `916a4d913c6fdf8340b67bcc88dcea184d67cd32`, empty index; remote synchronization was not refreshed here. Old Phase 3 publication-pending snapshots are dated history.
- Rejection evidence: former Phase 4 was executed and visibly rejected, not Not started or accepted. Its 18 technical dirty paths and immutable `output/spec-0006/phase-4/proof-manifest.json` (104,287 bytes, SHA-256 `e9b7e5949198b5d24e21bddc901738ddafb4b63d978cc8f4002a0ccc0211d81b`) are preserved unchanged. Typed-layer passing tests do not prove neutral same-cell behavior. New work substantially replaces that architecture from canonical bytes; rejected bytes are recovery evidence only.
- Corrected contract: versioned neutral V2 document/native factory/read-only legacy+V1 upgrade, ordered typed items, item-scoped transforms/IDs/selection, whole-cell ownership/clipboard/onion, one transaction/history root and complete mixed storage. Do not weaken V1 parsers or rewrite historical manifests. Current bitmap library definitions and real symbol instances become project-owned/persisted; no invented legacy symbol recovery, vector engine, linked-symbol editor, reusable rig feature or enabled Creator Save.
- Later focused steering: Phase 4B must prove app/page.tsx → OpenProjectBrowser type/build compatibility and ordinary New/Open; page.tsx is conditionally authorized only if traced cutover needs an edit (4B maximum25, combined maximum37; otherwise24/36). Disk files already use onOpenProject and are unchanged; PM reports a stale unsaved editor buffer. Do not save/overwrite the buffer or invent a source fix.
- Safe correction order: retain seven numbered phases. Phase 4A adds exactly 12 model/migration/minimal repository/proof paths; Phase 4B adds/changes exactly 24 planned interactive/proof paths, for at most 37 distinct technical paths including the conditional page path. Minimal transactional Save/readback/Open moves forward because ordinary New already has working Save/Save As and a mixed root may never lose data through a legacy projection. 4A is a technical checkpoint only; sequential stopped-owner transfer to 4B requires PM hash/index/allowlist verification, no intermediate Arthur acceptance/CPA/publication. One final ordinary New/same-cell/timeline/history/Save review closes Phase 4 after 4B. Phase 5 completes current tools/library/panels; Phase 6 completes all-source adoption/recovery proof; Phase 7 retires duplicate ownership/full acceptance. No new numbered phase/helper spec.
- Same-copy exception: PM explicitly requires this correction in the existing rejected app copy. After architecture owner stop and separately authorized records publication, an authorized recovery step inventories/hashes/verifies unique rejected bytes and required ignored proof, restores only identified technical paths from canonical foundation, and stops/verifies the old exact server/port before one replacement. Retain the worktree/address for this correction instead of D-0054's fresh-copy removal step. All backup, identity, exclusive ownership, one-server, non-reuse and final post-publication cleanup protections remain. This exception does not authorize this docs task to recover/clean/start/stop anything or mutate other worktrees.
- Product proof: ordinary New starts empty and normal manual actions create Drawing + Stick in the same layer/frame. No special seeded project can substitute. Neutral layer/clipboard/timeline/onion/full-loop/history and safe native mixed Save/Save As/reload/Open must pass. MIXED-REALISTIC-01/artwork/colored blocks/demo rig/foreground-background words/instrumentation remain test-only, absent from ordinary build/routes/new projects/catalogs. Every claim separates technical checkpoint, visible behavior, historical evidence and acceptance.
- Boundaries: no new SPEC-0007 manual or SPEC-0008 AI/video features, AI/provider/model/prompt/motion-engine/API code changes, external/paid calls, cloud/social sharing, export, dependency or deployment. No runtime, fixture, technical-test, original proof, AGENTS, generated tree, package/configuration, AI/provider/prompt/motion-engine, source-store, other-worktree, server or Git-ref/index bytes/actions changed in this architecture task. Historical proof remains bound to its original source/spec snapshots; the strict old dirty-path/current-spec validators are not claimed to pass after these docs edits.
- Next: Arthur already authorizes proceeding after safe architecture. PM coordinates separately authorized records publication and verified same-copy recovery, then sequential 4A → stopped-owner checkpoint → 4B in the retained `/6e90/` worktree under SPEC-0006 §12/16. No invisible checkpoint is product acceptance. No extra Plan-click or pasted-prompt approval is required. This docs-only task starts no executor/server and performs no Git publication or cleanup. Later numbered phases still require prior acceptance, CPA propagation and separate publication/integration.

### D-0063 — Accept corrected SPEC-0006 Phase 4 neutral workspace

- Date: 2026-09-12
- Authority: Arthur's explicit visible acceptance after reviewing the final pointer-corrected ordinary app copy; the Project Manager's technical acceptance and exclusive transfer after the Spec Executor stopped.
- Decision: Accept the corrected Phase 4 implementation as technically Verified pending GIT-056 publication/integration. The accepted worktree is `/Users/arthurcarlin/.codex/worktrees/1550/stick-animation-app` at detached GIT-055 base `e956840001d18757af8f2de5361640ba70d44d68`; index empty. The immutable technical manifest is `output/spec-0006/phase-4-neutral/proof-manifest.json`, SHA-256 `9d2d18871981db2e499c689723730ada4690dd5cb94ab620a038e45557f298de`, status PASS, with exactly 26 technical paths.
- Accepted outcome: ordinary New uses one empty neutral root; Drawing and editable structured-rig content coexist in one layer/frame; no Drawing/Stick kind labels or seeded demo scene enter the product; neutral timeline/history/playback/onion and complete V2 Save/Save As/Open are shared. Add Limb and Select/Move Joint track the actual pointer through inverse SVG mapping, keep gesture ownership against the pan overlay, and commit/cancel atomically.
- Evidence: Phase 4A foundation PASS (364 assertions); Phase 4B workspace PASS (six flows, four tabs, eight tools); browser proof PASS (16 flows); 4A/4B manifest validators PASS (12 and 26 bindings); focused lint zero errors/seven inherited warnings; browser console zero errors/warnings; HTTP 200; diff/index checks PASS. Arthur accepted the visible review at port 56544.
- Boundaries: no AI/provider/prompt/motion/video/tracking/API change, no cloud/social Share, no export change, no Phase 5 library/tool expansion, no Phase 6 exhaustive adoption/recovery, and no Phase 7 retirement. The former typed-layer Phase 4 remains rejected and non-reusable.
- Next: GIT-056 must publish only the accepted 26 technical paths and reviewed control-plane/tree paths from exact GIT-055 base, fast-forward a still-clean canonical `main`, push normally, verify clean `0/0`, and then perform D-0054 accepted-copy cleanup. Phase 5 cannot begin first.

### D-0064 — Complete GIT-056 publication and accepted-copy cleanup

- Date: 2026-09-12
- Authority: Arthur's explicit instruction to propagate, commit, integrate into the real Diamond Animator, push to the exact approved GitHub remote `git@github.com:arthurzeyacarlin-dev/Stick-Annimation-App.git`, and remove the accepted app copy.
- Decision: Publish the exact D-0063 accepted package as GIT-056 commit `9c971fa4f7ea0e636ecc6957755552f678f344df`, parent GIT-055 `e956840001d18757af8f2de5361640ba70d44d68`. The commit contains exactly 40 paths: 26 accepted technical paths plus 14 reviewed control-plane/tree paths. Canonical `main` was fast-forwarded, pushed normally to `origin/main`, and verified clean and synchronized at `0/0`.
- Page verification: `app/page.tsx` was not changed. The saved canonical file already uses the current `OpenProjectBrowser` `onOpenProject` contract and focused ESLint passes with zero findings. The previously pictured four red diagnostics came from a stale unsaved editor buffer; overwriting the correct tracked file was explicitly avoided.
- Cleanup: the accepted `/Users/arthurcarlin/.codex/worktrees/1550/stick-animation-app` review worktree and fully merged local Phase 4 branch were removed after synchronization. No canonical-main or active Project Manager worktree was removed.
- Boundaries: no AI/provider/prompt/motion/video/tracking/API behavior, Phase 5 capability, dependency, deployment, cloud/social Share, or unrelated runtime path changed. Phase 5 remains not started and requires separate authorization.

### D-0065 — Accept SPEC-0006 Phase 5 current tools, catalogs, and durable gestures

- Date: 2026-09-13
- Authority: Arthur's explicit visible acceptance, “I call it acceptable,” after reviewing the corrected Phase 5 app copy; the Project Manager's technical acceptance and explicit sequential transfer after the Spec Executor stopped.
- Decision: Accept SPEC-0006 Phase 5 as technically Verified pending separate GIT-057 publication/integration. The accepted worktree is `/Users/arthurcarlin/.codex/worktrees/5a2c/stick-animation-app`, detached at base/HEAD `740eb70d2c713bf6bf8bd08123a0ed5eef3bc34d`, with an empty index and exactly 17 technical dirty paths. The immutable manifest is `output/spec-0006/phase-5/proof-manifest.json`, 32,852 bytes, SHA-256 `6a1c65c967f8f1d87a27e3834677bea84381f0c2577125bebfead6ca77308b6d`, status PASS, integrity VALID, technicalAcceptance PASS, 17 source bindings, and 77 evidence bindings.
- Ownership: the Spec Executor completed its final correction and PM Review Packet before the Control Plane Architect entered `/5a2c/`. Arthur and the PM accepted that result, then delegated exclusive propagation-only ownership to this CPA turn. The active collaboration tree contains only the CPA agent; the PM remains in its separate `/2d14/` worktree. No concurrent executor or architect owns `/5a2c/`.
- Accepted outcome: Phase 5 completes the current Drawing/Stick capability matrix and shared tabs in order: Stick Figure Tools, Properties, Library, Assets. App-owned naming/collision UI creates reusable project-owned Drawing, Stick, and Drawing+Stick definitions. Placed instances remain independently transformable and survive Save/Open; Stick-bearing definitions/selections retain structured joints and segments with constant radii/thickness rather than raster smearing.
- Interaction result: ordinary Select and Lasso move/resize Drawing-only, Stick-only, and Mixed content through the existing command/history/repository owner. Add Limb and Select/Move Joint use synchronous ref-backed gesture ownership, consume the final pointer sample, seal before release/lost-capture, and commit once through the existing history owner. Selection commits seal before callbacks. Symbol handles clear on empty canvas and Drawing/Stick action activation, including same-tool reactivation; the symbol's own Select/history/property flow remains usable.
- Preserved results: purple previous/green next onion masks cover raster, text, editable rig, and all three symbol categories with hold/dedupe/blank barriers; two-layer/three-frame Brush commits remain aligned; compact timeline cells receive ordinary pointer clicks without overlapping panels. Dialogs/categories, atomic conversion/removal, exact Undo/Redo, save/reopen, connected-rig selection versus joint-only movement, and protected tools/fonts/colors/layout remain accepted.
- Evidence: both 1440×900 DPR1 and 390×844 DPR2 passed 17 flows/profile. Each profile passed 50 Add Limb and 50 Move Joint gestures with timing variation and 1,400 ms release/autosave observation (200 total); all six Select/Lasso × Drawing/Stick/Mixed groups with eight transforms (96 total); symbol deselection/action success; and 48 onion category-direction checks. Proof binds release/settle, exact Undo/Redo, scrub, Save/reload/Open hashes/geometry, and unrelated rig/raster invariance. Phase 1 passed 3,516 assertions; Phase 4A 364; Phase 4B six flows; Phase 5 tools/catalog 231; TypeScript, focused lint with zero errors/ten inherited warnings, scoped production build, diff/scope/index and strict manifest validation passed. The PM independently observed 12/12 Add Limb and 12/12 Move Joint first attempts, autosave stability, symbol convert/place/deselect, and Brush success.
- Limits: the compact profile is Chromium emulation, not physical-phone proof; existing compact title clipping remains. Paid AI was not run. One generic missing-favicon 404 and one Canvas readback performance warning remain unrelated/out of scope. The evidence proves the tested timing/profile matrix, not every possible device or scheduling interleaving.
- Boundaries: accepted runtime, fixture, technical-test, technical-manifest and evidence bytes remain immutable during CPA propagation. No AI/provider/model/prompt/motion/video/tracking/API behavior, dependency/configuration, Creator Save, new rig feature, automatic symbol attachment/following, export, deployment, source-store write, other worktree, Git ref or index is changed. Human acceptance is recorded here; the historical executor manifest field is not rewritten.
- Publication handoff: canonical local `main` is clean at `740eb70d2c713bf6bf8bd08123a0ed5eef3bc34d`; local `origin/main` is `9c971fa4f7ea0e636ecc6957755552f678f344df`; `main...origin/main` is `1/0` because `740eb70…` is the already-reviewed Phase 4 publication-closeout records commit. A separately authorized CPA publication must create a `codex/` publication branch in `/5a2c/`, stage only the exact accepted 17 technical paths plus reviewed Phase 5 control-plane/tree paths, commit once, fast-forward a still-clean canonical `main`, push normally, and verify local main/origin/live GitHub clean at `0/0`. Any main/remote advancement or byte/path mismatch is a stop condition: no pull, merge, rebase, force-push, history rewrite, or scope expansion.
- Cleanup/next phase: preserve the review server `http://127.0.0.1:56555/` (listener PID 7680, cwd `/5a2c/`), worktree, and proof through publication/synchronization. Then D-0054 cleanup stops that exact server, verifies the port closed, preserves proof, and removes only the obsolete `/5a2c/` worktree and unused publication branch. Never remove canonical main or active PM `/2d14/`. Phase 6 remains not started and unauthorized until Phase 5 is durably published and cleaned up.

### D-0066 — Publish and durably integrate accepted SPEC-0006 Phase 5

- Date: 2026-09-13
- Authority: Arthur's exact confirmation to publish accepted Phase 5 to GitHub repository `arthurzeyacarlin-dev/Stick-Annimation-App`, branch `main`, using a normal non-force push; the separate stopped CPA propagation and publication tasks.
- Decision: GIT-057 is complete. The exact D-0065 accepted package is published as commit `a759ae8afbb67e8fb723983851ae36947fd97f17`, parent `740eb70d2c713bf6bf8bd08123a0ed5eef3bc34d`, message `Implement SPEC-0006 Phase 5 tools and library`, with exactly 31 paths: 17 accepted technical paths and 14 reviewed control-plane/tree paths. This supersedes only D-0065's pre-publication lifecycle status; its acceptance evidence and original snapshot remain immutable history.
- Git result: publication branch `codex/spec0006-phase5-publication`, canonical local `main`, local `origin/main`, and live GitHub `main` all resolve to the exact commit with `main...origin/main` at clean `0/0`. Canonical and publication worktrees and both indexes were clean. Publication was a direct fast-forward and normal non-force push; no pull, rebase, reset, cherry-pick, merge commit, amend, force, or history rewrite occurred.
- Integrity: the unchanged 32,852-byte technical manifest remains SHA-256 `6a1c65c967f8f1d87a27e3834677bea84381f0c2577125bebfead6ca77308b6d`; prior CPA closeout remains SHA-256 `91129b7c1b6294acd2189e9820f0c0665b4c8f6981d97e04c6ee36d3431da01c`. All 108 accepted technical/evidence/control-plane bindings revalidated after publication with zero mismatches. No accepted file byte changed during publication.
- Preservation/next action: review server PID 7680 remains at `127.0.0.1:56555`, exact `/5a2c/` cwd, HTTP 200. First publish this record-only closeout under GIT-058. After that synchronization, D-0054 may stop the exact server, verify port closure and proof preservation, and remove only obsolete `/5a2c/` plus its merged publication branch. Never remove canonical main or active PM `/2d14/`.
- Boundary: Phase 6 remains unauthorized and not started. GIT-057, GIT-058 record publication, and D-0054 cleanup grant no Phase 6, AI/provider/prompt/motion/video/tracking/API, dependency/configuration, deployment, external-service, or unrelated implementation authority.

### D-0067 — Close GIT-058 and complete SPEC-0006 Phase 5 review-copy cleanup

- Date: 2026-09-13
- Authority/status: The Project Manager completed the D-0054 cleanup after GIT-058 synchronization and delegated this final records-only Control Plane Architect closeout. SPEC-0006 Phases 1–5 are Verified, published, integrated, recorded, and cleaned up. This decision and its exact 11-path records package are terminal GIT-059. Its commit SHA cannot be predeclared from within its own content; the authoritative GIT-059 identity and publication status must be read directly from the canonical commit containing D-0067 and current Git refs. Once that commit is published and synchronized, Phase 5 is fully closed with no follow-on records-only closeout.
- GIT-058 verification: exact 13-path commit `41983b0c88e1994675a4901d5814b3d972ad46b4`, parent GIT-057 `a759ae8afbb67e8fb723983851ae36947fd97f17`, message `docs: record phase 5 publication closeout`. Fresh checks found canonical `HEAD`, local `main`, local `origin/main`, and live GitHub `main` equal at that SHA with `main...origin/main` `0/0`; the index was empty before this records-only update.
- Cleanup verification: former review PID 7680 is absent; TCP port 56555 has no listener; `/Users/arthurcarlin/.codex/worktrees/5a2c/stick-animation-app` is absent and unregistered; and local branch `codex/spec0006-phase5-publication` is absent. Canonical `/Users/arthurcarlin/Projects/stick-animation-app` and active PM `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app` remain present and registered.
- Proof preservation: the complete ignored backup remains at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-057-phase-5-a759ae8/phase-5`, with 98 files and 17,456 KiB (approximately 17 MB). Fresh SHA-256 checks match the technical manifest `6a1c65c967f8f1d87a27e3834677bea84381f0c2577125bebfead6ca77308b6d`, prior CPA closeout `91129b7c1b6294acd2189e9820f0c0665b4c8f6981d97e04c6ee36d3431da01c`, and post-publication CPA closeout `32357b0351bd3f0f23f0bd78146c5e15c7e026296013d494b7330409e4855991`. The PM recorded that a checksum-mode rsync dry-run produced no output before source deletion. That pre-deletion comparison cannot be rerun after `/5a2c/` removal; the present CPA independently verified the preserved destination inventory and manifest hashes.
- Boundary and next gate: this closeout changes canonical records only. It does not change runtime, fixture, technical-test, proof, `AGENTS.md`, dependencies/configuration, AI/provider/prompt/API, deployment, another worktree, or Git refs/history/index; this CPA correction does not stage, commit, push, publish, clean anything else, or start Phase 6. The already-authorized publication task may publish only the exact reviewed 11-path GIT-059 package from exact GIT-058 base, then must verify canonical/local/live synchronization directly from Git. That publication completes this record contract and must not create a GIT-060 self-record. The next possible product action is Phase 6, but it remains unauthorized and not started and requires Arthur's separate explicit authorization.

### D-0068 — Accept SPEC-0006 Phase 6 all-source adoption and recovery

- Date: 2026-09-14
- Authority: Arthur's explicit acceptance after reviewing the identical Phase 6 app-copy bytes after the same review copy was restarted on port 56666; the Project Manager's technical acceptance and explicit sequential transfer after the Spec Executor fully stopped.
- Decision: Accept SPEC-0006 Phase 6 as technically Verified pending GIT-060 publication/integration. The accepted worktree is `/Users/arthurcarlin/.codex/worktrees/b70c/stick-animation-app`, detached at exact GIT-059 base/HEAD `62feafc220c35eb1203dc4f19820e533a54002e1`, with an empty index and exactly 18 technical dirty paths. The immutable technical manifest is `output/spec-0006/phase-6/proof-manifest.json`, 43,794 bytes, SHA-256 `5883ea735bd741b2f14e45d2fa84669ea3bb78ad5ed23693011e0280e4070902`, status PASS, integrity VALID, technicalAcceptance PASS, with 18 source and 13 evidence bindings.
- Accepted outcome: Drawing V1/V2, Stick V1/V2, strict unified V1 and native unified V2 all open through the common neutral V2 editor. First legacy save allocates a fresh canonical identity, preserves exact source provenance and leaves source stores byte-identical. Save As creates a separate copy identity and rebinds project-bound Drawing AI memory and Stick latch. Collection dedupe is exact-provenance based rather than title based and tells the user when the original local source is retained.
- Storage/recovery result: typed raster arrays and image/audio data URLs become immutable content-addressed assets; versions are staged, fully hydrated and read back before compare-and-swap head publication. Native/adopted saves share one repository. Read falls back through retained valid versions if the active version is corrupt. Encode/hash/decode/quota/asset/version/abort/readback/CAS/ID/stale-instance/stale-revision/stale-generation/Undo/Open-race/crash-before-head failures retain the complete root/history/last-good head/source and truthful dirty state.
- Evidence: 1,117 deterministic assertions, 1,000 repeated cases, 48 history operations, six source kinds and 16 fault families passed. Browser evidence covers ten adoption flows plus native V2 Save/Save As/reopen in 19 operations, four screenshots, 120 selection-settle samples/profile and realistic two-raster persistence at 1440×900 DPR1 and 390×844 DPR2. Two 60,268,104-byte raster assets, sound, catalogs and auxiliary state survived. Large-project open/reopen stayed below 775 ms, Save below 1.53 s, settled heap below 320 MiB, and no long task was recorded. TypeScript, focused lint with zero errors/one inherited warning, full-lint changed-line non-regression, scoped build, inherited Phase 1/4A/4B/5/Drawing/Stick/AI validators, diff/scope/index and the original strict manifest validator passed. Source-store writes, real API requests, external requests and AI changes were zero.
- Liveness clarification: the immutable manifest recorded the original successful pre-expiry validation and listener PID `23344`. That process expired overnight. Arthur later reviewed identical restarted bytes and accepted them; the current port has no listener. CPA freshly revalidated the exact GIT base/index/18-path set plus all 18 source and 13 evidence size/hash bindings with zero mismatches. The volatile historical PID is not treated as a technical byte failure, and the accepted manifest is not regenerated or rewritten.
- Boundaries: Phase 6 changes no legacy Drawing/Stick source schema or write authority, AI model/prompt/provider/API/task behavior, motion/video/tracking, Creator Save, new manual capability, automatic attachment/following, export, dependency/configuration, cloud/auth/billing, deployment or source deletion. Compact is Chromium emulation, not physical-phone proof; native/GPU allocation is not proven. Existing compact title clipping remains. Phase 7 duplicate-owner retirement and whole-spec completion remain unauthorized and unclaimed.
- Publication/cleanup: a later GIT-060 publication turn may stage only the exact accepted 18 technical paths plus the reviewed Phase 6 control-plane/tree paths from unchanged GIT-059, commit once on a `codex/` branch, fast-forward a still-clean canonical `main`, push normally and verify clean local/remote `0/0`. This CPA propagation does not stage, commit, integrate, push, publish or clean up. Preserve `/b70c/` and proof until publication/synchronization, then perform D-0054 cleanup only under explicit sequential authority.

### D-0069 — Accept SPEC-0006 Phase 7 duplicate-owner retirement and full ordinary acceptance

- Date: 2026-09-14
- Authority: Arthur's explicit PASS after reviewing the ordinary app at `http://127.0.0.1:56770/`; the Project Manager's acceptance and explicit sequential Control Plane Architect transfer after the Spec Executor fully stopped.
- Entry and Git fact: GIT-060 is complete as exact 32-path commit `cbe16411a0f83d3b86136f41d0a66d1874d009aa`, parent GIT-059 `62feafc220c35eb1203dc4f19820e533a54002e1`, message `Implement SPEC-0006 Phase 6 adoption and recovery`. Before Phase 7 propagation, detached `HEAD`, local `main`, local `origin/main`, and live GitHub `main` all resolved there with `main...origin/main` `0/0`; the index was empty.
- Decision: Accept SPEC-0006 Phase 7 as technically Verified pending GIT-061 publication/integration. The accepted worktree is `/Users/arthurcarlin/.codex/worktrees/cd3e/stick-animation-app`, detached at exact GIT-060 base/HEAD, with an empty index and exactly 11 technical dirty paths. The immutable technical manifest is `output/spec-0006/phase-7/proof-manifest.json`, 50,224 bytes, SHA-256 `c6202f6deab4d3bcc75af3ad6699fc2dc70fc58723eccaa8337b1a2aef74bc51`, status PASS, integrity VALID, technicalAcceptance PASS, with 11 source and 16 evidence bindings.
- Accepted outcome: every ordinary `WorkspaceCandidate` contains a unified V2 editor; New and all supported Open paths reach one `AnimationWorkspace` → one `DrawingWorkspace`; manual Save, Save As and AI-triggered Save use the same canonical V2 repository. Drawing-shaped compatibility state and `DrawingCanvas` remain controlled algorithm/interaction adapters rather than alternate persisted roots. The ordinary coordinator's reachable legacy Drawing hydration/storage/cloud-sync write branch is removed. The standalone Stick coordinator remains a historical/test anchor only and is not imported or mounted by the ordinary product path. Legacy source parsers remain read-only; no source record is deleted or rewritten.
- Creator/accessibility outcome: Stick Creator opens inside the unified root as an accessible modal; background content is inert while open, focus is keyboard-visible and returns to the trigger, 200% zoom and reduced motion remain usable, and Creator Save remains disabled. No new Stick feature or Creator persistence was added.
- Evidence: all 24 recorded commands passed. Phase 7 authority passed 30 assertions across nine ordinary flows and 12 regressions. Browser proof passed desktop 1440×900 DPR1 and compact 390×844 DPR2, canonical V2 writes only, zero second coordinator mounts, zero Axe critical/serious findings, Creator keyboard round trip, 200% zoom, reduced motion, and zero external/real API requests, console errors or page errors. Phase 6 stress evidence remained PASS with 19 operations, two 60,268,104-byte rasters, desktop open/reopen/save at 735.8/758.3/1507.5 ms, settled desktop heap 312,397,923 bytes, compact settled heap 11,204,437 bytes, 240 selection samples and zero long tasks. TypeScript and production build passed; focused lint had zero errors/eight inherited warnings; full lint remained five errors/72 warnings with zero changed-line findings. Manifest validation passed 95 assertions and 12 mutation cases.
- Historical-proof limitation: preserved Phase 3, Phase 5 and Phase 6 manifests fully revalidated. Phase 1, Phase 2, Phase 4A checkpoint and final Phase 4 manifest files were not preserved, so the result is correctly `PASS_WITH_RECORDED_GAPS`; only their recorded identities and publication commits are claimed. Compact is desktop Chromium emulation, not physical-phone proof, and native/GPU memory remains unproven.
- Snapshot/liveness: the immutable executor manifest retains `humanAcceptance: pending Arthur` and the finalization retains `controlPlaneUpdated: false`; those fields accurately describe the stopped executor snapshot and are not rewritten. This decision records later acceptance/propagation. Listener PID `34120` remains at port 56770 with the accepted worktree cwd and must stay preserved through publication.
- Boundaries: no accepted technical, fixture, technical-test, manifest or evidence byte changes during CPA propagation. Phase 7 changes no AI model/prompt/provider/API/task behavior, motion/video/tracking, new manual capability, automatic attachment/following, export, dependency/configuration, cloud/auth/billing, deployment, legacy source deletion, other worktree or Git index/ref/history. No physical-device or native/GPU-memory claim is made.
- Publication/cleanup: a later GIT-061 publication turn may stage only the exact accepted 11 technical paths plus the reviewed 14 Phase 7 control-plane/tree paths from unchanged GIT-060, commit once on a `codex/` branch, fast-forward a still-clean canonical `main`, push normally and verify clean local/remote `0/0`. This CPA propagation does not stage, commit, integrate, push, publish, stop the server or clean up. Preserve `/cd3e/`, PID 34120 and proof until synchronization, then perform D-0054 cleanup only under explicit sequential authority. GIT-061 grants no future SPEC-0007/0008 authority.

### D-0070 — Close SPEC-0006 after GIT-061 publication and D-0054 cleanup

- Date: 2026-09-14
- Authority: Arthur's explicit Phase 7 PASS and later direct instruction to record the control plane, commit, integrate, push GitHub, and stop when it is time to create SPEC-0007.
- Publication: GIT-061 is exact commit `185f58752c4f8dd4f5bfea0befe48998e5cd0f04`, parent GIT-060 `cbe16411a0f83d3b86136f41d0a66d1874d009aa`, message `Implement SPEC-0006 Phase 7 authority retirement`, containing exactly the reviewed 11 technical/proof and 14 control-plane/tree paths. It was created on `codex/spec0006-phase7-publication`, fast-forwarded into clean canonical `main`, pushed normally, and freshly verified as local `main`, `origin/main`, and live GitHub `main` with `0/0` divergence.
- Proof preservation: the required ignored evidence is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-061-phase-7-185f587`: 15 files, 668 KiB. Phase 7 manifest SHA-256 remains `c6202f6deab4d3bcc75af3ad6699fc2dc70fc58723eccaa8337b1a2aef74bc51`; the separately bound Phase 6 browser result is preserved at SHA-256 `477cd3f4f8fba91981a3cc33bf92af553fbb9fde94a8b537430b0709aec025f7`.
- Cleanup: review PID 34120 is stopped and TCP port 56770 is closed. After this closeout synchronizes, the obsolete `/cd3e/` worktree and merged local publication branch are removed; canonical `main` and the active PM worktree remain. No remote ref or recovery evidence is deleted.
- Consequence: all seven SPEC-0006 phases are accepted, Verified, published, integrated, recorded, and cleaned up. The next possible product action is a new SPEC-0007 planning task with Arthur. This decision does not approve a SPEC-0007 file, implementation phase, runtime change, provider/API request, deployment, or paid/external operation.

### D-0071 — Propose SPEC-0007 manual editor completion before future AI expansion

- Date: 2026-09-14
- Authority: Arthur's explicit instruction to act as SPEC-0007 Spec Architect and his supplied seven-phase scope, terminology, invariant, boundaries, lifecycle and final gate.
- Decision: Create [`SPEC-0007 — Manual Editor Completion and AI-Ready Tools`](specs/0007-manual-editor-completion-and-ai-ready-tools.md) as **Proposed; review-ready; no implementation phase authorized**. The exact order is Drawing Tool Stabilization; Rig Terminology/Selection; Rig Structure Editing; Rig Appearance/Joint-Attached Shapes; Rig Builder/Rig Symbols; Assets Stabilization; Final Manual Editor Bug Burn/AI-Readiness Proof.
- Shared-capability invariant: every project/document mutation eligible for a future AI caller must first have an ordinary manual control and use the same UI-independent validated command capability, one canonical V2 coordinator/history/repository, deterministic explicit inputs, atomic failure, inspection, Undo/Redo/refinement and persistence as applicable. SPEC-0007 exposes manual callers only; future-AI eligibility is registry metadata, not an AI execution route.
- Vocabulary: the product remains one Animation Workspace; user-facing Stick Figure Tools/Add Limb/Creator become Rig Tools/Add Segment/Rig Builder. Internal `stick-figure`, `stick-rig/v1`, `StickFigure*` and `limbs` compatibility names remain unless a narrow versioned extension is required. Library is project-created Drawing/Rig/Mixed Symbols; Assets is external supported still media in a separate tab. Creator Hub and animated symbol timelines remain future.
- Limits and behavior: the proposed spec fixes rig selection/gesture/cascade semantics, additive versioned appearance/attachment data, linked-definition update/Make Unique behavior, supported PNG/JPEG/WebP and exact import bounds, phase performance gates, desktop/compact proof, and a machine/human command-capability matrix. Bounded engineering mechanics may be measured at phase entry but cannot weaken the owner outcome silently.
- Lifecycle: every phase uses a fresh dedicated Plan-mode Spec Executor worktree from the exact integrated canonical-main SHA, then stopped executor → Arthur/PM decision → exclusive CPA propagation → separate explicit publication → verified synchronization/D-0054 cleanup. No phase overlap, rejected-byte reuse, or compilation-only readiness.
- Boundaries: this planning decision changes no runtime, fixture, technical test, proof artifact, dependency/configuration, AI/provider/model/prompt/API/task behavior, motion/video/tracking/reconstruction, legacy writer, deployment, other worktree, Git index/history/ref, or external/paid service. It does not approve this proposal, authorize Phase 1, or create SPEC-0008.
- Resolution: P-0006 is resolved for the SPEC-0007 manual editor scope. P-0008 remains pending for the future AI-first sequence and receives no implementation authority here.

### D-0072 — Approve SPEC-0007, authorize Phase 1, and publish the activation

- Date: 2026-09-14
- Authority: Arthur accepted the reviewed SPEC-0007 planning package, explicitly authorized publication to canonical `main` and GitHub, and separately authorized SPEC-0007 Phase 1 implementation after publication.
- Accepted design: [`SPEC-0007 — Manual Editor Completion and AI-Ready Tools`](specs/0007-manual-editor-completion-and-ai-ready-tools.md) is **Approved and active**. The accepted Proposed preimage is exactly 50,372 bytes at SHA-256 `3ffc99184b692cbb8e84b383735fa14c6d740d31c090c601cf6a943e51721c56` from base `42640983a4d99aab6cdfacfa869bd9fcc956a01b`. Activation changes lifecycle/status records only; it does not change the accepted seven-phase outcome, manual/shared-command invariant, technical limits, semantics, proof gates, or protected systems.
- Phase authorization: Phase 1 — Drawing Tool Stabilization is **Authorized; Not started**, entry-gated on this activation package being published and cleanly synchronized. Its executor authority is exactly SPEC-0007 §7 and the shared §6 lifecycle/proof rules. Phases 2–7 remain **Unauthorized; Not started** and cannot begin automatically.
- Phase 1 start: after publication, one fresh dedicated Spec Executor worktree starts in Plan mode from the exact activation publication SHA, refreshes the real execution path and measurements, freezes an exact path list inside §7's allowed families, performs no control-plane or Git mutation, returns its Implementation Review Packet, and stops. Recommended executor is `gpt-6-astra` with `ultra` reasoning.
- Publication authority: this Control Plane Architect may stage only the exact reviewed 14-path planning/activation allowlist, commit once on a `codex/` publication branch, fast-forward a still-clean canonical `main`, push normally to `origin/main`, and verify local `main`, local `origin/main`, and live GitHub `main` at clean `0/0`. Any advancement, path/byte mismatch, dirty canonical worktree, or non-fast-forward is a stop condition; no pull, merge, rebase, amend, force-push, history rewrite, or scope expansion is allowed.
- Boundaries: approval, activation, and publication add no runtime, fixture, technical test, proof artifact, dependency/configuration, AI/provider/model/prompt/API/task, motion/video/tracking/reconstruction, legacy writer, deployment, paid/external-service, server, review-copy, or Phase 1 implementation change. SPEC-0008 remains future and unauthorized.

### D-0073 — Correct SPEC-0007 Phase 1's inherited-performance entry deadlock without lowering a gate

- Date: 2026-09-14
- Authority: Arthur authorized one narrow Spec Architect correction after the first Phase 1 executor obeyed the original clean-base stop clause. Phase 1 remains authorized; this decision changes bounded engineering mechanics only and preserves the accepted user outcome.
- Evidence: the stopped clean `/4bb1/` executor used exact base/HEAD `5e65a3142937734896a3a6cbc3c4f6ff513280ae`, changed zero tracked paths, kept the index empty, stopped PID 44319 and closed port 56871. Its blocked entry manifest is SHA-256 `fb019f3f019e5d754dfde0c6189010e935176216d2bbc051a04d74b436e56a80`, with 13 source and 42 artifact bindings and 22 negative mutation cases. Five warmed desktop runs failed both existing ceilings (Open 809–881 ms; Save 1,538–1,587 ms); compact failed four Opens and five Saves (Open 739–982 ms; Save 1,564–1,781 ms). The first inherited stress run sampled 556,539,747–557,612,900 bytes transiently against 536,870,912 bytes, while the successful inherited replay settled at 315,897,698 bytes desktop and 301,739,988 bytes compact, below 335,544,320 bytes. Build/TypeScript and the repeated inherited persistence result passed. The separate approximately 618.6 MB post-batch open-workspace sample used a different lifecycle and is diagnostic, not the inherited settled-heap result.
- Correction: a clean-base miss remains mandatory before-evidence but no longer forces Phase 1 to stop. One fresh Phase 1 executor may first diagnose and narrowly optimize inherited Drawing snapshot serialization plus the exact existing V2 repository/storage/bootstrap helpers reached by the same representative Save/Open flow. Before/after proof must use identical fixture bytes/digests and methodology and must prove byte/digest equivalence, complete content retention, rollback/failure integrity, immutable-version/last-good/CAS/source safety, and no schema/limit/failure-semantic change. Performance-only success cannot bypass the full Drawing correctness and protected regression matrix.
- Unchanged gates: Open ≤775 ms, Save ≤1,530 ms, settled JS heap <320 MiB, inherited transient/stress heap ≤512 MiB, 256 MiB single-raster allocation, pointer latency/long-task gates, all Drawing visual/history/Save/Open acceptance, and every protected SPEC-0006 gate remain exact exit requirements. No threshold may be lowered or reinterpreted.
- Restart gate: after these correction records are reviewed, separately published and synchronized, preserve the `/4bb1/` entry evidence at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/SPEC-0007-phase-1-entry-5e65a31/`, verify its complete inventory and manifest hash, and complete `/4bb1/` D-0054 cleanup; then start one fresh dedicated Plan-mode Phase 1 executor from the exact correction-publication SHA. It may not resume `/4bb1/`, reuse its worktree as implementation, edit control-plane/Git state, or exceed SPEC-0007 §7's corrected families. Phases 2–7 remain unauthorized.
- Boundaries: this records-only decision changes no runtime, fixture, technical test, proof artifact, visible semantics, supported format, V2 schema/version/limit, persistence data, dependency/configuration, AI/provider/model/prompt/API/task, motion/video/tracking, legacy writer, deployment, server, worktree, Git index/history/ref, or external/paid service.

### D-0074 — Replace SPEC-0007's structured-rig direction with a drawing-only editor and reject the first Phase 1 result

- Date: 2026-09-15
- Authority: Arthur's explicit corrected product direction and Spec Architect assignment from exact clean canonical-main SHA `be41db9e06126ae4e15c162962c6b856cb97660e`; the index was empty before this records-only work.
- Supersession: this decision supersedes D-0071, D-0072, and D-0073 only where they prescribe or describe a seven-phase structured-rig/Creator/Rig Symbol future, the earlier Phase 1 outcome/proof, or restart from the earlier entry correction. Their historical proposal, activation, publication, clean-entry evidence, lifecycle, performance ceilings, shared-command principle, and no-AI boundaries remain factual history unless this decision says otherwise.
- Corrected outcome: the exact rewritten SPEC-0007 preimage is 51,063 bytes at SHA-256 `d4acdbedc0811fff8c0e80f86ed4db1d23fa411cf8ff989343d47e4f58525391`. It becomes a drawing-only five-phase program: (1) Drawing Engine and No-Loss Stabilization; (2) Draw Rig; (3) Safe Legacy Rig Retirement and Migration; (4) Assets Stabilization; and (5) Final Manual Editor Bug Burn/Future-AI Command Registry. Only corrected Phase 1 is Authorized/Not started. Phases 2–5 are Unauthorized/Not started and require separate sequential authorization after the prior phase is accepted, propagated, published, synchronized, and cleaned up.
- Drawing-only law: by the end of Phase 3, no active structured rig, joint/limb/topology authoring, Stick Figure Tools, Creator, or rig-backed symbol lifecycle remains. **Draw Rig** is an explicit Properties toggle that later creates ordinary raster paint with deterministic straight-ish segments/corners and polygonal circles; it creates no joint, bone, ID, graph, attachment, or hidden rig object. Smoothing zero never enables it silently.
- Legacy safety: supported historical structured-rig items and rig-backed/mixed symbols are validated and deterministically rasterized into ordinary drawing items or drawing-only symbols through a versioned, idempotent, fail-closed migration. Source stores and pre-migration immutable versions remain read-only/recoverable; partial, stale, failed, corrupt, quota-exceeding, or visually non-equivalent migration never mounts or saves. Structured-rig code may remain only as a read-only compatibility reader/renderer behind that boundary.
- No-loss and destructive-command law: no non-destructive action may remove, blank, stale-overwrite, or make unreachable authored pixels/items outside its validated mutation target. Painting, Fill, and transforms may change only their explicit target region. Intentional removal is limited to the closed runtime-used destructive-command registry: current Eraser, Knife removal, Delete selection, Remove Frame, confirmed Delete Layer, confirmed Clear Canvas, Delete symbol instance, unreferenced Delete symbol definition, Remove Attached Sound, and a future explicitly requested AI delete; Phase 4 may add deletion of an unreferenced asset/catalog entry. Every entry binds target, reference/minimum guard, confirmation policy, history, and persistence. No current asset-catalog delete surface is claimed.
- Drawing contract: ordinary Brush/Pencil/Sketch/Pixelate/Glow gain deterministic preview/final identity, true smoothing 0–100 without straight-turn artifacts, stable Sketch preview, bounded memory/latency, exact Undo/Redo/Save/Open, and per-pixel maximum same-paint coverage within and across repeated/offset gestures. At 10% opacity, same-paint crossings/joins/textures remain at 8-bit coverage 26 rather than accumulating. Glow is accepted by measured luminance/contrast plus Arthur's visible review, not painted-pixel count/radius.
- Rejected result: the dirty Phase 1 result in `/Users/arthurcarlin/.codex/worktrees/3cd3/stick-animation-app` is Rejected, unpublished, unaccepted, and non-reusable. It is detached at base/HEAD `be41db9e06126ae4e15c162962c6b856cb97660e`, has an empty index and exactly ten dirty tracked paths. Its 39,787-byte self-reported PASS manifest has SHA-256 `26acbf3e708b85e2269f546c2094e90c4d234d7a59b3568d07b0452623908ea3`, six command receipts, ten source bindings, `humanAcceptance: pending Arthur`, and zero external calls. Arthur's visible review found straight-turn smoothing, opacity accumulation/dark dots, unstable live/Sketch preview, weak Glow, and disappearing-content risk; those failures defeat technical self-reporting.
- Restart/cleanup: this package changes no `/3cd3/` byte and does not stop its exact review server or remove its worktree. After this control plane is separately reviewed, published, and synchronized, the PM coordinates D-0054 inventory/hash/verified recovery backup, exact server/port shutdown, worktree removal, and any now-unused local-branch removal. Rejected bytes are evidence only and may not be copied, resumed, cherry-picked, or corrected in place. One fresh Plan-mode corrected Phase 1 Spec Executor may then start from the exact new canonical-main SHA.
- Boundaries: this decision/spec task changes only canonical control-plane/tree records. It implements no runtime/fixture/test/proof change, performs no cleanup, stages/commits/pushes nothing, and changes no dependency/configuration, AI/provider/model/prompt/API/task, motion/video/tracking, export, deployment, external service, source-store byte, other worktree, or Git ref/history. SPEC-0008 remains future and unauthorized.

### D-0075 — Accept and technically verify SPEC-0007 Phase 1 drawing stabilization

- Date: 2026-09-16
- Authority: Arthur's explicit ordinary-app acceptance after reviewing the final correction at `http://127.0.0.1:56875/`.
- Status: Accepted and technically Verified; control-plane propagation complete in this worktree; publication/integration and D-0054 cleanup pending. Phase 2 remains Unauthorized; not started.
- Accepted identity: the stopped executor result is based on exact canonical-main SHA `1c5aee42fa87967ad058c00cc3d62b51589b77fa`, with detached HEAD unchanged, empty index, and exactly 17 accepted runtime/fixture/technical-test paths. The immutable technical manifest is `output/spec-0007/phase-1/proof-manifest.json`, SHA-256 `511c778e83741b7dde420863769f68132146934f072c6203289bcbf0ea05d689`, status PASS/integrity VALID. Independent validation is SHA-256 `61514dcb59275292b8f2d9e3b345552b3c8de5128290dde6ff9627b30dba7186`, PASS, and rejects 99 deliberate mutations. Aggregate source digest is `95bb92d5ea63c84f35e8fedcbe58050c3349858b82ed1accd15e7c7305949a89`; accepted runtime digest remains `232ad74e1d374e96fa0fc3830d45ba08550ee190ba1765c0bbfcf9ad235b3728`; production build ID is `3Zc3DGwyx72yx09DfwUk_`.
- Accepted behavior: Brush, Pencil, Sketch, Pixelate and Glow use the stabilized no-loss raster path; same-paint coverage is bounded and persistent; smoothing/texture/Glow behavior, preview/final identity, history and Save/Open meet the Phase 1 contract. The final correction gives Sketch paint durable contributor ownership so Knife moves both selected halves including detached texture dabs without ghosts, preserves nearby independent paint, remaps ownership on copy, and retains a connected-component fallback for legacy unowned raster. Onion preview reduces source paint to occupancy and applies canonical previous-purple/future-green display tint with future-on-top overlap without mutating authored pixels.
- Evidence: 126,771 independent oracle assertions passed (19,844 raster, 423 coverage-persistence, 106,504 no-loss). Eighteen production-browser groups passed 508 cases and 6,746 assertions across desktop and compact profiles. Twenty-six fresh screenshots were inspected and bound. TypeScript, focused/changed-line lint, production build, diff, scope and empty-index checks passed. Full lint retains the repository baseline of five errors/131 warnings with zero changed/new Phase 1 findings; it is not claimed clean. Zero unresolved current-source technical failure remains.
- Human acceptance: Arthur reviewed drawing variants, transparency, Knife/Lasso/Select, timeline/onion, Undo/Redo and related ordinary flows and called the result acceptable. This decision records that later acceptance without rewriting the executor manifest's historical `humanAcceptance: pending Arthur` or `controlPlaneUpdated: false` snapshot fields.
- Boundaries: the accepted implementation does not add Draw Rig, retire/migrate legacy structured rigs, stabilize Assets, implement the final command registry, add AI/provider/model/prompt/API/video/tracking behavior, change dependencies/configuration/deployment, or authorize Phase 2. Compact remains desktop Chromium emulation; physical-device/native-GPU proof and extreme projects beyond the executed fixtures remain unproven. Legacy raster without contributor metadata cannot reconstruct detached historical gesture membership.
- Publication/cleanup: preserve `/Users/arthurcarlin/.codex/worktrees/f5c0/stick-animation-app`, listener PID 45075 and port 56875, and all proof until publication/integration and clean synchronization succeed. A later explicit publication task may stage only the exact accepted 17 technical paths plus the reviewed control-plane/tree paths, commit once on a `codex/` branch, fast-forward a still-clean canonical `main`, push normally, and verify local/live `0/0`; no pull, merge, rebase, amend, force-push, history rewrite or scope expansion is allowed. After synchronization, preserve required proof, stop the exact server, prove the port closed, and remove only the obsolete review worktree and now-unused local branch under D-0054. This propagation does not stage, commit, push, publish or clean up.

### D-0076 — Record GIT-062 publication and close SPEC-0007 Phase 1

- Date: 2026-09-16
- Authority: Arthur's explicit post-CPA instruction to publish, commit, push GitHub, propagate the control plane, and stop when ready for Phase 2.
- Publication: GIT-062 is exact 30-path commit `f051aba7b927902e7aa5fc5f80c4b1d45fe6cca7`, parent `1c5aee42fa87967ad058c00cc3d62b51589b77fa`, message `Implement SPEC-0007 Phase 1 drawing stabilization`. It contains exactly the accepted 17 technical paths plus 13 reviewed control-plane/tree paths. A clean canonical `main` fast-forwarded to it, pushed normally, and local `main`, local `origin/main`, and live GitHub `main` were freshly verified equal at clean `0/0`.
- Proof preservation: the complete accepted Phase 1 output is preserved under `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-062-phase-1-f051aba/phase-1`: 702 files, 711 MiB, relative-path aggregate SHA-256 `1e4be791a8b4c8ccfbd4db130e38f6e83d141af600398d8715a5b26b9152015f`. Source/destination counts and digests match, and checksum rsync dry-run output was empty. Technical manifest remains `511c778e83741b7dde420863769f68132146934f072c6203289bcbf0ea05d689`; validation remains `61514dcb59275292b8f2d9e3b345552b3c8de5128290dde6ff9627b30dba7186`; pre-publication CPA packet remains `02c6dea541189cb89800e5e58634dd9ab29de5b6b10269e25a3a256e20fcb9df`.
- Cleanup: exact review PID 45075 was stopped and TCP port 56875 verified closed. The accepted `/Users/arthurcarlin/.codex/worktrees/f5c0/stick-animation-app` review worktree was removed/unregistered only after proof verification and synchronization. The fully merged local branch `codex/spec0007-phase1-publication` was deleted. Canonical main, the active PM worktree, remote refs and recovery material remain.
- Consequence: SPEC-0007 Phase 1 is accepted, Verified, published, integrated, recorded, and cleaned up. Phase 2 — Draw Rig is ready for Arthur's separate authorization but remains Unauthorized; not started. This closeout adds no Phase 2 implementation, AI/provider/model/prompt/API/video/tracking behavior, dependency/configuration, deployment, external/paid operation, or new app copy/server.

### D-0077 — Accept and technically verify SPEC-0007 Phase 2 Draw Rig

- Date: 2026-09-18
- Authority: Arthur's explicit acceptance of the final stronger-stick review copy at `http://127.0.0.1:56877/` after the separately authorized Phase 2 implementation and correction sequence.
- Status: Accepted and technically Verified; control-plane propagation complete in the accepted worktree; GIT-063 publication/integration, proof preservation and D-0054 cleanup pending. Phase 3 remains Unauthorized; not started.
- Accepted identity: exact ten runtime/fixture/technical-test paths from unchanged detached GIT-062 base/HEAD `88721481a345d6dda3f17b5620111a9cda2ab799`, empty index. Immutable technical manifest `output/spec-0007/phase-2/proof-manifest.json` is PASS at SHA-256 `21db2d920831240be7eeb18fb2ca37126197d762f7ff9a432a3912a0da5809bd`; source digest is `f8d80225c16449afb0a20c6eff88197ec2d2bdf27349e3240211725ee459e8dc`.
- Accepted behavior: Properties exposes Draw Rig for Brush, Pencil, Sketch, Pixelate and Glow. On uses causal append-only `causal-fixed-corridor/v3` centerline segmentation with tolerance `11`, minimum sample distance `0.75`, and direction-settle distance `12`; settled vertices/history never move, the live tail responds without a timer or confirmation delay, and release performs no refit. Smoothing cannot change Draw Rig geometry. Off remains the Phase 1 freehand path. Output is ordinary raster paint/coverage only, with no joint, bone, graph, topology, attachment, rig object or hidden selection model.
- Evidence: the geometry/raster oracle passed 801 assertions, including the final short-arc-one-elbow correction, stronger wobble, retained intentional nook, cadence equivalence, polygonal circle, 100,000-point O(n) work and all five variants/four opacities. Real-browser proof passed 195 assertions, ten evidence captures, the 20-case style/transparency matrix, preview/release identity, stable history, Undo/Redo, Save/Open/Export, frame/layer/onion/playback and 32 draw/undo cycles. Independent manifest validation passed 37 assertions. TypeScript/focused lint/protected Phase 1 regressions passed. The manifest honestly records the untouched inherited route-typing build failure and five-error repository-lint baseline.
- Human acceptance and backup: Arthur called the corrected result acceptable. The preceding accepted-feel review version is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/SPEC-0007-phase-2-v2-stronger-2026-09-18-a7a0/phase2-v2-stronger-runtime-tests-proof.tar.gz`, SHA-256 `368cbe9e076706bca623d2a35f6e6c980c0102c57faccb58fb6e86f1e586ad30`; the earlier V1 backup remains separately preserved. Physical-device/stylus/native-GPU behavior remains unproven.
- Boundaries: no active structured-rig retirement/migration, Assets change, Phase 5 command registry, AI/provider/model/prompt/API/video/tracking behavior, dependency/configuration, deployment, external/paid operation, or Phase 3 authorization is included. `DrawingCanvas.tsx` and `rasterGesture.ts` retain their protected final Phase 2 integration hashes through the strength-only correction.
- Publication/cleanup: preserve the accepted `/Users/arthurcarlin/.codex/worktrees/spec7-phase2-stronger-v3/stick-animation-app`, review server/port 56877 and proof until separate publication succeeds. GIT-063 may stage only the exact accepted ten technical paths plus the reviewed control-plane/tree paths, commit once on a `codex/` publication branch, fast-forward unchanged clean canonical `main`, push normally, and verify local/live `0/0`. No pull, merge, rebase, amend, force-push, history rewrite or scope expansion. After proof preservation and synchronization, stop the exact server, verify both superseded Phase 2 review ports closed, and remove only obsolete Phase 2 review worktrees/now-unused local branch under D-0054. This CPA turn stages, commits, pushes and cleans up nothing.

### D-0078 — Record GIT-063 publication and close SPEC-0007 Phase 2

- Date: 2026-09-18
- Authority: Arthur's explicit post-CPA instruction to put Phase 2 into Diamond Animator, commit, push GitHub, record the control plane and report when Phase 3 is ready.
- Publication: GIT-063 is exact 23-path commit `fb10e6239fd4b1edf319b3137a883e1219645ef3`, parent `88721481a345d6dda3f17b5620111a9cda2ab799`, message `Implement SPEC-0007 Phase 2 Draw Rig`. It contains exactly the accepted ten technical paths plus 13 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it, pushed normally, and canonical `main`, local `origin/main`, and live GitHub `main` were verified equal at clean `0/0`.
- Proof preservation: the complete accepted Phase 2 output is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-063-phase-2-fb10e62/phase-2`: 37 files, 956 KiB, aggregate SHA-256 `3f21db115795431a6564a1dc5833cccad5bd92c27cf240ae3f7e4e95ea982dc2`, with matching source/destination aggregates and empty `rsync -ani --delete`. Technical manifest remains SHA-256 `21db2d920831240be7eeb18fb2ca37126197d762f7ff9a432a3912a0da5809bd`; pre-publication CPA closeout remains SHA-256 `37549714b4d571d1d7bd42f7a628337662dd989c9efb357d14063b2a4d86d870`.
- Cleanup: exact accepted review PID 26063 was stopped; ports 56876 and 56877 were verified closed. `/Users/arthurcarlin/.codex/worktrees/a7a0/stick-animation-app` and `/Users/arthurcarlin/.codex/worktrees/spec7-phase2-stronger-v3/stick-animation-app` were removed/unregistered only after publication, synchronization and backup verification. The fully merged local branch `codex/spec0007-phase2-publication` was deleted. Canonical main, the active PM worktree, remote refs and recovery material remain.
- Consequence: SPEC-0007 Phase 2 is accepted, Verified, published, integrated, recorded and cleaned up. Phase 3 — Safe Legacy Rig Retirement and Migration — has its entry dependency satisfied and is ready for Arthur's separate authorization, but remains Unauthorized; not started. No Phase 3 runtime/proof, Assets, AI/provider/model/prompt/API/video/tracking behavior, dependency/configuration, deployment, external/paid operation or new app copy is included.

### D-0079 — Accept and technically verify SPEC-0007 Phase 3 Safe Legacy Rig Retirement and Migration

- Date: 2026-09-18
- Authority: Arthur's explicit acceptance after reviewing the Phase 3 app copy at `http://127.0.0.1:56940/`, followed by his instruction to record the control plane, commit, integrate into main and push GitHub.
- Status: Accepted and technically Verified; control-plane propagation complete in the accepted worktree. GIT-064 publication/integration, proof preservation and D-0054 cleanup remain pending. Phase 4 remains Unauthorized; not started.
- Accepted identity: exact 15 runtime/fixture/technical-test paths from unchanged GIT-063 base `be89f1421b0588c0a2289b8e174e06f201e8ac5d`, empty index. Immutable 6,066-byte technical manifest `output/spec-0007/phase-3/proof-manifest.json` is PASS at SHA-256 `5d4db763d436cae149405dfc50d757be8ec628f41a728ab336001153d6e61c20`; source digest is `dbb28ca4acdfac7f69192831e7eb0a06122bfd79ba31b5e48bf92d9321e03cb4`.
- Accepted behavior: the ordinary workspace has no active Stick Figure/Rig Tools, joint/limb authoring, Creator flow, new rig save path or active rig hydration. A versioned fail-closed retirement boundary validates and rasterizes old rig owners once into ordinary drawing items, converts rig-only symbols to Drawing Symbols and mixed definitions to drawing-only Mixed Symbols, preserves IDs/transforms/order where safe, returns no-rig projects unchanged, records a migration/recovery receipt, preserves source stores read-only, and retains the exact native-V2 pre-migration predecessor on first Save.
- Evidence: production build and full no-emit TypeScript passed. Static proof passed 23 retirement/isolation assertions. Real-browser proof passed 21 Open/migrate/edit/Undo/Save/recovery/reopen assertions with 3,457 migrated pixels and zero page/browser errors. Phase 1 raster proof passed 19,844 assertions, no-loss passed, Phase 2 corridor/style proof passed with protected command sources byte-unchanged, and six-source persistence passed 1,117 assertions across Drawing V1/V2, Stick V1/V2 and unified V1/V2. Independent manifest replay passed 11 assertions; external/provider requests were zero.
- Boundaries: historical structured-rig parser/validator/renderer code remains only behind the read/migration boundary. Accepted Drawing and Draw Rig behavior, toolbar/timeline, ordinary symbols, Assets, AI/provider/model/prompt/API/motion/video/tracking, Home/Tutorials, dependencies/configuration, auth/billing/export/deployment and external network behavior remain unchanged. Physical-device/native-GPU behavior and exhaustive owner-supplied historical-project visuals beyond the representative fixture remain unproven.
- Publication/cleanup: preserve the accepted Phase 3 worktree, port 56940 and proof until publication succeeds. GIT-064 may stage only the exact accepted 15 technical paths plus reviewed control-plane/tree paths, commit on `codex/spec7-phase3-retirement`, fast-forward unchanged clean canonical `main`, push normally and verify clean local/live `0/0`. No pull, merge, rebase, amend, force-push, history rewrite or scope expansion. After proof preservation and synchronization, stop the exact review server, verify the port closed, and remove only the obsolete Phase 3 worktree and merged local branch under D-0054.

### D-0080 — Record GIT-064 publication and close SPEC-0007 Phase 3

- Date: 2026-09-18
- Authority: Arthur's explicit post-review instruction to record the control plane, commit, integrate Phase 3 into main, push GitHub and report when Phase 4 is ready.
- Publication: GIT-064 is exact 28-path commit `b8d3b37faeaaa1a3d8bfb27359e04acd08f7acab`, parent `be89f1421b0588c0a2289b8e174e06f201e8ac5d`, message `Implement SPEC-0007 Phase 3 rig retirement`. It contains exactly the accepted 15 technical paths plus 13 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it, pushed normally, and canonical `main`, local `origin/main`, and live GitHub `main` were verified equal at clean `0/0`.
- Proof preservation: the complete accepted Phase 3 output is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-064-phase-3-b8d3b37/phase-3`: five files, 92 KiB, aggregate SHA-256 `168ebd8ad3aad1e18227edcafc3c1148fd25a8c3ab6d4185c46b8b00e1552a42`, with matching source/destination aggregates and empty `rsync -ani --delete`. Technical manifest remains SHA-256 `5d4db763d436cae149405dfc50d757be8ec628f41a728ab336001153d6e61c20`.
- Cleanup: exact review PIDs 32633/32655 were stopped and port 56940 verified closed. The obsolete Phase 3 implementation worktree was removed/unregistered and merged local branch `codex/spec7-phase3-retirement` deleted. Canonical main, the active PM worktree, remote refs and recovery material remain.
- Consequence: SPEC-0007 Phase 3 is accepted, Verified, published, integrated, recorded and cleaned up. Phase 4 — Assets Stabilization — has its entry dependency satisfied and is ready for Arthur's separate authorization, but remains Unauthorized; not started. No Phase 4 runtime/proof, AI/provider/model/prompt/API/video/tracking behavior, dependency/configuration, deployment, external/paid operation or new review app is included.

### D-0081 — Accept and technically verify SPEC-0007 Phase 4 Assets Stabilization

- Date: 2026-09-18
- Authority: Arthur's explicit acceptance after reviewing the Phase 4 app copy at `http://127.0.0.1:56950/`, testing image import and onion skin, followed by his instruction to record the control plane and later commit, integrate and push the accepted result.
- Status: Accepted and technically Verified; control-plane propagation complete in the accepted worktree. GIT-065 publication/integration, proof preservation and D-0054 cleanup remain pending. Phase 5 remains Unauthorized; not started.
- Accepted identity: exact 11 runtime/fixture/technical-test paths from unchanged GIT-064 base/HEAD `f16d36b454728b3e216ec738013366f0230ddbdb`, empty index. Immutable 5,851-byte technical manifest `output/spec-0007/phase-4/proof-manifest.json` is PASS at SHA-256 `36ad00d6bdbe9ebc2ab3640f2ab5a9987ab46780f910b5d470a9142f32f19c4c`; source digest is `509dbde25762fdb358d0625d9bf151c95b00e5d260bc984ae3a4b9d432439421`.
- Accepted behavior: chooser and OS-drop imports accept only fully validated static PNG/JPEG/WebP files. Atomic preflight enforces extension/MIME/signature/structure/non-animation agreement, size/dimension/pixel/RGBA/batch ceilings and duplicate-name/content rejection before catalog mutation. Truthful previews expose format, dimensions and bytes. Placement supports move, eight-handle resize, default aspect lock/explicit unlock, rotation and commit/cancel; committed pixels remain ordinary raster content. Explicit catalog Delete uses the destructive registry, rejects live references and is Undo/Redo-safe. Save/reload/Open preserves exact catalog and pixels; stale async completion cannot cross project context; decode object URLs are revoked; external fetch is absent.
- Evidence: static proof passed 28 assertions. Real-browser proof passed 30 assertions with representative desktop/compact imports at 49.24/32.43 ms, 20-cycle p95 57.65 ms, heap below 320 MiB, 20/20 URL revocation, zero errors and zero external requests. Phase 1 raster proof passed 19,844 assertions plus no-loss/coverage persistence; Phase 2 corridor proof and Phase 3 static/browser migration proof passed. Source TypeScript and focused lint passed with zero errors/18 inherited warnings. Production compilation reached only the untouched inherited `app/dev/ai-costs/lifetime/page.tsx` PageProps baseline error.
- Boundaries: accepted Drawing/Draw Rig/rig retirement, toolbar/timeline/layers/frames/onion/playback, symbols, AI/provider/model/prompt/API/motion/video/tracking, Home/Tutorials, dependencies/configuration, auth/billing/export/deployment and external network behavior remain unchanged. Static inputs intentionally exclude GIF/SVG/AVIF/HEIC/PDF/PSD, animated PNG/WebP, generic/spoofed/corrupt files, audio and video. Physical-device/native-GPU behavior remains unproven.
- Publication/cleanup: preserve the accepted Phase 4 worktree, port 56950 and proof until publication succeeds. A later explicit GIT-065 task may stage only the exact accepted 11 technical paths plus reviewed control-plane/tree paths, commit on a `codex/` publication branch, fast-forward unchanged clean canonical `main`, push normally and verify clean local/live `0/0`. No pull, merge, rebase, amend, force-push, history rewrite or scope expansion. After proof preservation and synchronization, stop the exact review server, verify the port closed, and remove only the obsolete Phase 4 worktree and merged local branch under D-0054.

### D-0082 — Record GIT-065 publication and close SPEC-0007 Phase 4

- Date: 2026-09-18
- Authority: Arthur's explicit post-CPA instruction to publish Phase 4, integrate it 100% into the real app and legitimately stop when Phase 5 is ready.
- Publication: GIT-065 is exact 24-path commit `0dc707ff249560f4f19b8359c6a44a383e8caf92`, parent `f16d36b454728b3e216ec738013366f0230ddbdb`, message `Implement SPEC-0007 Phase 4 assets stabilization`. It contains exactly the accepted 11 technical paths plus 13 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it, pushed normally, and canonical `main`, local `origin/main` and live GitHub `main` were verified equal at clean `0/0`.
- Proof preservation: the complete accepted Phase 4 output is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-065-phase-4-0dc707f/phase-4`: six files, 64 KiB, aggregate inventory SHA-256 `6e0239c7ec6ded8253b83833ea14d5e79b6570249fd5dce2b20ee25595013e47`, with matching source/destination inventories and empty `rsync -ani --delete`. Technical manifest remains SHA-256 `36ad00d6bdbe9ebc2ab3640f2ab5a9987ab46780f910b5d470a9142f32f19c4c`.
- Cleanup: exact review PIDs 36261/36270 were stopped and port 56950 verified closed. The obsolete Phase 4 review worktree was removed/unregistered and merged local branch `codex/spec0007-phase4-assets` deleted. Canonical main, the active PM worktree, remote refs and recovery material remain.
- Consequence: SPEC-0007 Phase 4 is accepted, Verified, published, integrated, recorded and cleaned up. Phase 5 — Final Manual Editor Bug Burn and Future-AI Command Registry — has its entry dependency satisfied and is ready for Arthur's separate authorization, but remains Unauthorized; not started. No Phase 5 runtime/proof, AI/provider/model/prompt/API/video/tracking behavior, dependency/configuration, deployment, external/paid operation or new review app is included.

### D-0083 — Accept and technically verify SPEC-0007 Phase 5 Final Manual Editor Bug Burn and Future-AI Command Registry

- Date: 2026-09-18
- Authority: Arthur's explicit acceptance after reviewing the final Phase 5 app copy at `http://127.0.0.1:56960/`, followed by his instruction to record the control plane and later commit, integrate and push the accepted result.
- Status: Accepted and technically Verified; control-plane propagation complete in the accepted worktree. GIT-066 publication/integration, proof preservation and D-0054 cleanup remain pending. SPEC-0008 remains Unauthorized; not created.
- Accepted identity: exact eight runtime/fixture/technical-test paths from unchanged GIT-065 base/HEAD `5f2637faf56ab1f2df1408080c7cc1cacf4d6fab`, empty index. Immutable 11,385-byte technical manifest `output/spec-0007/phase-5/proof-manifest.json` is PASS at SHA-256 `f2b2d5939488eba9caf757b0149b60476936996d5fcd6876f59d46c7a406174c`; source digest is `381ef09ad28a389c25ab4e3e547e96339648b09757cafe13a4ff4e012d560766`. Independent validation is VALID with 37 assertions and eight rejected manifest mutations.
- Root cause and accepted correction: a failed coordinator publication could occur after the canvas already held a destructive visible mutation. The rollback restored refs/history but not the visible canvas/transient editor, so a queued pointer-up autosave could later publish stale pixels and appear as random deletion. Failure now cancels transient editing and repaints the exact last-good committed bitmap before releasing the pending authoring marker. Tween playback cache construction now skips every owner that does not actually span a tween cell.
- Registry outcome: one runtime-used, UI-independent manual capability registry contains exactly 40 enabled commands and all 11 destructive commands. Each entry binds visible control, schema, validator, sanctioned handler, deletion class, no-loss rule, history class, persistence projection, failure behavior and future-AI eligibility. Manual adapters require the entry before entering the existing coordinator; no AI caller, provider, network path or second mutation engine is added.
- Evidence: static proof passed 155 assertions and seven negative registry mutations. Browser proof passed 51 assertions with one injected publication failure, 160 interleaved authored operations, 162 Undo and 162 Redo operations, five warmed playback loops, 191 ms maximum long task, 139,535,920-byte settled heap, no serious Axe result, no compact overflow, zero external requests and zero browser errors. Phase 1 raster/coverage, Phase 2 Draw Rig, Phase 3 migration, accepted Phase 4 receipt, SPEC-0006 migration/foundation/persistence and timeline/playback regressions passed. TypeScript, focused lint and production Webpack compilation passed before the unchanged inherited generated AI-cost route-type baseline.
- Boundaries and honest limitations: accepted brush/coverage and Draw Rig algorithms, migration/source writers, Assets importer/catalog contracts, AI/provider/model/prompt/API/motion/video/tracking, Home/Tutorials, dependencies/configuration, auth/billing/export/deployment and external services remain unchanged. The old Phase 1 no-loss oracle still hard-codes ten rather than eleven destructive commands; the old Phase 2 browser harness still waits for retired rig SVG; and the old Phase 4 rerun harness retains a nondeterministic render/GC heuristic. Those inherited harness limits are recorded, not converted into product regressions or weakened.
- Publication/cleanup: preserve the accepted Phase 5 worktree, PID 46510/port 56960 and complete ignored proof until publication succeeds. A later explicit GIT-066 task may stage only the exact accepted eight technical paths plus reviewed control-plane/tree paths, commit on a `codex/` publication branch, fast-forward unchanged clean canonical `main`, push normally and verify clean local/live `0/0`. No pull, merge, rebase, amend, force-push, history rewrite or scope expansion. After proof preservation and synchronization, stop the exact review server, verify the port closed, and remove only the obsolete Phase 5 worktree and merged local branch under D-0054. Only then is SPEC-0007 fully closed and ready for separately authorized SPEC-0008 creation.

### D-0084 — Record GIT-066 publication and close SPEC-0007

- Date: 2026-09-18
- Authority: Arthur's explicit post-CPA instruction to publish Phase 5 and start autopilot until the accepted result was integrated into the real Diamond Animator.
- Publication: GIT-066 is exact 21-path commit `c193b8ba89fa55ead02d84ea10bb81f71b960f8a`, parent `5f2637faf56ab1f2df1408080c7cc1cacf4d6fab`, message `Implement SPEC-0007 Phase 5 manual editor closeout`. It contains exactly the accepted eight technical paths plus 13 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it, pushed normally, and canonical `main`, local `origin/main` and live GitHub `main` were verified equal at clean `0/0`.
- Proof preservation: the complete accepted Phase 5 output is preserved at `/Users/arthurcarlin/.codex/worktrees/2d14/stick-animation-app/output/recovery/GIT-066-phase-5-c193b8b/phase-5`: six files, 96 KiB, aggregate inventory SHA-256 `9ca574bfc4cca30466101b4fff997d6eed66276a358dd8ed63fe659fdfbb6c98`, with matching source/destination inventories and empty `rsync -ani --delete`. Technical manifest remains SHA-256 `f2b2d5939488eba9caf757b0149b60476936996d5fcd6876f59d46c7a406174c`.
- Cleanup: exact review PID 46510 was stopped and port 56960 verified closed. The obsolete `/Users/arthurcarlin/.codex/worktrees/a1b1/stick-animation-app` Phase 5 review worktree was removed/unregistered and merged local branch `codex/spec0007-phase5-publication` deleted. Canonical main, the active PM worktree, remote refs and recovery material remain.
- Consequence: all five SPEC-0007 phases are accepted, Verified, published, integrated, recorded and cleaned up. Future SPEC-0008 is ready for Arthur's separate authorization and creation but remains uncreated and unauthorized. No handoff message, AI/provider/model/prompt/API/video/tracking work, external/paid operation or deployment is included.

### D-0085 — Propose the six-phase conversational reference-video-to-editable-drawing V1

- Date: 2026-09-18
- Authority: Arthur's explicit instruction to act as SPEC-0008 Spec Architect, create the decision-complete six-phase plan, update only planning/control-plane records, and stop before implementation, provider use, paid work, deployment, Git publication or a new PM handoff.
- Decision: Create [`SPEC-0008 — Conversational AI Animator and Editable Video Reconstruction`](specs/0008-conversational-ai-animator.md) as **Proposed; decision-complete and review-ready; no implementation phase authorized**. The exact order is: (1) Terra brain/intent routing/persistent streamed jobs; (2) provider-neutral reference video; (3) deterministic cucumber slicer; (4) editable drawing reconstruction; (5) conversational edits through the manual command registry; (6) beta closeout and downloadable YouTube-ready export.
- Brain/UI contract: production conversation uses exactly `gpt-5.6-terra` through the Responses API, with no model selector, alternate model, automatic fallback or silent paid retry. The only visible reasoning levels are Low/Medium/High/Extra High mapped to `low`/`medium`/`high`/`xhigh`; Medium is the default on every new/open workspace. The task picker retires; conversation infers plan/build/edit and asks exactly one focused question only for material ambiguity.
- Historical transaction contract, superseded for SPEC-0008 by D-0092: D-0085 originally required user-visible Preview and explicit Apply after isolated generation. The still-current parts require every authored mutation to compile to the runtime-used manual command registry, bind project generation/base digest, and commit as one atomic V2 history transaction; failure/stale state leaves the original byte-equivalent; AI cannot use a capability unavailable to a human; and destruction requires explicit current-message intent plus a registered manual destructive command. D-0092 replaces only the second user approval gate with explicit-message authorization and automatic commit after complete internal validation.
- Provider and deprecation contract: reference-video generation is a replaceable adapter behind an explicit provider/access/cost/privacy gate; no provider is selected now. Arthur's dated planning input records the OpenAI Sora 2/Videos API as deprecated with shutdown scheduled for 2026-09-24, so it cannot be hardwired and the gate must recheck current official state before live use. Validated local MP4/WebM upload is the fallback. Provider state normalizes queued/in-progress/completed/failed/cancelled with explicit retry/cancel and no canvas mutation.
- Product/proof contract: V1 is bounded to 4–12 seconds, one to three shots, 6–12 distinct authored frames per second with at most 144 reference samples, at most 1280×720 reference dimensions, at most six reconstructed layers per shot and existing storage/memory limits; final export may reach 1920×1080 through the separately gated encoder path. Status language is Thinking/Planning/Making video/Animating/Working/Finalizing/Done/Failed/Cancelled. **Animating** means only deterministic cutting/slicing of the reference video into frames. **Working** means building/reconstructing those frames into ordinary editable content in Phase 4 and preparing registered conversational edit commands in Phase 5. Unused stages are skipped and determinate progress uses only real measurements—never timers or fake percentages. Two non-hardcoded build stories (black-vs-red action and alien/UFO) plus one registered-command edit story prove the real flow. Phase 6 owns security/ownership/rate-limit/privacy/log-retention/spend and same-day YouTube-ingest/encoder gates; direct YouTube-account upload is later work.
- Lifecycle: the target is one phase/day plus one correction/publication buffer day, but a phase/day is incomplete until Spec Executor result, Arthur/PM acceptance, CPA propagation, separate publication/integration, clean synchronization and D-0054 cleanup all finish. Schedule never weakens proof. Phase 1 is ready only for Arthur's later separate approval/authorization after this proposal is reviewed and published. Phases 2–6 remain unauthorized behind their predecessor and named entry gates.
- Boundaries: this record changes only the new spec and canonical planning/control-plane/tree files. It performs no runtime, fixture, technical-test or proof implementation; no provider/API/paid/live request, upload, credential/dependency/configuration, server/worktree, deployment, stage, commit, push or publication; and no new PM V4 task or handoff message. Existing accepted SPEC-0006/0007 behavior and rejected historical evidence remain unchanged.

### D-0086 — Publish the SPEC-0008 planning package without authorizing implementation

- Date: 2026-09-18
- Authority: Arthur explicitly confirmed that the SPEC-0008 planning/history package should be propagated, recorded, committed and pushed to GitHub while keeping real app behavior unchanged.
- Publication: GIT-067 is exact 15-path planning-only commit `ba5ecd694df78414240ce83a9140c5196334ebdc`, parent `f923c35aa13cfd476e712f4ead89419263fc0893`, message `Propose SPEC-0008 conversational AI animator`. Clean canonical `main` was fast-forwarded and pushed normally; canonical `main`, local `origin/main` and live GitHub `main` matched that commit before this records-only reconciliation.
- Lifecycle: SPEC-0008 remains **Proposed; published; awaiting Arthur's explicit approval**. All six phases remain Unauthorized/Not started. The exact next owner action is separate approval of SPEC-0008 and separate authorization of Phase 1; only then may one fresh Plan-mode Spec Executor app copy start from the final synchronized records-publication SHA.
- Boundaries: publication changed only the reviewed spec, planning/control-plane records and generated project tree. It changed no runtime, fixture, technical test, proof artifact, dependency/configuration, provider/API/model behavior, paid/external request, upload, deployment or Phase 1 implementation.

### D-0087 — Approve SPEC-0008 and authorize Phase 1 only

- Date: 2026-09-18
- Authority: Arthur explicitly instructed the Project Manager to start autopilot after archiving obsolete tasks, use one fresh app copy, and begin the first programming phase. This is the separate owner approval of the published SPEC-0008 proposal and the separate authorization required for Phase 1.
- Decision: [`SPEC-0008`](specs/0008-conversational-ai-animator.md) is **Approved/active** exactly as published under D-0086/GIT-067. **Phase 1 — Terra brain, automatic intent routing, and chat/job foundation** alone is **Authorized/Not started**. One fresh Plan-mode Spec Executor may begin only after this authorization record is committed, published, and synchronized.
- Execution boundary: the executor implements and technically proves only SPEC-0008 §9 from the exact published authorization base, freezes the smallest exact runtime/fixture/test/proof allowlist after fresh tracing, returns its Implementation Review Packet, and stops without canonical-doc or Git mutation. It uses `gpt-5.6-sol` for the Codex programming task as Arthur requested; the product contract itself remains fixed to `gpt-5.6-terra` through the Responses API.
- Cost/time boundary: technical proof uses a deterministic Responses API double and zero live/provider/search/video requests by default. A real paid Terra smoke still requires separate exact request-count, spend, privacy and current-access approval. Arthur requested a bounded roughly one-hour execution window; schedule pressure does not weaken proof or expand scope.
- Later gates: Phases 2–6 remain Unauthorized/Not started. No video provider/model selection, reference-video call, slicer, reconstruction, registered-command edit execution, export, public beta, deployment, implementation publication or paid/external operation is authorized by this decision.
- Task cleanup: before authorization, the Project Manager archived the thirteen obsolete SPEC-0006/0007 tasks identified from Arthur's screenshot and preserved the active Project Manager V4 and SPEC-0008 Architect tasks. Archiving changed task visibility only; it did not delete repository bytes or accepted proof.
- Publication: GIT-068 is exact ten-path commit `1d80141f2465db6f0e389fb43dc1cdfb8dafbdf8`, parent `a7fac36311e34425d8ff08c0a66bc758a938268f`, message `Authorize SPEC-0008 Phase 1 Terra brain`. Clean canonical `main` was fast-forwarded and pushed normally; canonical `main`, local `origin/main` and live GitHub `main` matched that commit at clean `0/0` before this records-only reconciliation.

### D-0088 — Reject the first SPEC-0008 Phase 1 result and authorize one fresh correction

- Date: 2026-09-19
- Authority: Arthur visibly reviewed the first Phase 1 app copy, typed `hello`, observed the generic message “The AI Animator could not complete this request. No animation change occurred.” without the expected gradient Thinking state, called the result a failure, and explicitly authorized one correction autopilot. Arthur then separately authorized at most six short live Terra conversation requests with a hard aggregate spend ceiling of $0.50.
- Rejection: the stopped `/b027/` result is **Rejected, unpublished, unaccepted and non-reusable**. Its self-reported 13-path technical result and manifest SHA-256 `981d1a1ddfa848449008acc87818e654e5754105bd4c93c7a4f728962a984abb` do not override the failed visible conversation flow. A valid greeting must receive a useful real Terra reply; Phase 1 may not replace normal conversation with a generic animation failure or a hard-coded message.
- Cleanup: the exact 13 dirty paths plus ignored Phase 1 proof were preserved before removal at `output/recovery/SPEC-0008-phase1-rejected-2026-09-19-b027`: 20 files, approximately 1.1 MiB, aggregate inventory SHA-256 `e872b12265b40fdc9e57ec8cd9c19658baeef5d01a8a221e687bbc7b894d3cb2`, with an empty `rsync -ani --delete` comparison. Exact server PID 63165 was stopped, listener 63166 disappeared, port 57080 closed, and `/Users/arthurcarlin/.codex/worktrees/b027/stick-animation-app` was removed/unregistered. It had no local branch to delete.
- Correction outcome: one fresh Plan-mode Phase 1 Spec Executor may implement from the final synchronized D-0088 publication SHA without copying, resuming, cherry-picking or correcting rejected bytes in place. The one chat must use the real `gpt-5.6-terra` Responses API for normal in-bounds conversation, understand arbitrary user wording from bounded context, avoid hard-coded replies/keyword recipes, visibly show truthful gradient **Thinking**, preserve the non-mutating Phase 1 create/edit readiness boundary, and leave Brush, timeline, canvas, manual tools, history, persistence and every other workspace subsystem unchanged.
- Paid-smoke boundary: the executor may make at most six short text-only Terra requests and may spend no more than $0.50 total. Search and hosted tools remain off; no raster, image, asset, audio, full project bytes, secrets or unrelated project data may be sent. There is no automatic retry. The existing key remains server-only and may be supplied to the review server at runtime without printing, copying into tracked files, browser storage, logs, screenshots, manifests or project bytes. Every live call must record redacted request identity, provider model identity, usage/cost when returned, and the running count/ceiling.
- Boundaries: deterministic provider-double proof remains mandatory and live smoke cannot replace it. Phases 2–6, video providers, slicing, reconstruction, registered-command mutation, export, deployment and implementation publication remain unauthorized.
- Publication: GIT-069 is exact 11-path control-plane commit `e682bd5357624c934789eefbc64dbba43116b246`, parent `dfa945595965f8d4d74e503965169fcef033f523`, message `Authorize SPEC-0008 Phase 1 correction`. Clean canonical `main` fast-forwarded and pushed normally; canonical `main`, local `origin/main` and live GitHub `main` matched that commit at clean `0/0` before this records-only closeout.

### D-0089 — Accept and technically verify the corrected SPEC-0008 Phase 1 result

- Date: 2026-09-19
- Authority: Arthur reviewed the final correction at `http://127.0.0.1:57120/`, called it acceptable, and explicitly instructed the Project Manager to record the control plane, integrate the accepted result into the real Diamond Animator, commit it and push it to GitHub.
- Accepted identity: the stopped correction worktree is detached at unchanged GIT-069 closeout base/HEAD `3da58e096dd748c7c3bd23fbb9271d53e33597ca`, with empty index and exactly 19 technical dirty paths. Immutable 26,698-byte manifest `output/spec-0008/phase-1/proof-manifest.json` is PASS/VALID at SHA-256 `80a5463775f498116389e49cb94d3282023355c6f7dd1bae9cc026a727916313`; aggregate source SHA-256 is `59b985054243e83b177be241fe55c05634a6b5f278ea981e2d69203c3c6d330a`; independent validation rejects 40 material mutations.
- Accepted behavior: one ordinary AI Animator chat uses exact `gpt-5.6-terra` Responses calls with no alternate/fallback model, maps the four visible reasoning levels exactly and defaults Medium, semantically handles arbitrary bounded conversation/create/edit/clarify turns, stores bounded project-scoped transcript/job state, exposes truthful cancel/reconnect/failure behavior, and makes no Phase 1 authored animation/history/repository mutation. Assistant replies use the accepted unboxed fast reveal; active work shows a truthful minimum two-second Thinking state with sequential full-label bright-blue/cyan sweeps. Arthur's later explicit narrow correction authority also accepts the 280–520 px right-sidebar resize/default snap, fixed centered canvas geometry, matching toolbar width, and expanded timeline overlay stacking; these are presentation mechanics only and do not authorize Phase 2.
- Evidence: browser proof passed 254 assertions over desktop, compact and reduced-motion profiles, 18 deterministic captured requests, sidebar/timeline/manual-tool regressions, zero external requests and zero errors. Source TypeScript, focused lint, protected storage/memory/timeline checks and production build `pEecesY-BkoQ2ZdbBifLL` passed; exact authored project/history/repository/canvas digests were preserved. Six separately authorized short text-only live Terra calls used exact model `gpt-5.6-terra` and cost $0.010854 total under the $0.50 ceiling; search/tools and raster/assets/full-project data were absent. CPA reran the unchanged strict validator before propagation (PASS/VALID, 40 mutation rejections); after adding the 14 reviewed control-plane/tree paths, that dirty-set validator correctly rejects the expanded set, while an independent offline recheck still passes all 19 source bindings, 22 artifact bindings and the base-commit spec binding.
- Boundaries and limitations: Phase 1 creates no reference video, frame bundle, reconstruction, registered-command edit, project version, export or deployment. Compact proof is desktop Chromium emulation, not a physical-device/native-GPU claim. A full process restart cannot resume an in-memory active provider job and instead yields an honest recoverable failure. The manifest retains the stopped executor's historical `humanAcceptance: pending Arthur`, `controlPlaneUpdated: false` and `gitPublication: false`; this decision records later acceptance without rewriting executor evidence.
- Publication/cleanup: control-plane propagation is complete in the accepted worktree. GIT-070 publication/integration, proof preservation and D-0054 cleanup remain pending. The separately authorized publisher may stage only the exact 19 accepted technical paths plus reviewed control-plane/tree paths, commit on a `codex/` branch, fast-forward unchanged clean canonical `main`, push normally and verify clean local/origin/live equality. After synchronization, preserve complete proof, stop the exact port-57120 review server, verify closure, and remove only the obsolete `/22ac/` review worktree and merged local branch. Phases 2–6 remain unauthorized.

### D-0090 — Record GIT-070 publication and close SPEC-0008 Phase 1

- Date: 2026-09-19
- Authority: Arthur's explicit instruction after accepting the final review copy to place it in the real Diamond Animator, record the control plane, commit and push to GitHub, and report when Phase 2 is ready.
- Publication: GIT-070 is exact 33-path commit `76708645c96b3b0ea95c524f162bb6152d539fcf`, parent `3da58e096dd748c7c3bd23fbb9271d53e33597ca`, message `Implement SPEC-0008 Phase 1 conversational Terra`. It contains exactly the accepted 19 technical paths plus 14 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it and pushed normally; canonical `main`, local `origin/main` and live GitHub `main` matched that commit at clean `0/0` before this terminal records commit.
- Proof preservation: the complete accepted Phase 1 output is preserved at `/Users/arthurcarlin/.codex/worktrees/7d98/stick-animation-app/output/recovery/GIT-070-phase-1-7670864/phase-1`: 36 files, 1,258,202 bytes, aggregate framed inventory SHA-256 `d91b641e88030ada0a173c01c005bc336c8a765fdb7da64badc759f249e4c133`. Source and destination file counts, bytes and aggregate hashes matched; checksum-mode `rsync -ani --delete` was empty. Technical manifest remains SHA-256 `80a5463775f498116389e49cb94d3282023355c6f7dd1bae9cc026a727916313`.
- Cleanup: exact review PIDs 85980/85995 are gone and port 57120 is closed. `/Users/arthurcarlin/.codex/worktrees/22ac/stick-animation-app` is absent/unregistered and merged local branch `codex/spec0008-phase1-publication` is deleted. Canonical main, the active Project Manager worktree, remote refs and recovery material remain.
- Consequence: SPEC-0008 Phase 1 is accepted, Verified, published, integrated, recorded and cleaned up. Phase 2 — Provider-neutral reference-video generation and verification — has its predecessor gate satisfied and is ready for Arthur's separate authorization, but remains Unauthorized/Not started. Any live provider use also requires the separate same-day §10.1 provider/access/cost/privacy approval; no provider, live video call, Phase 3 work, deployment or additional paid operation is included.
- Terminal record: the canonical commit containing D-0090 is GIT-071. Its authoritative SHA is read from Git after this exact records-only package is committed, fast-forwarded and synchronized; it is intentionally not self-declared in these bytes, and no follow-on records-only closeout is required.

### D-0091 — Integrate bounded Terra internet research into existing SPEC-0008 Phase 2

- Date: 2026-09-19
- Authority: Arthur explicitly chose Option B and instructed the SPEC-0008 Spec Architect to integrate internet searching into the existing Phase 2 contract before programming, keep exactly six phases and the one-week delivery target, update only the active spec and appropriate canonical control-plane/project-tree records, and stop before runtime implementation, live provider/API use, credits, deployment or Git publication.
- Six-phase decision: Phase 1 remains closed and byte-preserved. Phase 2 becomes **Internet research and provider-neutral reference-video preparation**; no Phase 7 is added, no Phase 1 behavior is reopened, and Phases 3–6 retain their owners and order. Phase 2 remains **Unauthorized/Not started** until this reviewed control-plane correction is separately published/synchronized and Arthur later gives a separate implementation authorization.
- Official capability basis: current official OpenAI documentation retrieved 2026-09-19 says `gpt-5.6-terra` supports Responses API web search, new Responses integrations use hosted `web_search` rather than legacy `web_search_preview`, web-derived claims require visible clickable citations, `max_tool_calls` caps built-in tool use, and `max_output_tokens` includes both visible output and reasoning tokens. This confirms a viable contract only; it does not prove account access, current pricing/retention, or authorize a live call.
- Search contract: Phase 2 may use only fixed `gpt-5.6-terra` through Responses with hosted `web_search`, live external access, low context, default return budget, required tool choice, `store: false`, no parallel tool calls, no model/search/provider fallback, and no app-owned scraping. Search is text-only: remote image search/results are not enabled, displayed, downloaded, or stored. The request preserves the visible Low/Medium/High/Extra High choice as Terra `low`/`medium`/`high`/`xhigh`; Medium remains the new/open default and no silent effort downgrade is allowed. Explicit search/look-up/YouTube/TikTok reference requests search; one unfamiliar named public reference may trigger one automatic search only when its exact missing fact and necessity are recorded. Greetings, simple conversation, generic brainstorming, or answers available from bounded context do not search. Search emits **Searching the internet** only while the real tool call is active and stops immediately on completion/cancel/failure/timeout.
- Inspiration and source contract: a successful search produces schema-validated, project/job/generation-bound `inspiration-brief/v1` with query digest, timestamps, observed-versus-inferred high-level subject/action/pacing/camera/color/atmosphere/prop/constraint findings, and exact source title/URL/citation bindings. Citations are visible, keyboard reachable and clickable. Web text is untrusted data that cannot alter system instructions, model, caps, provider choice, project state or schema. High-level inspiration is permitted; copying frames/assets/characters/creator identifiers/dialogue/music/shot sequences, close imitation, reconstruction of the source, or implied endorsement is forbidden.
- Social/video boundary: the app does not gain arbitrary URL fetching, custom crawling/scraping, login/cookie/private-page access, paywall/restriction bypass, raw YouTube/TikTok/social-video download, stream extraction or remote frame capture. If indexed evidence cannot establish exact motion, the UI says so and offers the validated local MP4/WebM path. Exact motion/timing/camera/frame evidence requires a locally selected validated file plus explicit ownership/right-to-use acknowledgment. The provider-neutral video adapter and representative-frame verification remain Phase 2 owners.
- Hard limits and cost/privacy gate: one research attempt has one search-enabled Responses request, at most two built-in tool calls, at most eight candidate source records accepted/processed by the application and six retained/cited, 512 normalized query characters, 16,000 input tokens, 4,000 output tokens including reasoning, low search context, default returned-token budget, a 45-second call deadline, a 60-second research-stage deadline, and no automatic retry. Extra provider-returned source records are ignored after stable canonicalization; this application limit does not claim to control which sources the hosted provider internally consulted. Any separately authorized live use additionally fails closed above $0.15/attempt, $0.60/project/UTC day, or $5/private-development-environment/UTC month using a same-day approved price table and remaining ledger. Unknown pricing/usage prevents the call. The search query is a minimal public-information projection and excludes project/transcript history, identities, local paths, secrets and authored media. Raw provider responses are discarded after validation; at most eight terminal briefs remain in the project-scoped local ledger with oldest-first eviction and deletion alongside that project/AI ledger. Telemetry excludes secrets, cookies, headers, page bodies, arbitrary transcripts, downloaded media, hidden prompts and unrelated project data. Phase 2 adds no server-side application retention.
- Status animation and proof: every active status label uses the same length-independent 1.0-second bright-blue sweep, immediately followed by a 1.0-second dark-cyan sweep and about a 1.5-second no-sweep pause; it stops on transition and becomes text-only under reduced motion. Deterministic Responses/search doubles, fake incompatible video adapters, semantic trigger/no-trigger cases, clickable citation binding, injection/tamper negatives, stale/cancel/reconnect/isolation cases, inaccessible social links, cap/cost failures, local-upload rights acknowledgment, representative-frame mismatch detection, and exact zero editor/history/repository mutation are mandatory Phase 2 exit proof. Live proof, if separately authorized, is supplemental only.
- Boundaries and publication closeout: this decision changed only the active spec and canonical planning/control-plane/tree records from exact base `888aae769e67e08adb1862c81f21bec171fd0592`. It changed no runtime, fixture, technical test, proof artifact, dependency, credential, provider integration or deployment; made no live OpenAI/provider request; and spent no credits. Arthur accepted the correction and separately authorized publication. GIT-072 is exact 13-document commit `9b4b116d24b076b1c05d91a02fd7318ae5a44148`, parent GIT-071 `888aae769e67e08adb1862c81f21bec171fd0592`, message `Specify SPEC-0008 Phase 2 web research`; canonical `main`, local `origin/main` and live GitHub `main` were synchronized at clean `0/0`. Phase 2 implementation remains Unauthorized/Not started, and live search/video-provider tests retain separate dated approval packets.

### D-0092 — Treat an explicit create/edit message as authorization for automatic atomic commit

- Date: 2026-09-19
- Authority: Arthur explicitly instructed that after a clear request to create or edit an animation, Diamond Animator must not stop at a Preview/Apply approval gate. He authorized this documentation-only final correction from published GIT-072 while prohibiting runtime, test, fixture, provider or app-behavior changes and requiring the task to stop before Git publication.
- Authorization decision: an unambiguous current message explicitly asking Diamond Animator to create an animation or edit the current animation authorizes one bounded mutating job. Planning, brainstorming, discussion, questions and advice remain non-mutating. Genuine material ambiguity asks exactly one focused question before mutation. A destructive operation still requires explicit current-message destructive intent, an exact resolvable target and a registered manual destructive command with its existing confirmation rules; general create/edit wording cannot authorize inferred deletion.
- Transaction decision: the system builds a project/generation/base-digest-bound candidate internally, performs the complete schema/reference/no-loss/coverage/render/playback/storage/command/policy validation, revalidates the live base and automatically commits the whole result as one global V2 history transaction or nothing. Success immediately shows the resulting animation/current project. There is no user-facing Preview, Apply, second confirmation or post-generation Cancel step. Undo restores the exact pre-commit project and Redo restores the exact successful result.
- Cancellation/race decision: the active-job Cancel control remains available while work is running. Cancellation accepted before the final compare-and-swap prevents publication and leaves authored content/history unchanged. After the atomic commit succeeds, the job is Done and cannot be relabelled Cancelled or partially rolled back by the job control; Undo/Redo is the correction path. Failure, timeout, stale base, validation error, project switch and accepted pre-commit cancellation remain exact no-mutation outcomes.
- Phase/proof consequence: Phases 4–6, cross-phase artifacts, acceptance stories, ownership tables, roadmap, architecture and proof language use an internal candidate plus automatic atomic commit. Phase 4 proves explicit-create auto-commit; Phase 5 proves explicit-edit auto-commit; Phase 6 proves the complete flow without a second approval gate. Negative proof covers plan-only no-op, ambiguity question, destructive-intent enforcement, active cancellation before commit, cancellation/commit race ordering, stale/failed validation no-op, one history entry, visible result, exact Undo/Redo and absence of Preview/Apply/post-generation Cancel controls.
- Lifecycle/publication: D-0091 remains published in GIT-072. Phase 2 remains Unauthorized/Not started. This D-0092 correction changes documentation/control-plane records only and is review-ready but uncommitted/unpublished; a later explicit publication instruction is required before Arthur may separately authorize one fresh Phase 2 Spec Executor. No live provider/API call, spend, implementation, deployment or Phase 2 start is included.

### D-0093 — Pause SPEC-0008 after completed Phase 1

- Date: 2026-09-20
- Status: Accepted planning/control-plane decision; no runtime or provider change
- Authority: Arthur explicitly accepted the prior pause decision and authorized its reconciliation onto current canonical main while requiring the task to stop before staging, commit, merge, push or publication.
- Stable-ID reconciliation: the accepted read-only planning package used D-0091 for this pause, but current canonical main already durably uses D-0091 for the Phase 2 web-research contract and D-0092 for the automatic-commit contract. Decision IDs are append-only, so this record preserves those current-main decisions as historical planning material and assigns the accepted pause the next free ID. Current main commit `42bfe1a2607a85a87d486e21d6e573b4e4084d5a` is the publication anchor for D-0092/GIT-073; its parent is GIT-072 `9b4b116d24b076b1c05d91a02fd7318ae5a44148`. Neither decision grants execution authority while this pause is active.
- Decision: Preserve SPEC-0008 Phase 1 exactly as accepted, Verified, published, integrated, recorded and cleaned up under D-0089/D-0090/GIT-070. Pause Phases 2–6 before implementation. Because those phases never produced an implementation for review, their accurate status is **Paused; Unauthorized; Not started; not rejected**.
- Architecture effect: the published provider-neutral reference-video → slicing → reconstruction sequence, including D-0091 research and D-0092 transaction mechanics, remains historical approved design intent but loses current dispatch readiness and is under reconsideration. Research into reusable editable motion/Blender-authored assets is unproven and does not supersede the spec, select a provider, authorize asset purchasing, or authorize implementation.
- Resumption gate: no later SPEC-0008 executor, live/paid provider use, asset purchase, or replacement architecture may begin until Arthur explicitly resumes AI Animator work. The Project Manager/Spec Architect must then refresh evidence and reconcile the post-Phase-1 architecture and acceptance proof before any phase receives separate authorization.
- Current priority consequence: select work outside the AI Animator through its own spec and authorization lifecycle. This pause changes no app/runtime/fixture/test/proof bytes, accepted Phase 1 evidence, provider account, deployment, Git history or remote state.

### D-0094 — Propose standalone local Animation Export as SPEC-0009

- Date: 2026-09-20
- Status: Proposed planning/control-plane decision; not Approved; no implementation phase authorized
- Authority: Arthur accepted the prior SPEC-0009 planning result and authorized its reconciliation onto current canonical main while preserving D-0091/D-0092 as history and leaving SPEC-0008 paused under D-0093.
- Stable-ID reconciliation: the accepted read-only proposal used D-0092, but current canonical main already durably assigns that ID to the automatic-commit planning contract. This decision therefore preserves the accepted proposal under the next free append-only ID. The copied SPEC-0009 bytes differ from accepted SHA-256 `a69f4bce4b0501b68415e28ab4685db7b39db799e9c30afecce7307e3ec9dc7b` only where decision references necessarily change from D-0091/D-0092 to D-0093/D-0094.
- Decision: Create [`SPEC-0009 — Animation Export`](specs/0009-animation-export.md) with exactly three separately gated phases: (1) Choose and Watch from Home Export and workspace File → Export using saved-animation cards and the actual selected-animation player; (2) Create and Save Video with faithful saved-content/audio/timing export, inherited non-editable FPS, 720p/1080p, truthful progress/cancel/failure and a macOS Finder location flow; and (3) data-driven social destination presets plus final whole-feature proof.
- Product boundary: V1 saves a validated local MP4 to the Mac. Destination entries prepare shape/resolution/container/file guidance and default to complete-animation contain framing; they never silently crop. V1 does not log in to, upload to, post to, or integrate directly with a social service. A future direct-integration spec is mandatory.
- Ownership boundary: export reads one immutable validated saved snapshot through the accepted project collection/V2 repository path and creates no project version/history/repository mutation. Unsaved workspace edits are never included implicitly. One canonical read-only evaluator must serve selected-animation playback and eventual export fidelity. Project-owned background is part of the saved animation; no export-only white-background control exists.
- AI/cost/privacy boundary: export makes no Terra/OpenAI/video-provider/social-network request, spends no AI credits, and does not alter AI Animator jobs, prompts, models, credit accounting, providers, accounts or deployment. SPEC-0008 Phases 2–6 remain paused/unauthorized/not started under D-0093; SPEC-0009 neither revives nor implements its Phase 6.
- Lifecycle: the proposal is decision-complete and review-ready but remains **Proposed**. Arthur must separately approve the whole spec and separately authorize Phase 1 from an exact synchronized canonical-main SHA. Phases 2 and 3 require full predecessor acceptance/publication/integration/synchronization/proof-preservation/cleanup and separate authorization. This reconciliation itself authorizes no programming.
- Task boundary: this Control Plane Architect task changes only canonical planning/control-plane/tree files. It changes no app/runtime/fixture/technical-test/proof/dependency/provider/account/deployment behavior and performs no stage, commit, merge, push or publication.

### D-0095 — Record publication of the SPEC-0008 pause and Proposed SPEC-0009

- Date: 2026-09-20
- Status: Accepted terminal publication record; no product approval or implementation authority
- Authority: Arthur explicitly authorized reconciliation and publication after accepting the SPEC-0009 planning result, while separately withholding programming permission.
- Publication: GIT-074 is exact commit `30f6f85e5c483c9df15946e7a451bf2a31374f2e`, parent `42bfe1a2607a85a87d486e21d6e573b4e4084d5a`, containing exactly the 15 reviewed planning/control-plane/tree paths. Canonical `main`, local `origin/main` and live GitHub `main` were verified clean and synchronized at `0/0` after the normal push.
- Integrity: the reconciled 47,740-byte/604-line SPEC-0009 file remains SHA-256 `3a4028deffe45c21223ac21732111c0cb1d614a6c84c3471477eb50a5a2dd0dc`. GIT-074 changes no app/runtime/test/fixture/proof/dependency/provider/account/deployment byte and spends no AI or provider credit.
- Lifecycle: D-0093 remains active; SPEC-0008 Phases 2–6 remain Paused/Unauthorized/Not started/not rejected. SPEC-0009 remains Proposed, not Approved, with no phase authorized. Whole-spec approval and a separate Phase 1 authorization from synchronized canonical main remain mandatory before a fresh Spec Executor app copy may begin.
- Closeout: GIT-075 is the records-only follow-up that corrects stale prepublication language. It does not change the product plan, the spec hash, or any runtime state.

### D-0096 — Accept and technically verify SPEC-0009 Phase 1 Choose and Watch

- Date: 2026-09-20
- Status: Accepted implementation; technical proof PASS/VALID; Control Plane Architect propagation complete; publication/integration pending
- Authority: Arthur explicitly authorized SPEC-0009 Phase 1 in one fresh app copy, directed the visible corrections in that same copy, reviewed the corrected result, called it acceptable, and instructed the Project Manager to record the control plane and integrate/publish it. The stopped Spec Executor transferred the accepted worktree sequentially to the Control Plane Architect with an empty index.
- Accepted identity: exact unchanged base/HEAD `37cdb7203286c2ea333a3766ba2e81bc06830200`; exact 12-path technical dirty allowlist recorded below; immutable 8,688-byte manifest `output/spec-0009/phase-1/proof-manifest.json` at SHA-256 `40af1b220958a5d99094a07c763194b4cb6e2338ee6e14f0b2674a671564c2fb`; aggregate accepted-source digest `982cd78ad9342bc59e44de45a6f7972c276e3d9255cc29a5ec772a0b4047e745`. Fresh unchanged-manifest validation returned `VALID` with 16 assertions and rejected mutations of status, base, dirty set, source, screenshot, oracle, correction-browser and check data.
- Accepted outcome: Home **Export** and workspace **File → Export** now open one Phase 1 saved-animation chooser. A user explicitly selects a ready saved project, activates **Use this animation**, and watches the immutable revalidated saved snapshot with Play/Pause, saved-FPS timing, frame scrubber and **Change animation**. Workspace entry truthfully excludes unsaved edits. Empty, unavailable, failed and source-changed states fail closed without project/repository/history mutation. Encoding, Finder saving and destination presets remain absent.
- Accepted review corrections: Save As no longer calls unsupported `window.prompt`; it uses an accessible focused in-app dialog with safe cancel, bounded validation and failure retention. The saved-raster evaluator now follows the accepted workspace's owner/hold/tween rules, centered 4.6× authoring-world transform, rounded tween origins and accumulator playback. Terra conversation now requests a purpose-specific missing-key error so a greeting never leaks the Generate Frames error; no Terra model, instructions, reasoning mapping, job/status UI, gradient, transcript, animation mutation or fallback behavior changed.
- Scope reconciliation: the original Phase 1 entry text excluded AI files, but Arthur later explicitly required the narrow Terra regression repair while prohibiting broader AI changes. Latest owner authority therefore admits only `src/lib/openai/client.ts` and `src/lib/openai/generateAiAnimatorReply.ts` for that purpose. It does not reopen SPEC-0008 or authorize later AI/provider work. The Save As correction is likewise an explicitly accepted protected-regression repair, not export scope expansion.
- Verification: the Phase 1 oracle passed 25 duration/owner/hold/tween/raster-transform/empty/stale-source assertions; the real-app correction flow passed 23 Save As, UI-authored raster/hold/keyframe, chooser/player, saved-FPS traversal and deterministic Terra-route assertions with zero external requests and zero console errors. TypeScript, focused lint and production build passed; focused lint retains only three pre-existing unused-variable warnings in `DrawingWorkspace.tsx`. After Oliver installed a server-only local key, one separately observed live Terra smoke request returned `Hello — connection confirmed.` with zero browser console errors. The key value is ignored, mode-600 local data and is not recorded or publishable.
- Boundaries and limitations: Phase 1 proves choosing and watching; it does not create MP4/WebM bytes, open Finder, show encode progress, create destination/social presets, crop/reframe, upload/post, spend video-provider credits or authorize Phase 2. The immutable executor manifest records its deterministic provider double and zero paid calls; the later one-message live smoke is separate acceptance evidence and does not rewrite that historical manifest. Physical-device/native-GPU behavior and Phase 2's project-owned background/encoder fidelity remain unproven.
- Exact accepted technical paths: `app/page.tsx`; `scripts/spec0009-export/correctionBrowserProof.ts`; `scripts/spec0009-export/phase1Oracle.ts`; `scripts/spec0009-export/recordPhase1Proof.ts`; `scripts/spec0009-export/validatePhase1Proof.ts`; `src/components/export/AnimationExportFlow.tsx`; `src/components/workspace/AnimationWorkspace.tsx`; `src/components/workspace/DrawingTopBar.tsx`; `src/components/workspace/DrawingWorkspace.tsx`; `src/lib/export/exportPhase1.ts`; `src/lib/openai/client.ts`; `src/lib/openai/generateAiAnimatorReply.ts`.
- Lifecycle: this propagation task changes only canonical records/tree bytes and keeps every accepted technical/proof byte immutable. It stages, commits, integrates and pushes nothing. Although Arthur expressed publication intent in the acceptance message, the repository's sequential lifecycle requires this Control Plane Architect PM Review Packet to finish first and a later explicit publication turn to revalidate/stage the exact reviewed paths. Phase 2 remains Unauthorized/Not started.

### D-0097 — Close SPEC-0009 Phase 1 and authorize Phase 2 Create and Save Video

- Date: 2026-09-20
- Status: Accepted
- Authority: Arthur explicitly confirmed that Phase 1 publication/integration was approved and that Phase 2 must start without another approval gate. He approved the concrete local implementation direction: persist the project's background with white as the default, create video locally on the Mac/browser, target H.264 video plus AAC audio in MP4, and use Mediabunny for assembly. He explicitly excluded AI calls, an online video company and per-export charges.
- Phase 1 publication: GIT-076 is exact commit `548063b9247708106c7c8c8a11978563d07d6597`, parent GIT-075 `37cdb7203286c2ea333a3766ba2e81bc06830200`, with the accepted 12 technical paths and 14 reviewed control-plane/tree paths. Canonical `main`, local `origin/main` and live GitHub `main` were verified equal and clean at `0/0`.
- Phase 1 preservation/cleanup: the complete seven-file/159,644-byte proof is preserved under `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-076-spec0009-phase1-548063b/` at aggregate SHA-256 `7137a1de4ad8a0be73c17b45699592a98ed876b4858e18df6a36cf8d4b509fbb`; the immutable manifest remains SHA-256 `40af1b220958a5d99094a07c763194b4cb6e2338ee6e14f0b2674a671564c2fb` and all 12 source hashes matched the published commit. Port 57200 is closed; the obsolete executor review worktree and unused publication branch are removed. `.env.local` was intentionally neither backed up nor published.
- Phase 2 decision: Phase 2 is **Authorized; Not started**. It must use one fresh dedicated Spec Executor worktree from the synchronized GIT-077 authorization record, begin in Plan mode, trace and revalidate current runtime mechanics, and remain inside SPEC-0009 Phase 2's runtime/fixture/technical-test/proof boundary. The project-owned background contract defaults new/absent background state to white and must round-trip through Save/Open; export must read that saved truth rather than inject an export-only matte. The file target remains local MP4 with H.264 video and AAC audio when authored audio exists; Mediabunny is the approved assembly library/mechanism subject to exact version/license/runtime verification in the phase proof.
- Boundaries: no AI/Terra/OpenAI/video-provider call, search, online encoding, external upload, per-export charge, direct social login/upload/posting, Phase 3 catalog work, deployment, automatic Git publication or weakening of fidelity/cancel/failure/partial-file/post-write inspection gates is authorized. A separately authorized live/provider test is not needed for this local-only phase. Phase 3 remains Unauthorized/Not started.
- Publication: GIT-077 is the records-only terminal closeout/activation publication. It changes no runtime, fixture, technical test, dependency, credential, provider/account or deployment byte. After clean synchronization, Phase 2 is dispatch-ready without further product approval.

### D-0098 — Accept and technically verify corrected SPEC-0009 Phase 2 Create and Save Video

- Date: 2026-09-20
- Status: Accepted implementation; technical proof PASS/VALID; Control Plane Architect propagation complete; publication/integration pending
- Authority: Arthur rejected the first Phase 2 app copy after its interaction layer blocked ordinary Home controls, authorized one fresh replacement from the working canonical app, directed two narrow review corrections for squeezed export geometry and Terra environment restoration, then reviewed `http://127.0.0.1:57470/`, called the corrected result acceptable, and authorized control-plane recording plus later publication/integration. The correction Spec Executor completely stopped before sequential exclusive Control Plane Architect ownership.
- Accepted identity: detached unchanged base/HEAD `b45921262b57902ddbaea9519a03f7aa7289621c`; empty index; exact 15 technical dirty paths; ignored `.env.local`; review server PID 59172 with exact worktree cwd and port 57470. Immutable 11,229-byte manifest `output/spec-0009/phase-2/proof-manifest.json` has SHA-256 `4f141b18e4601ef1faca2cd4f2d325bd11cc3f06303dac09f2ac1f07d838c341`; aggregate accepted-source digest is `354eaf6f0d924168a2e36b0e4ec5ed240e41e89a1b3dd577b9f85af9b9fdf733`.
- Accepted outcome: project-owned solid background persists with new/absent state defaulting to white. One shared read-only renderer resolves the saved scene into a uniform centered contain fit, keeping asymmetric art shape-preserved and uncropped in Watch and final video. A user chooses 720p or 1080p, names the file, chooses a Finder location, sees real progress, may cancel, and receives a locally written H.264 MP4 with AAC when authored audio exists. Distinct prepare/encode/write/inspect failures, zero-byte partial cleanup and post-write media validation fail closed. Mediabunny is pinned exactly to `1.58.1`, licensed MPL-2.0.
- Technical proof: fresh pre-propagation manifest validation returned VALID with 20 assertions and rejected 13 material mutations across status, base, dirty set, sources, spec, dependency, artifact, checks, outputs, network, credits and Finder disclosure. The Phase 2 oracle passed 38 assertions; TypeScript and diff checks passed. Real local write/read inspection passed at 1280×720 and 1920×1080, 12 FPS, one H.264 frame per tier, with non-zero files. Shape ratios stayed within raster tolerance (`1.15385` authored; approximately `1.151–1.153` preview/decoded output) and rejected the old squeezed result. Home/navigation, Save As/Open, Drawing, layers, onion and playback regressions passed.
- AI/cost/privacy boundary: export makes zero OpenAI, Terra, search, video-provider, social, analytics or other network call; consumes zero credits; and changes no project/history/repository state. All Terra source bytes are unchanged. Deterministic regression used a no-cost double; exactly one separate short Low-reasoning live Terra smoke passed after restoring the ignored local environment. No credential value was read into proof, hashed, recorded or made publishable.
- Exact accepted technical paths: `package-lock.json`; `package.json`; `scripts/spec0009-export/phase2Oracle.ts`; `scripts/spec0009-export/recordPhase2Proof.ts`; `scripts/spec0009-export/validatePhase2Proof.ts`; `src/components/export/AnimationExportFlow.tsx`; `src/components/workspace/DrawingCanvas.tsx`; `src/components/workspace/DrawingWorkspace.tsx`; `src/lib/animation/unifiedAnimationContractV2.ts`; `src/lib/animation/unifiedAnimationMigrationV2.ts`; `src/lib/animation/unifiedWorkspaceFactoryV2.ts`; `src/lib/export/exportAudio.ts`; `src/lib/export/exportContracts.ts`; `src/lib/export/exportRenderer.ts`; `src/lib/export/exportVideo.ts`.
- Honest limits: automated file proof uses the real browser encoder with an OPFS FileSystemFileHandle seam; the native Finder dialog/user-chosen path remains human-review evidence rather than a bound automated artifact. Final decoded receipts are one-frame raster/no-audio fixtures at both tiers. AAC, mixed text/symbol/effect rendering and long-duration paths are implemented/contract-tested but are not claimed as visually human-accepted by those final receipts. Compact physical-device/native-GPU behavior remains unproven.
- Lifecycle: this propagation changes only the canonical 14 control-plane/tree paths and keeps all accepted runtime, fixture and technical-test bytes immutable. A later explicitly authorized publication task must reverify the unchanged 29-path combined scope, stage only those paths, commit/integrate/push, verify clean synchronization, preserve proof, stop the exact review server and complete D-0054 cleanup. Until then the server/worktree/proof remain preserved. Phase 3 remains Unauthorized/Not started; this decision adds no destination preset, direct upload, deployment or SPEC-0008 authority.

### D-0099 — Close SPEC-0009 Phase 2 publication and cleanup

- Date: 2026-09-20
- Status: Accepted; published; integrated; synchronized; proof-preserved; cleaned up
- Decision: record exact 29-path GIT-078 commit `682fd9732b9cc30e545b3b709ced62ffd94edc39`, parent `b45921262b57902ddbaea9519a03f7aa7289621c`, and close SPEC-0009 Phase 2 after canonical `main`, `origin/main` and live GitHub `main` matched at clean `0/0`.
- Preservation: 12 required proof files/335,553 bytes are preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-078-spec0009-phase2-682fd97/`, aggregate SHA-256 `d2a13e835cbc19fce32620197a801bea942ffdd15beaf8d2d900f2e6a13e64bb`. `.env.local` and all secrets are excluded.
- Cleanup: PID 59172 is stopped, port 57470 is closed, `/Users/arthurcarlin/.codex/worktrees/spec0009-phase2-rebuild/stick-animation-app` is removed/unregistered and local branch `codex/spec0009-phase2-publication` is deleted. Canonical main, remote refs, recovery evidence and the Project Manager worktree remain.
- Consequence: Phase 2 is fully closed. Phase 3 remains Unauthorized/Not started and requires Arthur's separate authorization.

### D-0100 — Accept and technically verify SPEC-0009 Phase 3 Social Destinations and Final Testing

- Date: 2026-09-21
- Status: Accepted implementation; technical proof PASS/VALID; Control Plane Architect propagation complete; publication/integration pending
- Authority: after Phase 2 was fully closed, Arthur explicitly authorized Phase 3 in one fresh app copy. Arthur reviewed the completed Phase 3 app at `http://127.0.0.1:57500/`, directed one server-environment correction when Terra reported the missing API key, accepted the corrected result, confirmed that the destination controls should remain useful local shape-preparation choices with **Original** as the unchanged download, and separately authorized control-plane propagation plus commit/integration/push. The Phase 3 Spec Executor is completely stopped/interrupted before sequential exclusive Control Plane Architect ownership.
- Accepted identity: detached unchanged base/HEAD `53d825490c08bce620784f0213b4574792732f22`; empty index; exact 10 technical dirty paths. Immutable 34,983-byte manifest `output/spec-0009/phase-3/proof-manifest.json` has SHA-256 `0d9b48f584f89d54604284c03713037402e2d68decde5c3b0d0a844cd8065277`; aggregate source digest is `abb0ba8538454eea3af8cb2761b4225a1c3ed294de21f1097ea9c6b58b21e987`. The ignored mode-600 `.env.local` is environment-only and excluded from proof/publication.
- Accepted outcome: a versioned local catalog supplies Original, YouTube, YouTube Shorts, TikTok, Instagram Reels/Stories/Feed, Facebook Reels/Feed, Discord, Snapchat, X, Reddit and Custom / Other. Original keeps the saved stage shape. Other choices prepare the documented 16:9, 9:16 or 4:5 local canvas; Custom / Other supports 16:9, 9:16, 1:1, 4:5 and even dimensions from 256 through 1920 pixels per side. Every path uses complete-animation centered contain framing, disclosed project-background padding and no crop. The UI uses neutral text fallbacks, dated guidance and explicit non-guarantee language. It does not sign in, upload or post.
- Technical proof: fresh Control Plane Architect validation returned VALID with 61 assertions and rejected 53 material mutations. Bound evidence reports 354 Phase 3 contract assertions, 318 real-Chrome assertions, 26 preset/tier geometry receipts, all 14 destination cards at both tiers, six written/decoded H.264 MP4 families, valid/invalid custom cases, truthful Finder cancellation/permission failure, and unchanged project/credit storage. A five-minute 720p/24 FPS/7,200-frame eight-layer audio fixture completed in 34.15 seconds at 207,409,943-byte peak heap and 213 ms maximum long task. A sixty-second 1080p/24 FPS/1,440-frame audio fixture completed in 9.36 seconds with first frame in 240.02 ms. Cancellation acknowledged in 30.97 ms, terminated in 54.64 ms and left zero bytes. Production build, TypeScript and focused exact-path lint passed; full lint remains the inherited five-error/81-warning untouched baseline.
- AI/cost/privacy boundary: export made zero external/network/AI/provider/paid calls, zero credit changes, zero project/history/repository writes, zero uploads and zero deployments. Terra source and accepted Phase 1 behavior are unchanged; deterministic regression used a no-cost double. After the ignored server-only key was restored, exactly one separate Low-reasoning live smoke returned `Terra connected.` No key value is recorded, hashed, logged, staged or publishable.
- Exact accepted technical paths: `scripts/spec0009-export/phase3BrowserProof.ts`; `scripts/spec0009-export/phase3Oracle.ts`; `scripts/spec0009-export/phase3PerformanceProof.ts`; `scripts/spec0009-export/recordPhase3Proof.ts`; `scripts/spec0009-export/validatePhase3Proof.ts`; `src/components/export/AnimationExportFlow.tsx`; `src/lib/export/exportAudio.ts`; `src/lib/export/exportContracts.ts`; `src/lib/export/exportDestinationCatalog.ts`; `src/lib/export/exportVideo.ts`.
- Honest limits: automated saving used the real Chrome encoder through an OPFS FileSystemFileHandle seam; the native macOS Finder dialog/final user-selected path remains human-review evidence. Current Chrome on this Mac is proven, not every browser/device or social-platform ingestion. Catalog facts are dated `2026-09-20` guidance, not posting guarantees. Direct upload/posting, account connection, crop/fill, provider work and deployment remain absent.
- Lifecycle: this propagation changes only the canonical 14 control-plane/tree paths and keeps all accepted runtime, fixture and technical-test bytes immutable. Arthur's separate publication authority permits a later publication step to reverify the exact 24-path combined scope, stage only those paths, commit/integrate/push, verify clean synchronization, preserve proof, stop the exact review server and complete D-0054 cleanup. Until that succeeds, SPEC-0009 Phase 3 is accepted/technically Verified with publication pending.

### D-0101 — Record GIT-079 publication and close SPEC-0009 Phase 3

- Date: 2026-09-21
- Status: Accepted, Verified, published, integrated, synchronized, proof-preserved and cleaned up
- Authority: Arthur accepted the corrected Phase 3 app, separately authorized control-plane propagation and publication/integration, and triggered that already-specified work by saying `start autopilot`.
- Publication: GIT-079 is exact 24-path commit `555d60b48ec97e066b1110d40638927d68c8d34b`, parent `53d825490c08bce620784f0213b4574792732f22`, message `Complete SPEC-0009 Phase 3 export destinations`. It contains exactly the accepted 10 technical paths plus 14 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded to it, pushed normally, and canonical `main`, local `origin/main` and live GitHub `main` were verified equal at clean `0/0` before this terminal records-only closeout.
- Proof preservation: the complete accepted Phase 3 output is preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-079-spec0009-phase3-555d60b/`: 19 files, 5,202,329 bytes, aggregate framed inventory SHA-256 `6cba2ea9c3730e7cd874e3db1b16cf0fc0a7e9123942c77b0708a69cce970985`, with matching source/destination inventories and an empty checksum comparison. The technical manifest remains SHA-256 `0d9b48f584f89d54604284c03713037402e2d68decde5c3b0d0a844cd8065277`; `.env.local` and all secrets are excluded.
- Cleanup: exact review PID 79009 was stopped and port 57500 verified closed. The obsolete `/Users/arthurcarlin/.codex/worktrees/8c9e/stick-animation-app` worktree was removed/unregistered and merged local branch `codex/spec0009-phase3-publication` deleted. Canonical main, the active Project Manager worktree, remote refs and recovery material remain.
- Consequence: SPEC-0009 and all three phases are fully closed. Destination choices remain local file-preparation controls only; no direct social login/upload/posting, deployment, provider/paid action or SPEC-0008 resumption is included. The terminal records-only commit containing D-0101 changes no runtime, fixture, technical-test, proof, package, dependency or credential byte; its SHA is read from Git after publication rather than embedded recursively.

### D-0102 — Propose SPEC-0010 Project Safety and Recovery as three separately gated phases

- Date: 2026-09-21
- Status: Proposed planning/control-plane decision; no implementation phase authorized
- Authority: Arthur selected Project Safety and Recovery as the next feature, approved creation of a dedicated SPEC-0010 architect, and asked for a decision-complete three-phase plan before Phase 1 execution begins.
- Decision: create [`SPEC-0010 — Project Safety and Recovery`](specs/0010-project-safety-and-recovery.md) with exactly three separately gated phases: (1) **File → Save and Exit** through the existing canonical official Save path, reaching Home only after a current-generation success; (2) one separate, bounded, local latest-workspace emergency draft that never becomes or silently replaces an official project; and (3) startup **Unsaved work found** with **Recover Work** / **Discard Draft**, isolated validated recovery, and final safety/regression proof.
- Product meaning: the recovery draft is an emergency local backup, not ordinary Save. Official Save, Save As and Save and Exit remain explicit V2 repository operations. Recovery-only actions write no official project head/version/history and create no project-list entry. A recovered candidate changes official state only after the user explicitly saves.
- Safety contract: meaningful committed edits schedule bounded debounce/latest-sequence writes into a separate recovery database; transient gestures/navigation do not. The preceding valid draft survives failed replacement. One logical draft, session/workspace CAS, 128 MiB candidate ceiling, validation/readback, quota/corruption handling and exact-generation clearing are mandatory. Async unload completion is best-effort only; the product may promise only the last completed verified draft.
- Boundary: no My Projects redesign, AI Dashboard/Assistant/Finalizer, AI Animator resumption, cloud sync, account, network, analytics, dependency, provider, paid call, deployment, official project schema/store change, or background network activity. D-0093 continues to pause SPEC-0008 Phases 2–6.
- Lifecycle: SPEC-0010 is Proposed and planning-complete from clean canonical planning base `092a96c6a17db1bbb307d21128bed84377eba3e7`. This package changes planning/control-plane/tree records only. It creates no executor/app copy/server/recovery database, changes no runtime/test/fixture/dependency/credential/Git ref, and authorizes no phase. Arthur/PM approval plus separate explicit Phase 1 authorization remain required.

### D-0103 — Approve SPEC-0010 and authorize Phase 1 Save and Exit

- Date: 2026-09-21
- Status: Accepted by Arthur; whole-spec approval and Phase 1-only authorization
- Authority: after reviewing the completed three-phase plan, Arthur explicitly authorized recording/publication and instructed the Project Manager to start a fresh Phase 1 executor/app copy without another approval gate.
- Planning publication: GIT-080 is exact 13-path commit `cb80f2baf92f812c97ce758067f6ea28b3c6c187`, parent `092a96c6a17db1bbb307d21128bed84377eba3e7`, message `Define SPEC-0010 project safety and recovery`. Canonical `main` and `origin/main` were synchronized by normal fast-forward/push; no runtime byte changed.
- Authorization: SPEC-0010 is Approved. Phase 1 **File → Save and Exit** is Authorized/Not started and may begin in one fresh dedicated Plan-mode Spec Executor from the clean synchronized activation-record SHA. It may touch only its bounded runtime/test/proof allowlist, reuse the canonical Save transaction, navigate Home only after a current-generation success, and stop with an Implementation Review Packet for Arthur.
- Boundaries: Phase 1 must not begin Phase 2 recovery storage, Phase 3 startup recovery, AI/Terra work, export changes, provider/network/paid activity, deployment, dependency changes, control-plane mutation, Git staging/commit/push, or another phase. Phases 2–3 remain Unauthorized/Not started.

### D-0104 — Accept and technically verify SPEC-0010 Phase 1 Save and Exit

- Date: 2026-09-21
- Status: Accepted by Arthur; technically Verified; control-plane propagation and later publication/integration/cleanup explicitly authorized
- Authority: Arthur reviewed the corrected Phase 1 app copy, called it acceptable, and explicitly authorized recording the control plane, committing, integrating into the real app, pushing to GitHub, and final cleanup.
- Accepted result: exact ten tracked technical paths from unchanged base/HEAD `db0be15decae427e3ca0d696d945a650f363a4aa`, empty index. The 8,302-byte immutable PASS/VALID manifest `output/spec-0010/phase-1/proof-manifest.json` has SHA-256 `b400293271c1ae0cf78007de5024eb616516a6219d3a9965e03a16d8f7890f19` and source digest `5ff9239626dd05e456e038b821171f093bbc6120e3810d1dff651a7793ba0900`. Fresh strict pre-propagation validation passed 16 assertions and rejected ten material mutations.
- Behavior: **Save and Exit** is the final File action, reuses one canonical official V2 Save, returns Home only when the write succeeds and covers the current generation, stays editable on failure/stale success, and suppresses duplicate write/navigation. No recovery store/startup prompt exists yet.
- Proof: 18 oracle, 17 contract and 29 real-Chrome assertions across six flows passed with TypeScript, focused lint, unchanged full-lint baseline, focused production build, diff/scope/index, keyboard/compact/reduced-motion and protected workspace regressions. Deterministic proof recorded zero external/provider/AI/paid calls and zero credit changes.
- Terra correction: the review worktree's ignored `.env.local` was restored byte-identically from canonical private configuration without reading, exposing, tracking or publishing the secret. Exactly one authorized live `gpt-5.6-terra` greeting returned `Hi! What would you like to make?`, used 407 tokens and an estimated $0.001214. Tracked Terra/Thinking source bytes remained unchanged.
- Boundary: propagation must not change the accepted ten technical bytes. `.env.local` remains ignored and excluded from proof/publication. Phase 2 recovery storage and Phase 3 startup recovery remain Unauthorized/Not started. Publication/integration, proof preservation, port-57610 shutdown and D-0054 cleanup occur only in the later sequential publication step already authorized by Arthur.

### D-0105 — Close SPEC-0010 Phase 1 and authorize Phase 2 Emergency Recovery Draft

- Date: 2026-09-21
- Status: Accepted; published; integrated; synchronized; proof-preserved; cleaned up; Phase 2 separately authorized
- Phase 1 publication: GIT-081 is exact commit `44cdafc534d7ef096cd2e283532a956c4eaa91f6`, parent `db0be15decae427e3ca0d696d945a650f363a4aa`, message `Complete SPEC-0010 Phase 1 Save and Exit`. Canonical `main`, `origin/main` and live GitHub `main` were synchronized after the normal push.
- Preservation: the complete fifteen-file/451,235-byte proof is preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-081-spec0010-phase1-44cdafc/`; the deterministic sorted checksum-inventory SHA-256 is `ead373f36b683047df9e011f74708e8bb694b4317ca204c2e9b9a3e6039f20c4`. `.env.local` and secrets are excluded.
- Cleanup: port 57610 is closed; the obsolete Phase 1 review worktree and its unused publication branch are absent. Canonical `main`, remote refs, recovery evidence and the Project Manager worktree remain.
- Phase 2 authority: after Phase 1 closed, Arthur explicitly authorized a fresh SPEC-0010 Phase 2 executor/app copy to implement the spec's one latest emergency recovery draft. Phase 3 remained Unauthorized/Not started.

### D-0106 — Accept and technically verify corrected SPEC-0010 Phase 2 Emergency Recovery Draft

- Date: 2026-09-21
- Status: Accepted by Arthur; technically Verified; control-plane propagation and publication/integration/proof-preservation/cleanup explicitly authorized
- Authority: Arthur authorized Phase 2 in a fresh app copy, reviewed the result, identified the one-click Save-and-Exit and review-environment Terra corrections, accepted the corrected copy as a pass, and explicitly authorized integration into the real app, canonical recording, commit and GitHub push. The Spec Executor completely stopped before exclusive Control Plane Architect ownership.
- Accepted identity: exact eleven tracked technical paths from unchanged base/HEAD `44cdafc534d7ef096cd2e283532a956c4eaa91f6`, empty index. The 8,685-byte immutable PASS/VALID manifest `output/spec-0010/phase-2/proof-manifest.json` has SHA-256 `444cf4695574a6d7653a628e4a3a2035db3305054d2a76905e73cd3fa0ccc2e2` and source digest `b11323e481074e18683bd838f48df0f664ca83365874716af9e39aeba898318d`. Fresh validation passed 19 assertions and rejected thirteen mutation classes.
- Accepted outcome: committed meaningful edits schedule one bounded local emergency draft after a 750 ms debounce/three-second maximum wait. A separate recovery-only IndexedDB owner stages, hydrates, validates and atomically publishes one content-addressed V2 candidate, preserves the prior valid draft on fault, rejects foreign/newer writers and never creates an official project/list/export entry. Transient navigation, tool selection, playback, hover, menus and AI-chat text do not schedule recovery. Save and Save As clear only an exact covered draft.
- Correction: one activation of **Save and Exit** performs exactly one official Save, cancels/awaits pending recovery work, publishes and clears the exact covered recovery generation when needed, then returns Home automatically. A failed official Save or newer edit remains editable. The review `.env.local` was restored as ignored environment-only data; no tracked Terra source, prompt, model, route, Thinking wording, gradient, timing or animation changed.
- Proof: 27 Phase 2 Chrome assertions across eight scenarios, 27 oracle assertions, 18 Phase 2 contract assertions, 17 inherited Phase 1 contract assertions and 29 inherited Phase 1 Chrome assertions passed with TypeScript, focused lint, production build, diff/scope/index and protected workspace regressions. Deterministic proof recorded zero external/provider/AI/paid calls and zero credit changes. Exactly one separately authorized live Terra POST returned naturally with zero console errors.
- Exact accepted technical paths: `scripts/spec0010-project-safety/phase2BrowserProof.ts`; `phase2Contract.ts`; `phase2GateProof.ts`; `phase2Oracle.ts`; `recordPhase2Proof.ts`; `validatePhase2Proof.ts`; `src/components/workspace/DrawingTopBar.tsx`; `src/components/workspace/DrawingWorkspace.tsx`; `src/lib/animation/projectRecoveryContractV1.ts`; `src/lib/animation/projectRecoveryStorageV1.ts`; `src/lib/animation/unifiedProjectStorageV2.ts`.
- Boundary: propagation must not change the accepted eleven technical bytes. `.env.local` remains ignored and excluded from publication. Phase 3 startup recovery remains Unauthorized/Not started until Phase 2 is published/integrated/synchronized/proof-preserved/cleaned up and Arthur separately authorizes it.

### D-0107 — Record GIT-082 publication and close SPEC-0010 Phase 2

- Date: 2026-09-21
- Status: Accepted; published; integrated; synchronized; proof-preserved; cleaned up
- Publication: GIT-082 is exact 24-path commit `7e7063eaab3c486278355559c490611e3ca590ea`, parent GIT-081 `44cdafc534d7ef096cd2e283532a956c4eaa91f6`, message `Implement SPEC-0010 Phase 2 recovery draft`: the exact eleven accepted technical paths plus thirteen reviewed control-plane/tree paths. Canonical `main`, local `origin/main` and live GitHub `main` matched after the normal push.
- Preservation: twelve proof files/127,498 bytes are preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-082-spec0010-phase2-7e7063e/`; source and destination directories were byte-identical and share relative checksum-inventory SHA-256 `b5717d6e803df1b2d96741128fd5428b6e6bbd95debc8494841afb375ef84ee7`. `.env.local` and secrets are excluded.
- Cleanup: PID 99243 was stopped, port 57620 is closed, the obsolete `/c4b5/` accepted review worktree is removed/unregistered, and merged local branch `codex/spec0010-phase2-publication` is deleted. Canonical main, remote refs, recovery evidence and the active Project Manager worktree remain.
- Consequence: Phase 2 is fully closed. Phase 3 **Recover Work / Discard Draft and final safety proof** has its predecessor gate satisfied and is ready for Arthur's separate authorization, but remains Unauthorized/Not started. This terminal records-only follow-up changes no runtime, fixture, technical-test, proof, dependency, credential, AI, export or recovery behavior.

### D-0108 — Accept and technically verify SPEC-0010 Phase 3 Recover Work / Discard Draft

- Date: 2026-09-21
- Status: Accepted implementation; technical proof PASS/VALID; Control Plane Architect propagation complete; publication/integration pending
- Authority: Arthur separately authorized Phase 3, reviewed the completed app, performed a real computer restart, identified only the recurring ignored server-environment gap, accepted the corrected result as done and explicitly authorized SPEC-0010 closure, control-plane recording, commit/integration/push, proof preservation, cleanup and archival of obsolete SPEC-0009/SPEC-0010 tasks while preserving the SPEC-0008 architect.
- Accepted identity: detached unchanged base/HEAD `337433b37716fdd71ecc2a9d0239e239519ef655`; empty index; exact twelve technical dirty paths; ignored `.env.local`; review server PID 4973 at port 57630. Rebound immutable 9,898-byte manifest `output/spec-0010/phase-3/proof-manifest.json` has SHA-256 `5dc275dc6d30da693ab14e861d7f12728038da0f6b3b1f30a66252880fdd23f5`; aggregate accepted-source digest is `48ec146163f03d21015771597b1d31fd043f6aac35cf2c4f9c5abca37a548d60`.
- Accepted outcome: startup resolves the separate recovery owner before Home/New/Open. Valid recovery shows **Unsaved work found** with project/time; click-time revalidation and exact-generation compare-and-swap allow one winning tab. Matching source revision/digest retains identity. Missing, changed, newer, corrupt or unreadable official sources mount as a new revision-zero **Recovered copy**, preventing overwrite. Recover writes zero official bytes; exact Save/Save As/Save and Exit or confirmed Discard performs verified cleanup. Invalid recovery disables Recover but remains discardable; deletion failure permits truthful Home continuation without falsely removing the draft.
- Technical proof: fresh validation passed 21 assertions and rejected fourteen mutation classes. Bound proof passed 45 oracle assertions, 27 deterministic contract assertions and 55 real-Chromium assertions across nine scenarios, plus inherited Phase 1 browser and Phase 1/2 contract/oracle regressions, focused lint/build, exact scope/index/diff, keyboard/compact/reduced-motion, startup/fault/concurrency and zero deterministic external/provider/AI/paid requests. Startup measured 1,017.37 ms and 116,170,679 JS heap bytes. The full repository build retains only the inherited out-of-scope developer AI-cost `PageProps` failure; the Home/workspace production route passes network-denied.
- AI/cost/privacy boundary: Phase 3 changes no AI/Terra/model/prompt/reasoning/gradient/provider/credential code. After the real restart, the ignored `.env.local` was restored byte-identically from canonical main without exposing or tracking its secret. Exactly one separate Low `gpt-5.6-terra` greeting returned naturally in 2,969 ms using 346 input/42 output/388 total tokens at estimated `$0.001196`. Deterministic Phase 3 proof itself made zero AI/provider/paid calls.
- Exact accepted technical paths: `app/page.tsx`; `scripts/spec0010-project-safety/phase3BrowserProof.ts`; `phase3Contract.ts`; `phase3GateProof.ts`; `phase3Oracle.ts`; `recordPhase3Proof.ts`; `validatePhase3Proof.ts`; `src/components/recovery/ProjectRecoveryPrompt.tsx`; `src/components/workspace/AnimationWorkspace.tsx`; `src/components/workspace/DrawingWorkspace.tsx`; `src/lib/animation/projectRecoveryStorageV1.ts`; `src/lib/animation/unifiedWorkspaceBootstrap.ts`.
- Honest limits: only the last completed, verified local transaction can recover. Final in-flight milliseconds, killed pre-commit work, manually cleared/evicted site data, private/profile/device/disk loss and damage outside the origin remain unrecoverable. Full-build developer-page debt remains out of Phase 3 scope.
- Lifecycle: propagation changes only canonical control-plane/tree paths and none of the accepted twelve technical bytes. Arthur's explicit publication authority permits the later stage/commit/fast-forward/push/proof-preservation/server-shutdown/worktree-and-branch cleanup step without another approval request. Until that succeeds, the accepted review worktree remains preserved.

### D-0109 — Record GIT-083 publication and close SPEC-0010 Phase 3

- Date: 2026-09-21
- Status: Accepted; Verified; published; integrated; synchronized; proof-preserved; cleaned up
- Publication: GIT-083 is exact 25-path commit `da348337d9329044937b2d27cb76498b743e778f`, parent `337433b37716fdd71ecc2a9d0239e239519ef655`, message `Complete SPEC-0010 Phase 3 recovery startup`: the exact twelve accepted technical paths plus thirteen reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded normally; canonical `main`, `origin/main` and live GitHub `main` matched at GIT-083 after the normal push.
- Preservation: thirteen proof files/794,137 bytes are preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-083-spec0010-phase3-da34833/`; source and destination are byte-identical and share relative checksum-inventory SHA-256 `b1c42860225724bf29a4cc02aea45ad6315f73da66052d652a8c5b2a1ccd8ab5`. `.env.local` and all secrets are excluded.
- Cleanup: PID 4973 is stopped and port 57630 is closed. After this terminal record is published, the obsolete `/Users/arthurcarlin/.codex/worktrees/84a2/stick-animation-app` review worktree is removed/unregistered and merged local branch `codex/spec0010-phase3-publication` is deleted. Canonical main, remote refs, recovery evidence and the active Project Manager worktree remain.
- Consequence: SPEC-0010 and all three phases are fully closed. The next task is Arthur's choice of a new non-AI feature/spec. SPEC-0008 Phases 2–6 remain paused under D-0093; this closeout authorizes no AI/provider/asset/deployment work.

### D-0110 — Propose SPEC-0011 My Projects Library, Movie Viewer, and Shared Project Management

- Date: 2026-09-21
- Status: Accepted planning decision; proposal only; all implementation phases Unauthorized/Not started
- Authority: Arthur explicitly requested a new dedicated Spec Architect planning task for the Home project library, exact playback-only movie viewer, and shared local project management, while prohibiting runtime implementation, external/paid operations, and Git publication.
- Decision: create [`SPEC-0011 — My Projects Library, Movie Viewer, and Shared Project Management`](specs/0011-my-projects-library-movie-viewer-and-shared-project-management.md) with exactly three separately gated phases: (1) **Project Library / My Projects**, replacing the inert Home action with a real professional local library and factoring the accepted saved-project evaluator/player into one shared owner; (2) **Movie Viewer**, completing exact saved frames/layers/FPS/timing/background/audio, centered contain geometry, seek/fullscreen/keyboard/accessibility, and one playback clock; and (3) **Shared Management and Polish**, giving My Projects and Open Project one Rename/Duplicate/Delete/Search/Sort command owner with legacy/recovery/reference/concurrency safety.
- Product split: My Projects is the Watch surface and opens the centered playback-only viewer. Open Project is the Edit surface and continues through the existing bootstrap into the sole DrawingWorkspace. Both use one dark gray/navy project-library shell with bright blue/cyan emphasis and truthful real-project metadata/status; labels and primary actions preserve the distinction.
- Reuse boundary: listing uses the existing unified collection/source readers; viewing/posters reuse the accepted SPEC-0009 snapshot evaluator, compositor, and one shared player owner; editing uses the existing bootstrap/editor; commands extend the canonical V2 repository/storage boundary; recovery remains a separate owner; Export encoding/destinations/Finder remain unchanged. No fake list, stored demo thumbnail, second playback engine, second repository, or direct IndexedDB write from UI is allowed.
- Mutation rules: Rename is a validated metadata-only next revision preserving identity/provenance/history. Duplicate uses the existing copy/Save As semantics with a new UUID/revision-one/copy provenance and independent auxiliary/recovery identity; valid protected legacy sources may duplicate to native V2 but are never mutated. Delete is native-V2-only, exact-head/digest/revision confirmed, blocks active/recovery conflicts, removes only the exact project/history, and garbage-collects only provably unreferenced official assets. Recovery and other projects are never silently deleted.
- Viewer rules: modal by default; saved aspect ratio is uniformly contained with no crop/stretch; one clock drives frame/time/audio; completion stops without implicit looping; controls are hover/focus/paused visible; fullscreen is explicit; Escape exits fullscreen before closing; close/backdrop remove only the viewer; keyboard/focus/reduced-motion/compact behavior is mandatory. The optional viewer Export handoff is omitted, preserving existing Export entry points.
- Proof observation: a bounded replay check of the accepted SPEC-0009 browser proof against clean planning base `788e69826438f56ee648452706ce1f68c07592bc` used one disposable environment; the initial execution and one immediate same-environment confirmation reached the player and failed the same inherited frame-3 X-mapping assertion (`0.749406441925578` versus `0.8215350990452878`). No further diagnosis/retry was performed. This is not classified as a proven product regression. Phase 2 entry must reproduce and trace product versus fixture/harness/environment drift, preserve accepted shape tolerance, and may not delete or weaken the assertion merely to pass.
- Boundaries: SPEC-0008 Phases 2–6 remain paused under D-0093. No AI Dashboard/Assistant/Finalizer, notification, drawing/timeline, Save/recovery policy, export encoding/destination, cloud/account, provider/paid, deployment, dependency, or Git publication change is authorized.
- Lifecycle: the proposal is decision-complete but not approved for implementation. Arthur's separate proposal approval, durable activation-record publication, and separate Phase 1 authorization are required. Each later phase requires full predecessor acceptance/control-plane/publication/synchronization/proof-preservation/cleanup plus separate authorization.

### D-0111 — Approve SPEC-0011 and authorize Phase 1 Project Library / My Projects

- Date: 2026-09-21
- Status: Accepted directly by Arthur; SPEC-0011 Approved/active; Phase 1 Authorized/Not started; Phases 2–3 Unauthorized/Not started
- Authority: Arthur explicitly instructed the Project Manager to create one brand-new app copy and start one SPEC-0011 Phase 1 Spec Executor, implement everything in Phase 1, monitor it through completion, and stop only for a genuine owner-blocking emergency or a review-ready app with a simple checklist.
- Phase authorization: after this activation record is committed, pushed, and synchronized, exactly one fresh dedicated Plan-mode Spec Executor may start from that canonical-main SHA and implement only SPEC-0011 Phase 1. The executor must trace the real collection/evaluator/player path, remain inside the Phase 1 runtime/fixture/test/proof boundary, create and independently validate its immutable technical proof manifest, keep the review server running, return the full Implementation Review Packet, and stop.
- Protected boundaries: no Phase 2 synchronized-audio/fullscreen completion, no Phase 3 Rename/Duplicate/Delete/Search/Sort or Open Project restyling, no AI/Terra/Thinking/Assistant/Finalizer/Dashboard/notification work, no drawing/timeline/onion-skin/editor/Save/recovery/export-encoding/destination/Finder behavior change, no dependency/schema change, no external/provider/paid call, no deployment, and no real AI test. Export may change only through behavior-equivalent shared-player factoring required by Phase 1.
- Git boundary: the Spec Executor may not edit canonical control-plane files, stage, commit, merge, push, publish, deploy, clean another worktree, or begin another phase. Arthur/PM acceptance, exclusive CPA propagation, later separate publication, synchronization, proof preservation, and D-0054 cleanup remain sequential future gates.

### D-0112 — Accept and technically verify SPEC-0011 Phase 1 Project Library / My Projects

- Date: 2026-09-21
- Status: Accepted by Arthur; technically Verified; Control Plane Architect propagation complete; publication/integration/proof-preservation/cleanup explicitly authorized and pending
- Authority: Arthur reviewed the ordinary app, marked the Phase 1 result PASS, confirmed the My Projects library/viewer looks good and Terra remains connected, and explicitly accepted absent fullscreen as the correct Phase 2 boundary. He separately authorized canonical control-plane propagation and the later publication/integration lifecycle. The stopped Spec Executor transferred exclusive ownership with an empty index; this propagation task performs no staging, commit, merge, push, cleanup or Phase 2 work.
- Accepted identity: activation base and detached HEAD are `0169c09cc8dbede616fdf099b31925669ec35f6e`; local `main` and `origin/main` matched that commit at takeover. The immutable 28,886-byte manifest `output/spec-0011/phase-1/proof-manifest.json` has SHA-256 `69a169bdd7d7ee96b82d61699b56e1d28f55f4dfeaa0c8614127e4153c0539b6`; the 47,161-byte browser result has SHA-256 `89cc86c8f93638cfbbb11519f345d11a9726de888d62156e88fc4a5a70ae7a81`. Strict pre-propagation validation passed 14 assertions and rejected base, path-set, source-hash, artifact-hash, project/recovery-digest, request-ledger, performance, acceptance, control-plane and publication mutations. That run refreshed the ignored mutable `validation.json` receipt to 1,430 bytes/SHA-256 `c3e6822d5564d4ed99c2b3eaaa3f80d67a998ca722a4d59858cbc3fef3a355da`; it is not an immutable manifest identity or publication path.
- Accepted outcome: Home **My Projects** opens a real local library for native and all five supported legacy kinds with truthful loading/empty/unavailable/invalid/source-changed states, metadata and deterministic earliest-visible-frame posters. Visible/near-visible snapshot evaluation is capped at four. Selection revalidates the exact source and opens a centered playback-only modal using one shared `CanonicalProjectPlayer`; close/backdrop/Escape, focus restoration and library scroll state are preserved. **Open Project** remains the sole editor route, My Projects never mounts `DrawingWorkspace`, and Export retains its accepted chooser/player/encode/destination/Finder behavior.
- Exact technical paths: `app/page.tsx`; `scripts/spec0011-project-library/phase1BrowserProof.ts`; `scripts/spec0011-project-library/phase1Fixtures.ts`; `scripts/spec0011-project-library/phase1Oracle.ts`; `scripts/spec0011-project-library/recordPhase1Proof.ts`; `scripts/spec0011-project-library/validatePhase1Proof.ts`; `src/components/export/AnimationExportFlow.tsx`; `src/components/project-library/ProjectLibrary.tsx`; `src/components/project-library/ProjectPoster.tsx`; `src/components/project-library/projectLibrary.module.css`; `src/components/project-player/CanonicalProjectPlayer.tsx`; `src/components/project-player/ProjectMovieViewer.tsx`; `src/components/project-player/projectPlayer.module.css`; `src/lib/project-library/projectLibraryController.ts`; and `src/lib/project-library/projectLibraryModel.ts`.
- Proof: the Phase 1 oracle passed 35 assertions over native plus five supported legacy kinds with maximum snapshot concurrency four. Real Chromium passed 60 assertions across eight operations and eight screenshots at 1440×900, 390×844 reduced motion, 320×568 and 720×450 200%-zoom equivalent. TypeScript, focused lint and production build passed. First usable list was 101.824875 ms; viewer first frame 126.470333 ms; seek acknowledgement/canvas update 2.672959/123.727459 ms; play acknowledgement 23.852834 ms; five-cycle settled heap delta 8,215,763 bytes; maximum long task 78 ms. Official/recovery storage digests were identical before/after desktop library/viewer, Export shared-player and changed-source retry flows. Full lint retains the inherited 5-error/81-warning baseline in untouched files.
- Protected boundaries: deterministic proof recorded zero external/provider/AI/paid requests, zero credit changes and zero official/recovery writes. AI/Terra sources, dependencies, schema, editor/timeline/onion, Save/Save As/Save and Exit/recovery, export encoding/destinations/Finder, deployment and every unrelated worktree are unchanged. Arthur's Terra observation is human-review evidence; this task made no new live provider call.
- Honest limits: Phase 1 does not implement fullscreen, synchronized audio or the complete Phase 2 accessibility/resource contract; it does not implement Phase 3 Rename/Duplicate/Delete/Search/Sort. Physical-device and non-Chromium behavior, plus a full 64-project all-snapshots-hydrated run, remain unproven. The inherited SPEC-0009 `0.749406441925578` versus `0.8215350990452878` frame-3 mismatch remains a Phase 2 reproduce/reconcile gate, not a Phase 1 regression.
- Lifecycle: the reviewed control-plane/tree paths are `docs/00_MASTER_PROJECT.md`; `docs/AI_SYSTEM.md`; `docs/CURRENT_STATE.md`; `docs/DECISIONS.md`; `docs/PROJECT_MANAGER_CONTEXT.md`; `docs/ROADMAP.md`; `docs/SESSION_HANDOFF.md`; `docs/TODO.md`; `docs/architecture.md`; `docs/changelog.md`; `docs/specs/0011-my-projects-library-movie-viewer-and-shared-project-management.md`; `docs/specs/README.md`; `docs/testing_workflow.md`; and `project/project_structure.txt`. The later publication turn is limited to those 14 plus the exact 15 technical paths, preserves the proof, stops PID 13866/port 57680 only after successful synchronization, and performs D-0054 cleanup. Phases 2–3 remain Unauthorized/Not started.

### D-0113 — Close SPEC-0011 Phase 1 publication, preservation, and D-0054 cleanup

- Date: 2026-09-21
- Status: Accepted; Verified; published; integrated; synchronized; proof-preserved; cleaned up
- Publication: GIT-084 is exact 29-path commit `9a3c5733da8080e09ec93129285ec97a7aebc039`, parent `0169c09cc8dbede616fdf099b31925669ec35f6e`, message `Complete SPEC-0011 Phase 1 project library`: the exact 15 accepted technical paths plus 14 reviewed control-plane/tree paths.
- Synchronization: clean canonical `main` fast-forwarded normally, the push was non-force, and canonical `main`, `origin/main` and live GitHub `main` were verified equal at GIT-084 with `0/0` divergence before this records-only closeout.
- Preservation: 22 proof files/905,940 bytes are preserved at `output/recovery/GIT-084-spec0011-phase1-9a3c573/phase-1`; source and destination files are byte-identical and the relative checksum inventory SHA-256 is `e2fc1d2fa98bcae461a59e9750a4eb104436bbbec5c7fb2e56a5fac4d9ba5d47`. The immutable manifest remains SHA-256 `69a169bdd7d7ee96b82d61699b56e1d28f55f4dfeaa0c8614127e4153c0539b6`. `.env.local` and secrets are excluded.
- Cleanup: review process parent PID 13851 and listener PID 13866 are stopped, port 57680 is closed, the obsolete `/Users/arthurcarlin/.codex/worktrees/3301/stick-animation-app` worktree is absent/unregistered, and merged local branch `codex/spec0011-phase1-publication` is deleted. Canonical main, remote refs, recovery evidence, the active Project Manager worktree and unrelated `/7d98/` remain.
- Boundaries: this records-only closeout changes no accepted runtime, fixture, technical-test or proof byte; no AI/provider/paid call, dependency/schema change, deployment, Phase 2/3 code or SPEC-0008 resumption occurred. Its own terminal-record commit SHA must be read directly from Git.
- Consequence: SPEC-0011 Phase 1 is fully closed. Phase 2's predecessor gate is satisfied and it is ready for Arthur's separate authorization, but remains Unauthorized/Not started. Phase 3 also remains Unauthorized/Not started.

### D-0114 — Authorize SPEC-0011 Phase 2 Movie Viewer after activation synchronization

- Date: 2026-09-21
- Status: Accepted directly by Arthur; Phase 2 Authorized/Not started after activation publication and synchronization; Phase 3 Unauthorized/Not started
- Authority: Arthur explicitly authorized the Control Plane Architect to record and publish the Phase 2 activation now, then separately authorized exactly one fresh Phase 2 Spec Executor after clean synchronization. The pre-activation canonical parent is `119a1eac0febeb2028d1895efbf4ad67a6ad28cb`, with local `main`, local `origin/main`, and live GitHub `main` equal at clean `0/0` before this records-only edit.
- Activation publication: GIT-085 is the canonical commit containing this decision and the reviewed activation-only control-plane package. Its exact SHA is read directly from Git after commit/push rather than embedded recursively in its own bytes. That synchronized SHA is the sole authorized Phase 2 executor base.
- Phase authorization: after GIT-085 is present on canonical `main`, `origin/main`, and live GitHub `main` at clean `0/0`, exactly one fresh dedicated Plan-mode Spec Executor may implement only SPEC-0011 §7 Movie Viewer. It must first verify the single shared evaluator/player owner, reproduce or honestly document non-reproduction of the inherited SPEC-0009 frame-3 mapping mismatch, trace workspace-stage versus canonical-render geometry, and preserve accepted contain/shape tolerances before coding.
- Required outcome: complete one monotonic media clock, exact saved-audio scheduling/seek/pause/resume/overlap/trim behavior, truthful audio failures, explicit fullscreen with actual-state Escape semantics, keyboard/focus/accessibility/control-visibility behavior, contain geometry, bounded resource cleanup, and the full Phase 2 proof/manifest contract. If correctness would require changing accepted Export geometry/output, the executor must stop for owner/spec review.
- Protected boundaries: no Phase 3 Rename/Duplicate/Delete/Search/Sort or Open Project restyling; no project/recovery/storage/schema write change; no encoder/destination/Finder/dependency/package change; no AI/Terra/Thinking/Assistant/Finalizer/Dashboard/notification change; no external/provider/paid call, deployment, SPEC-0008 resumption, or live AI test. Existing Export behavior/output is a protected regression.
- Role/Git boundary: this Control Plane Architect activation changes canonical records only and implements no Phase 2 byte. The fresh Spec Executor may edit only the Phase 2-authorized runtime/fixture/test/proof boundary, must independently validate its immutable manifest, return the full Implementation Review Packet, and stop. It may not edit canonical control-plane files, stage, commit, merge, push, publish, deploy, clean another worktree, or begin Phase 3.

### D-0115 — Accept and technically verify SPEC-0011 Phase 2 Movie Viewer

- Date: 2026-09-21
- Status: Accepted by Arthur; technically Verified; Control Plane Architect propagation complete; publication/integration/proof-preservation/D-0054 cleanup pending under GIT-086; Phase 3 Unauthorized/Not started
- Authority and ownership: Arthur accepted the corrected ordinary review app and explicitly transferred exclusive ownership from the fully stopped Spec Executor to this Control Plane Architect for propagation only. This task may update canonical records and regenerate the project tree, but may not alter accepted technical/proof bytes, stage, commit, merge, push, deploy, stop the review server, remove the worktree/branch, or begin Phase 3.
- Accepted identity: detached base/HEAD, local `main`, and local `origin/main` are all `e6f6b1d7887bbbfaed7a9b00b8b472c5b28fe834`; the index is empty. Immutable 29,132-byte manifest `output/spec-0011/phase-2/proof-manifest.json` has SHA-256 `ddf3b9acbbe5395d92bdeea0c09da9b8dbcfef3bae38c3e1593ee1220a79823d`. Fresh unchanged-tree validation passed 22 assertions and rejected 16 material mutation classes.
- Exact technical paths: `scripts/spec0011-project-library/phase2AudioOracle.ts`; `scripts/spec0011-project-library/phase2BrowserProof.ts`; `scripts/spec0011-project-library/phase2BuildGate.ts`; `scripts/spec0011-project-library/phase2Oracle.ts`; `scripts/spec0011-project-library/recordPhase2Proof.ts`; `scripts/spec0011-project-library/validatePhase2Proof.ts`; `src/components/project-library/ProjectLibrary.tsx`; `src/components/project-player/CanonicalProjectPlayer.tsx`; `src/components/project-player/ProjectMovieViewer.tsx`; `src/components/project-player/projectPlayer.module.css`; `src/components/workspace/ai/DrawingAiPanel.tsx`; `src/lib/export/exportAudio.ts`; `src/lib/project-player/projectPlayerAudio.ts`; `src/lib/project-player/projectPlayerClock.ts`; and `src/lib/project-player/projectPlayerGeometry.ts`.
- Accepted viewer outcome: one monotonic media-time owner drives saved-FPS frame selection, label, seek and audio schedule; active scheduled audio uses `AudioContext.currentTime` and silent playback uses `performance.now()`. Pause/resume/seek, mid-clip offsets, overlap, duration trim, hidden-page reconciliation, completion, Replay to visible frame 1, source invalidation and teardown are deterministic. Fullscreen uses actual browser state; Escape exits fullscreen before modal close. Focus order/trap/return, keyboard/frame-step/Home/End behavior, truthful failure states, high contrast, reduced motion and serious/critical Axe checks pass. The control strip is below the animation with no black overlay gradient, so authored bottom-edge pixels remain visible.
- Geometry reconciliation: the inherited SPEC-0009 frame-3 mismatch was stale independent-axis proof math. Uniform contain yields corrected expected X `0.7501221828399292` versus rendered `0.749406441925578`, within the unchanged `0.025` tolerance. Product geometry and Export output were not changed; no tolerance was weakened.
- Proof and performance: real Chromium passed 146 assertions across 15 flows and seven screenshots. Official/recovery digests are identical before/after. Browser audio created/closed nine contexts and recorded 14 starts, stops and disconnects; source invalidation closes its active context. Unique-asset decode concurrency observed one and remains bounded by two. Viewer-open p95 is `111.473791 ms`, Play acknowledgement p95 `40.647 ms`, released-seek canvas update p95 `30.32725 ms`, maximum long task `104 ms`, and settled five-cycle heap delta about `134,943` bytes (`132,164,437 → 132,299,380`; transient maximum `134,739,602`).
- Focused Terra-route correction: the former focused production build contained only `app/page.tsx`, so `/api/ai-animator` returned HTML 404 and the client blindly parsed JSON. The accepted narrow correction includes the real route in the focused build and safely handles content type/JSON failures for POST/GET/DELETE. Terra prompts, fixed model, reasoning mapping/default, job/tool/provider implementation, transcript/mutation behavior and Thinking gradient remain unchanged. The focused Home plus `/api/ai-animator` production build passes. The full repository build still stops only at the inherited untouched `app/dev/ai-costs/lifetime/page.tsx` `PageProps/searchParams` type failure after successful compilation.
- AI/cost boundary: deterministic browser proof recorded zero provider/paid calls. One separately authorized live `hi` used `gpt-5.6-terra`, Medium reasoning, returned JSON `202` then terminal JSON `200`, displayed `Hi! What would you like to make or explore?`, changed no animation, used 367 input/42 output/409 total tokens, took 3,831 ms and had estimated cost `$0.001238`. It must not be repeated for this phase.
- Honest limits and environment: physical phones/tablets, non-Chromium browsers, native OS audio output/permission UI, native Finder/new encoded output and a fully hydrated 64-project full-resolution collection remain unproven. The ignored `.env.local` is excluded from proof/publication; its contents are not recorded and byte identity is not claimed. Before the accepted correction, one mechanical hash/compare check read its bytes without printing contents; no accepted patch touches it.
- Lifecycle: the reviewed control-plane/tree set is `docs/00_MASTER_PROJECT.md`; `docs/AI_SYSTEM.md`; `docs/CURRENT_STATE.md`; `docs/DECISIONS.md`; `docs/PROJECT_MANAGER_CONTEXT.md`; `docs/ROADMAP.md`; `docs/SESSION_HANDOFF.md`; `docs/TODO.md`; `docs/architecture.md`; `docs/changelog.md`; `docs/specs/0011-my-projects-library-movie-viewer-and-shared-project-management.md`; `docs/specs/README.md`; `docs/testing_workflow.md`; and `project/project_structure.txt`. A later explicit GIT-086 publication turn may stage only those 14 paths plus the exact 15 accepted technical paths, publish/integrate from the unchanged base, verify clean synchronization, preserve proof, then perform exact server/worktree/branch cleanup under D-0054. Until that succeeds, `http://127.0.0.1:57760/`, its listener PID 23010, the accepted worktree and proof stay preserved. Phase 3 remains Unauthorized/Not started.

### D-0116 — Close SPEC-0011 Phase 2 publication, preservation, and review-server lifecycle

- Date: 2026-09-21
- Status: Accepted; Verified; published; integrated; synchronized; proof-preserved; review server stopped; D-0054 cleanup complete
- Publication: GIT-086 is exact 29-path commit `faf2965cb51d47ebcd2b68c9378db8a39486915d`, parent `e6f6b1d7887bbbfaed7a9b00b8b472c5b28fe834`, message `Complete SPEC-0011 Phase 2 movie viewer`: the exact 15 accepted technical paths plus 14 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded normally and the normal push synchronized canonical `main`, local `origin/main` and live GitHub `main` at clean `0/0`.
- Technical evidence: immutable 29,132-byte manifest SHA-256 remains `ddf3b9acbbe5395d92bdeea0c09da9b8dbcfef3bae38c3e1593ee1220a79823d`. Bound proof remains 146 real-Chromium assertions across 15 flows/seven screenshots, one monotonic playback clock, synchronized audio, completion/Replay, fullscreen, keyboard/focus/accessibility, responsive uniform-contain geometry and full resource teardown. The inherited frame-3 mismatch remains resolved as stale independent-axis proof math with unchanged product geometry, Export output and `0.025` tolerance.
- Preservation: 33 files/1,370,820 bytes are preserved at `/Users/arthurcarlin/Projects/stick-animation-app/output/recovery/GIT-086-spec0011-phase2-faf2965/phase-2`; sibling `checksum-inventory.txt` has SHA-256 `ee4861967a418098e0eae73be71a7108a0bf0f00ea704988a48c7bf4dbef03ac`, and source/destination checksum comparison is empty. `.env.local`, credentials and secrets are excluded; byte identity for `.env.local` is not claimed.
- AI/build boundary: deterministic proof made zero provider calls. The sole authorized live Medium `gpt-5.6-terra` `hi` remains JSON `202/200`, reply `Hi! What would you like to make or explore?`, 367 input/42 output/409 total tokens, 3,831 ms, estimated `$0.001238`, and no animation mutation; it must not be repeated. Focused Home plus `/api/ai-animator` build/error handling passes. The full build's inherited untouched `app/dev/ai-costs/lifetime/page.tsx` `PageProps/searchParams` failure remains outside Phase 2 and unchanged.
- Cleanup: review listener PID 23010 is stopped and ports 57760 and 57730 are closed. After the terminal-record agent stopped, the Project Manager reverified both worktrees at clean synchronized `c0c4877817e73900b4aa672fa8ccc1b4cb40da4c`, then removed only `/Users/arthurcarlin/.codex/worktrees/35aa/stick-animation-app` and merged local branch `codex/spec0011-phase2-publication` under D-0054. Canonical main, remote refs, recovery evidence and unrelated worktrees remain protected.
- Unchanged systems: no post-GIT-086 runtime, fixture, technical-test, proof, dependency/package/lockfile, credential/environment, prompt/model/reasoning/job/tool/provider/Thinking, project/recovery/storage/schema, Export encoder/destination/Finder/output, deployment or paused SPEC-0008 byte changed. Physical-device, non-Chromium, native-audio, native-Finder/new-encoding and fully hydrated 64-project coverage remain unproven rather than newly claimed.
- Consequence: SPEC-0011 Phase 2 is fully accepted, Verified, published, integrated, synchronized, proof-preserved and cleaned up. Phase 3 is entry-ready for Arthur's separate explicit authorization but remains Unauthorized/Not started. This decision does not authorize Phase 3, another provider call, deployment or any new implementation.

### D-0117 — Authorize SPEC-0011 Phase 3 Shared Management and Polish after activation synchronization

- Date: 2026-09-21
- Status: Accepted directly by Arthur; Phase 3 Authorized/Not started after activation publication and synchronization
- Authority: Arthur explicitly authorized SPEC-0011 Phase 3 implementation and the publication needed to begin it. This Control Plane Architect must record and publish only the Phase 3 activation, then stop. The pre-activation canonical parent is `2f2a3c4866c46d1362d7184f389ca02f856883b6`, with local `main`, local `origin/main`, and live GitHub `main` equal at clean `0/0` before this records-only edit.
- Activation publication: GIT-087 is the canonical commit containing this decision and the reviewed activation-only control-plane package. Its exact SHA is read directly from Git after commit/push rather than embedded recursively in its own bytes. That synchronized SHA is the sole authorized Phase 3 executor base.
- Phase authorization: after GIT-087 is present on canonical `main`, `origin/main`, and live GitHub `main` at clean `0/0`, exactly one fresh dedicated Plan-mode Spec Executor may implement only SPEC-0011 §8 Shared Management and Polish. It must first trace the live My Projects/Open Project collection, repository/storage Save/Save As, recovery-metadata, open-view/editor ownership, legacy hydration and asset-reference paths before coding.
- Required outcome: one `ProjectManagementCommandOwner` serves both My Projects and Open Project for local Search/Sort and revalidated Rename/Duplicate/Delete. Rename is a same-ID metadata-only next revision; Duplicate uses the existing new-ID copy path for native or valid legacy sources; Delete is native-only, exact-head/revision/digest bound, active-view/editor and recovery-conflict blocked, and removes only the exact project/history plus provably unreferenced official assets. Same-tab and cross-tab refresh, stable races, overflow/context/keyboard routes, Watch-versus-Edit clarity, responsive/accessibility polish, and the full Phase 3 proof/manifest contract are mandatory.
- Protected boundaries: no Movie Viewer/player/audio/fullscreen/geometry change; no editor/timeline/onion/manual-command or Save/Save As/Save and Exit behavior change; no recovery-policy or recovery-write/delete change; no legacy-source mutation; no Export encoder/destination/Finder/output change; no package/dependency/schema migration; no AI/Terra/Thinking/Assistant/Finalizer/Dashboard/notification change; no external/provider/paid call, deployment, SPEC-0008 resumption, or live AI test.
- Role/Git boundary: this Control Plane Architect activation changes canonical records only and implements no Phase 3 byte. The fresh Spec Executor may edit only the Phase 3-authorized runtime/fixture/test/proof boundary, must independently validate its immutable manifest, return the full Implementation Review Packet, and stop. It may not edit canonical control-plane files, stage, commit, merge, push, publish, deploy, clean another worktree, or begin another spec/phase.

### D-0118 — Accept and technically verify SPEC-0011 Phase 3 Shared Management and Polish

- Date: 2026-09-22
- Status: Accepted directly by Arthur; technically Verified; publication/integration, proof preservation, review-server shutdown and D-0054 cleanup pending
- Authority and ownership: Arthur accepted the completed ordinary review app and explicitly transferred exclusive ownership from the fully stopped Spec Executor to the Control Plane Architect for propagation only. This task may update canonical records and regenerate the project tree, but may not change accepted technical/proof bytes, stage, commit, merge, push, deploy, stop the review server, remove the worktree/branch, or begin another spec/phase.
- Accepted identity: detached base/HEAD, local `main`, and local `origin/main` are all `b57ff995df9bb351dacb9a6a79b8cbd23203e988`; the index is empty. Immutable 46,868-byte manifest `output/spec-0011/phase-3/proof-manifest.json` has SHA-256 `92ae122c227a36fd8a637b1922b1bca139224be4caf0e42d3a147f2a43147aaa` and source digest `05ee661120e7751658b9ea2e5269c7cc1cbc666645568af1db7b1ff1f08074d1`. Fresh unchanged-tree validation returned VALID with 18 assertions and rejected 12 material mutation classes.
- Exact technical paths: `app/page.tsx`; `scripts/spec0011-project-library/phase3BrowserProof.ts`; `scripts/spec0011-project-library/phase3Oracle.ts`; `scripts/spec0011-project-library/recordPhase3Proof.ts`; `scripts/spec0011-project-library/validatePhase3Proof.ts`; `src/components/open-project/OpenProjectBrowser.tsx`; `src/components/project-library/ProjectLibrary.tsx`; `src/components/project-library/projectLibrary.module.css`; `src/lib/animation/unifiedProjectCollection.ts`; `src/lib/animation/unifiedProjectManagementV2.ts`; `src/lib/animation/unifiedProjectRepositoryV2.ts`; `src/lib/animation/unifiedProjectSourceReader.ts`; `src/lib/animation/unifiedProjectStorageV2.ts`; and `src/lib/project-library/projectLibraryModel.ts`.
- Accepted outcome: My Projects and Open Project now share one authoritative `ProjectLibrary` shell and one `ProjectManagementCommandOwner`. NFC-normalized/case-insensitive search and four sorts are local presentation state. Rename is native-only and creates one metadata-only next revision under CAS. Duplicate accepts valid native or legacy sources through the existing Save As path while legacy source bytes remain unchanged. Delete is native-only, binds exact head/revision/digest, blocks active viewer/editor/recovery conflicts, removes only the target project/history, deletes only provably unreferenced assets, and preserves shared assets.
- Concurrency and interaction: Web Locks plus localStorage heartbeat leases serialize management/editor ownership and the editor lease follows Save/Save As identity. BroadcastChannel/storage events are advisory invalidation only; authoritative rereads decide state. Visible overflow, right-click, `Shift+F10`/Context Menu, arrow/Home/End/Escape, accessible dialogs/focus return and distinct Watch/Edit actions pass.
- Proof and performance: Phase 3 oracle passed 31 assertions. Real Chromium passed 47 assertions across nine flows and four screenshots, including desktop/compact/reduced-motion/forced-colors/200%-zoom-equivalent and zero serious/critical Axe findings. The compact 64-project shell completed in 215.432 ms; search p95 was 18.1 ms, sort p95 17.3 ms and menu p95 17.9 ms. TypeScript, focused lint, focused production build, Phase 1 oracle, Phase 2 player oracle and Phase 2 audio oracle pass. The full repository build still compiles and then stops only at the inherited untouched `app/dev/ai-costs/lifetime/page.tsx` `PageProps/searchParams` failure.
- Protected boundaries: deterministic proof made zero external/provider/AI/paid requests. Movie Viewer/player/audio/fullscreen/geometry, editor/timeline/onion/manual commands, Save/Save As/Save and Exit semantics, recovery policy/writes/deletes, legacy source stores, Export encoder/destinations/Finder/output, dependencies/schema, credentials/environment, AI/Terra/Thinking, deployment and paused SPEC-0008 remain unchanged. Physical devices and non-Chromium browsers remain unproven.
- Lifecycle: the reviewed control-plane/tree set is `docs/00_MASTER_PROJECT.md`; `docs/AI_SYSTEM.md`; `docs/CURRENT_STATE.md`; `docs/DECISIONS.md`; `docs/PROJECT_MANAGER_CONTEXT.md`; `docs/ROADMAP.md`; `docs/SESSION_HANDOFF.md`; `docs/TODO.md`; `docs/architecture.md`; `docs/changelog.md`; `docs/specs/0011-my-projects-library-movie-viewer-and-shared-project-management.md`; `docs/specs/README.md`; `docs/testing_workflow.md`; and `project/project_structure.txt`. A later GIT-088 publication turn may stage only those 14 paths plus the exact 14 accepted technical paths, publish/integrate from the unchanged base, verify clean synchronization, preserve proof, then stop review PID 32270/port 57840 and complete exact D-0054 worktree/branch cleanup. Until then, the accepted worktree, server and proof stay preserved. All three SPEC-0011 product phases are accepted/Verified; there is no Phase 4.

### D-0119 — Close SPEC-0011 Phase 3 publication, preservation, cleanup, and the three-phase spec

- Date: 2026-09-22
- Status: Accepted directly by Arthur; GIT-088 published/integrated/synchronized/proof-preserved/cleaned up; SPEC-0011 closed
- Authority: Arthur accepted the Phase 3 correction, explicitly authorized canonical control-plane propagation, commit/push, closure of SPEC-0011, archival of obsolete tasks, and integration of all three phases into the real Diamond Animator.
- Publication: GIT-088 is exact 28-path commit `d0ec1b23d7e1d9ff13a0779c2758e33e858691f0`, parent `b57ff995df9bb351dacb9a6a79b8cbd23203e988`, message `Complete SPEC-0011 Phase 3 shared project management`: 14 accepted technical paths plus 14 reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded normally, the push succeeded, and canonical `main`, local `origin/main`, and live GitHub `main` were verified equal at clean `0/0`.
- Preservation: 19 files/604,195 bytes are preserved under `output/recovery/GIT-088-spec0011-phase3-d0ec1b2/phase-3`; sibling `checksum-inventory.txt` hashes to `196e1e7a5dd7af4b82eeff9630d535dd4ba3ae45b56f2bb3eec63bbb46fa4dac`, and the source/destination checksum comparison was empty. The immutable technical manifest remains SHA-256 `92ae122c227a36fd8a637b1922b1bca139224be4caf0e42d3a147f2a43147aaa` with source digest `05ee661120e7751658b9ea2e5269c7cc1cbc666645568af1db7b1ff1f08074d1`.
- Cleanup: review PID 32270 is stopped and port 57840 is closed. D-0054 removed only `/Users/arthurcarlin/.codex/worktrees/d436/stick-animation-app` and local `codex/spec0011-phase3-publication` after publication/preservation. The obsolete dirty activation worktree `/Users/arthurcarlin/.codex/worktrees/ac96/stick-animation-app` was inventoried and backed up under `output/recovery/obsolete-spec0011-phase3-activation-ac96-2f2a3c4/` before its worktree and local branch were removed. Canonical main, the active Project Manager worktree, remote refs, and recovery evidence remain protected.
- Boundary: this terminal closeout changes canonical records only. It changes no accepted runtime, fixture, technical-test, proof, dependency, credential/environment, AI/Terra/Thinking, Movie Viewer, editor/timeline/onion/manual command, Save/Save As/Save and Exit/recovery, legacy-source, Export, deployment, or paused SPEC-0008 byte. Phase 3 publication made zero provider/AI/paid requests.
- Consequence: SPEC-0011 Phases 1–3 are fully Verified, published, integrated, synchronized, proof-preserved, and cleaned up. SPEC-0011 is closed and has no Phase 4. D-0093 continues to keep SPEC-0008 Phases 2–6 paused; the next feature/spec requires separate Arthur authorization.
- Terminal record: the records-only closeout successor is `f334639f33b7b130a2f9409dcdf146dc2fffb165`, direct parent GIT-088 product commit `d0ec1b23d7e1d9ff13a0779c2758e33e858691f0`. Canonical checkout, local `main`, local `origin/main`, and live GitHub `main` synchronized at that successor; it became the later SPEC-0012 planning parent without changing runtime bytes.

### D-0120 — Create decision-complete SPEC-0012 Diamond Animator Guidance Assistant plan

- Date: 2026-09-22
- Status: Accepted product direction and Proposed implementation specification; no phase authorized; no Git publication
- Authority: Arthur accepted the six-phase outcome and instructed a dedicated Spec Architect to create the complete planning/control-plane package, trace the live Home/AI Animator/storage/job/activity/API/project-mutation boundaries, and stop before runtime implementation, live or paid calls, Phase 1 executor creation, staging, commit, push, publication or deployment.
- Current evidence: clean canonical checkout, local `main`, local `origin/main`, and live GitHub `main` started synchronized at terminal SPEC-0011 closeout successor `f334639f33b7b130a2f9409dcdf146dc2fffb165`, direct parent GIT-088 product commit `d0ec1b23d7e1d9ff13a0779c2758e33e858691f0`. `app/page.tsx` includes `assistant` in `HomeCardId`, but the visible **AI Assistant — Ask questions and get guidance** button has no activation handler or mounted view. Fresh real Chromium confirms clicking it leaves Home unchanged and makes no Assistant request/storage/project change. The accepted workspace AI Animator remains a separate `DrawingAiPanel` → `/api/ai-animator` → `AiAnimatorJobService` → fixed-Terra path with project-scoped ledger and no Phase 1 animation mutation.
- Product decision: create [`SPEC-0012 — Diamond Animator Guidance Assistant`](specs/0012-diamond-animator-guidance-assistant.md) with exactly six separately gated phases: (1) navigation/responsive shell with no paid call; (2) bounded 50-session local persistence and management; (3) fixed-Terra versioned-catalog guidance; (4) optional hosted web search with sources/truthful activity; (5) separately approved microphone dictation; and (6) beta hardening/regression closeout.
- Session decision: persist at most 50 sessions and never silently evict. At 50, block New Chat until confirmed deletion. Blank chats remain ephemeral until first Send. Persist first user turn before the request; the same Terra call returns answer plus title candidate; manual rename is permanently authoritative. Selection, rename and confirmed delete exist; Duplicate does not.
- Ownership decision: Assistant uses dedicated `/assistant`, `/api/diamond-assistant`, contracts, job service, IndexedDB, knowledge catalog, components, fixtures/tests and proof. Its request/response has no project ID/bytes/workspace context or mutation command, and its runtime may not import/reuse animation repository/storage/recovery/Export/project-management/manual-registry or AI Animator route/job/storage ownership. It may explain navigation but not control UI.
- Model/activity decision: production guidance uses only `gpt-5.6-terra`; visible Low/Medium/High/Extra High map to `low`/`medium`/`high`/`xhigh`, with Medium default. Truthful labels are Thinking; Searching the internet only during an actual hosted-search interval; and Finalizing answer only after reasoning/tools while composing/validating output. Each bright-blue and dark-blue full-label sweep lasts exactly one second independent of label length, followed by the accepted 1.5-second pause; reduced motion is static text.
- Knowledge/search decision: internal Diamond Animator questions use checked-in versioned `diamond-animator-knowledge/v1`. Hosted Responses `web_search` may be offered automatically only for a genuine external/current fact after local retrieval and after Phase 4's current official access/privacy/pricing gate. Search answers bind accessible sources. The Assistant never claims to have watched inaccessible YouTube/TikTok/social video and gains no arbitrary fetch/scrape/download/media-inspection path.
- Dictation decision: microphone input is bounded dictation, not live voice. User-gesture permission, actual waveform, Cancel/Stop, editable transcription and explicit Send are mandatory; raw audio is never persisted. The transcription route/provider/model and live use require a separate Phase 5 decision.
- Safety/cost decision: server-only credentials, bounded session/context/output/tool/source/storage/audio/time/concurrency limits, terminal cost receipts, no automatic paid retry, honest missing-key/offline/provider/restart/quota/corruption behavior, accessibility and exact zero-project-mutation proof are mandatory. Deterministic provider/search/transcription doubles are the default; every live/paid gate is separately authorized.
- Schedule decision: target Tuesday, September 29, 2026 and 85–90% local/private-beta readiness only if each phase passes without major rejection. This is not a certainty, production/public-beta claim, phase-combination permission or lifecycle bypass.
- Lifecycle decision: every phase uses one fresh Plan-mode Spec Executor → stopped Implementation Review Packet → Arthur/PM acceptance or rejection → sequential exclusive Control Plane Architect propagation/closeout → stop → later separate publication/integration → proof preservation/D-0054 cleanup. The next phase starts only after durable canonical-main integration. This planning package creates no Phase 1 executor and authorizes no implementation or publication.
- Readiness: no unresolved blocker prevents Phase 1 after this exact planning package is reviewed, separately published/synchronized, and Arthur separately authorizes it. Later live Terra, search, transcription and public-beta gates remain intentionally unresolved until their named phases.

### D-0121 — Publish SPEC-0012 planning and authorize Phase 1 only

- Date: 2026-09-22
- Status: Accepted by Arthur; GIT-089 published/synchronized; Phase 1 authorized from the exact planning SHA
- Authority: Arthur accepted publication of the D-0120 plan and then explicitly authorized one fresh Phase 1 app copy/executor. This decision does not authorize Phase 2, a real guidance call, search, dictation, deployment, or any AI Animator phase.
- Publication: GIT-089 is exact planning commit `f2bda33842a3f5a4b560a7d3aab170a6ca4c2fd0`, parent terminal SPEC-0011 closeout successor `f334639f33b7b130a2f9409dcdf146dc2fffb165`. Canonical `main`, local `origin/main`, and live GitHub `main` were synchronized there before the Phase 1 executor started.
- Phase boundary: Phase 1 is the dedicated `/assistant` navigation and responsive local shell only. Real sessions, Assistant API/provider answers, search, dictation, project/animation context or mutation, and deployment remain outside scope.

### D-0122 — Accept and technically verify corrected SPEC-0012 Phase 1

- Date: 2026-09-22
- Status: Accepted directly by Arthur; Control Plane Architect propagation complete; GIT-090 publication/integration authorized and pending
- Authority: after multiple narrow visible corrections, Arthur called the final Phase 1 app acceptable and explicitly authorized canonical control-plane propagation, commit, push and integration into the real Diamond Animator. The stopped executor transferred exclusive ownership with base/HEAD GIT-089 `f2bda33842a3f5a4b560a7d3aab170a6ca4c2fd0`, empty index and exactly eleven technical dirty paths.
- Accepted outcome: Home opens dedicated `/assistant`; Back returns Home and restores focus. The Home-style navy shell uses the official solid-white sidebar diamond and translucent-white hero diamond with preserved subtle blue glow, exact accessible greeting rendered as two deliberate lines at all sidebar bounds, a fully visible compact composer, Medium-default four-level reasoning control, and preview-only New Chat/microphone/Send. Desktop sidebar resizing is bounded 200/256/440 pixels, preserves at least 560 pixels of conversation space, supports pointer capture plus Arrow/Home/End, has hover/focus/active affordance only, does not collapse or persist, and becomes a fixed top strip at 760 pixels or below.
- Proof: immutable `output/spec-0012/phase-1-correction/proof-manifest.json` SHA-256 `3d25091e617b24544439419a99426b373cc560df64e01d4c735518d1f11104dd`, source digest `3f3948195cec0f10c68889e1990782e0872cec2cb52c644ff532ea9058608e04`, validation receipt SHA-256 `86d82bea5f5984d402fde564294312b7785fc2d1c00c874d76c00a7b7276ff29`. Fresh pre-propagation validation passed all substantive assertions/mutation rejections and stopped only when the immutable existing validation receipt refused overwrite.
- Verification: 351 real-Chromium shell assertions across seven profiles/13 screenshots, no horizontal overflow, zero serious/critical Axe finding, unchanged Assistant storage digests, zero Assistant API/provider/search/transcription calls, 254 protected AI Animator assertions/18 mocked requests, TypeScript/focused lint/focused build, and exact source/path checks pass. Repository-wide lint remains the inherited 5-error/81-warning baseline; the full build retains the inherited untouched dev AI-cost route typing failure.
- Protected boundary: no accepted source path changes AI Animator model/prompt/route/job/storage/Thinking, editor/timeline/canvas/tools, projects/recovery, My Projects, Export, dependencies, credentials, deployment, or paused SPEC-0008. The ignored review environment targets the canonical ignored environment without reading/tracking its contents. Two separately authorized small Animator greeting observations across executor/PM evidence returned naturally with no animation mutation; publication makes no further live call.
- Publication boundary: only the exact eleven accepted technical paths plus reviewed canonical control-plane/tree paths may enter GIT-090. After clean synchronization, proof preservation, exact review-server shutdown and D-0054 cleanup, Phase 2 is merely ready for separate authorization; it does not start automatically.

### D-0123 — Close SPEC-0012 Phase 1 publication, preservation, and cleanup

- Date: 2026-09-22
- Status: Accepted directly by Arthur; GIT-090 published/integrated/synchronized/proof-preserved/cleaned up; Phase 1 closed
- Authority: Arthur accepted the corrected Phase 1 app and explicitly authorized control-plane propagation, commit, push, and integration into the real Diamond Animator. This decision records completion of that exact publication authority; it does not authorize Phase 2 or any live/paid Assistant/search/transcription action.
- Publication: GIT-090 is exact 26-path commit `e725f562307e0c7530b32b416218018af87b65fd`, parent planning GIT-089 `f2bda33842a3f5a4b560a7d3aab170a6ca4c2fd0`, message `Complete SPEC-0012 Phase 1 assistant shell`: eleven accepted technical paths plus fifteen reviewed control-plane/tree paths. Clean canonical `main` fast-forwarded normally, the push succeeded, and canonical `main`, local `origin/main`, and live GitHub `main` were verified equal at clean `0/0`.
- Preservation: ninety files/30,483,497 bytes are preserved under `output/recovery/GIT-090-spec0012-phase1-e725f56/phase-1`; source/destination checksum comparison was empty and `checksum-inventory.txt` hashes to `181ffc69965be95e5172c446b1d70862e8d23474a581b991d023727e7a340aad`. The immutable technical manifest remains SHA-256 `3d25091e617b24544439419a99426b373cc560df64e01d4c735518d1f11104dd`; validation receipt remains SHA-256 `86d82bea5f5984d402fde564294312b7785fc2d1c00c874d76c00a7b7276ff29`.
- Cleanup: exact review PID 74761 is stopped and port 57950 is closed. D-0054 removed only accepted `/Users/arthurcarlin/.codex/worktrees/9aa0/stick-animation-app` and merged local `codex/spec0012-phase-1-correction-registration`. The obsolete rejected duplicate `/Users/arthurcarlin/.codex/worktrees/e81a/stick-animation-app` had no running server; its eleven exact dirty files/136,920 bytes, binary patch and metadata were preserved under `output/recovery/obsolete-spec0012-phase1-executor-e81a-f2bda33/` with inventory SHA-256 `c391bd17b97055d006c23fdb8f6be9e94dea6231bf2ad2008c6452e35aded103` and patch SHA-256 `fc0aa0194ffae1c2a65310e14df754626f4d00e32842d7826ccee891293f55de` before its worktree/local branch were removed. Canonical main, active Project Manager worktree, remote refs and recovery evidence remained protected.
- Boundary: this terminal closeout changes canonical records/tree only. It changes no accepted runtime, fixture, technical-test, proof, dependency, environment/credential, AI Animator Terra/model/prompt/route/job/storage/Thinking, editor/timeline/canvas/tools, project/recovery/My Projects/Export, deployment, or paused SPEC-0008 byte. No additional provider/AI/paid call occurred during publication/cleanup.
- Consequence: SPEC-0012 Phase 1 is fully Verified, published, integrated, synchronized, proof-preserved and cleaned up. Phase 2 is entry-ready but remains Unauthorized/Not started until Arthur separately authorizes it. The records-only terminal closeout successor's exact SHA must be read directly from Git.

### D-0124 — Accept combined SPEC-0012 Phase 2 and remove cumulative local Assistant spending stops

- Date: 2026-09-22
- Status: Accepted directly by Arthur and Project Manager; technical verification and Control Plane Architect propagation complete; GIT-091 publication/integration pending separate authority
- Authority and scope: Arthur separately authorized Phase 2, and the PM delegation explicitly moved the first functional fixed-Terra/local-catalog guidance outcome forward from planned Phase 3 into the same executor phase. After the stopped executor's initial result and presentation corrections, Arthur explicitly directed complete removal of the cumulative local Assistant daily/monthly reservation/cap mechanism, including the budget file/lock gate and former reached/could-not-verify messages. Arthur understands the spend consequence and chooses when to stop local review use.
- Accepted outcome: the dedicated `/assistant` surface now owns Assistant-only IndexedDB sessions; blank-chat non-persistence; atomic first Send; same-call answer/title with manual-title authority; selection, rename, confirmed delete, 50-session no-eviction blocking, bounded messages/bytes/context, long-chat Jump to latest, reload/restart/two-tab/fault recovery, accepted typewriter/presentation behavior, and one-shot long-wait local progress text. Dedicated local-only `/api/diamond-assistant`, strict contracts, `DiamondAssistantJobService`, versioned local knowledge and fixed `gpt-5.6-terra` guidance are included. Search/tools and microphone remain off. The planned standalone Phase 3 scope is satisfied here and has no separate executor.
- Retained safeguards: exact `$0.15` maximum estimated cost per request; 24,000 input and 4,000 output tokens; 90-second timeout/deadline; `maxRetries: 0`; no automatic resend; at most two active jobs and one per session; exact identity/dedupe; cancel/deadline terminal single winner; 500 remembered job identities; 50 chats; strict answer/model/usage validation; usage/cost receipt; local-only route; server-only credential; `tools: []`; `store: false`; and hard zero animation/project context, imports and mutation. Historical budget files are inert, not migration inputs.
- Technical identity: detached base/HEAD `652431396e78370f3dfb7549294e66d154c3b7d4`, empty index, exactly 34 accepted technical paths. Final immutable manifest `output/spec-0012/phase-2-final-correction/proof-manifest.json` has SHA-256 `b6251390f20e8bb7dfe26208241b163c3f306e197ae37ace5340b6a8f572350c`, source digest `304ef20707128abef122355ccf5e0692ed7663be45e75cb3640d519f500e88d7`, 34 source and 116 evidence bindings, and 34 rejected mutation cases. Original seal `63a87f3e832a200a52c18115fa9ed7533eaf1fb4b5cfc01438e2c1f8c1b3c8ec`/67 evidence and correction seal `68953d8900f793676cdb5eaa611a449112664b4f1af504690cac9a682cd5e6f5`/101 evidence remain intact.
- Verification: 120 provider-adapter/installed-SDK transport-double successes across exhausted, malformed, locked and absent legacy budget fixtures; twelve consecutive ordinary browser Sends in one chat; all session, timing/presentation, protected-browser, restart, fault, Animator-smoke and thirteen protected technical suites passed. TypeScript, focused lint, focused production build, exact diff/scope and final restored-page smoke passed. Executor automated live/paid provider calls and cost were zero.
- Honest limits: live provider availability/answer quality, physical devices/non-Chromium and the historical Animator transient-reveal observation remain unproven. Full build retains the untouched dev AI-cost PageProps/searchParams failure; repository lint retains five errors/81 warnings only in byte-identical base files.
- Boundary and consequence: no accepted runtime/fixture/technical-test/proof byte changed during CPA propagation. No hosted search/citation, transcription, animation/project mutation, SPEC-0008 resumption, deployment, staging, commit, push, proof preservation, server shutdown or cleanup is claimed. GIT-091 publication/integration, preservation and D-0054 cleanup require a later explicit instruction; Phase 4 cannot start before that closeout.

### D-0125 — Accept and technically verify SPEC-0012 Phase 4 hosted search and corrupt-row recovery

- Date: 2026-09-23
- Status: Accepted directly by Arthur and Project Manager; technical verification and Control Plane Architect propagation complete; GIT-092 publication/integration pending separate authority
- Prior closeout: GIT-091 is exact 49-path commit `fa2ef6c526d04de9c77b356b53b4768890c46e1f`, message `Complete SPEC-0012 Phase 2 guidance assistant`: 34 accepted technical plus 15 reviewed record/tree paths. Canonical `main`, `origin/main` and live GitHub `main` are synchronized there. Complete Phase 2 proof is preserved at `output/recovery/GIT-091-spec0012-phase2-fa2ef6c`: 1,778 files/79,483,466 bytes, inventory SHA-256 `ac86b7f5ca1c4ef44de036f0f20d94851801a58a43c6f04a95035ee43bf1c97b`, exact source/destination equality. Port 57970 is closed and the obsolete Phase 2 review worktree is absent/unregistered. This records an already completed prior closeout; D-0125 performs no Phase 2 publication or cleanup action.
- Authority and acceptance: after Phase 4 authorization, bounded live-use decisions and the stopped executor's technical/corrupt-row correction proof, Arthur and the Project Manager accepted the final exact review copy. Exclusive ownership transferred sequentially to the Control Plane Architect on branch `codex/spec0012-phase-4-fresh-search`, exact GIT-091 base/HEAD `fa2ef6c526d04de9c77b356b53b4768890c46e1f`, empty index and exactly 27 technical dirty paths.
- Accepted outcome: deterministic local-catalog retrieval and search eligibility run before provider tools. Internal product questions, greetings and stable animation principles remain zero-search. Genuine external/current questions may use only current Responses hosted `web_search` with `tool_choice: required`; real provider events own Searching and Finalizing. Canonical public HTTPS sources, titles, actions and output-text citation ranges are validated before publication and persist across reload. The Assistant never claims to have watched inaccessible video. Arbitrary fetch/scrape/download, media inspection, image search, social authentication, model fallback, automatic retry and animation/project mutation remain absent.
- Limits and presentation: fixed `gpt-5.6-terra`; Low/Medium/High/Extra High with Medium default; maximum two hosted calls, eight processed/six displayed sources, 512 query characters and a 45-second search deadline inside the 90-second job; `$0.01` per hosted call inside the existing `$0.15` request ceiling; `store:false`. The accepted inherited full-label presentation is one-second bright + one-second dark + 1.75-second pause, with static reduced motion. This accepted 3.75-second cycle supersedes D-0120's stale original 3.5-second planning value without changing Phase 4 runtime during CPA propagation.
- Corrupt-row correction: unreadable raw IndexedDB rows are conservatively included in row/byte capacity, their canonical bytes remain exactly unchanged, unsafe overwrite is rejected, and an independent healthy chat remains usable. Its only runtime correction paths are `src/lib/assistant/assistantStorage.ts` and `src/components/assistant/useAssistantSessions.ts`; phase-owned proof refreshes `phase4BrowserRegressions.ts`, `phase4FaultProof.ts`, `recordPhase4Proof.ts`, `validatePhase4Proof.ts` and adds `phase4CorruptRowProof.ts`. Accepted runtime hashes are `c0c91ef607025f707d40b5af5e644aad12f0ef824a76e87f9e1279ee362cf80c` and `1b3ae967b385ca2b5114b1883cb940c2af62ccc8fcfa7518b895d725c2bd2ac9`; the nine search/provider/presentation sources are exact against the prior seal.
- Technical identity: immutable `output/spec-0012/phase-4/proof-manifest.json` SHA-256 `08ff725e2d9bc658e198462e171623b1290d7b7de0b82bb3da93d085e4cfd095`; validation receipt SHA-256 `3107a15f9193719fe3ef1b8461f1003187ae75157f2786956baca4096a528060`; source digest `073bc363b1ba26e6be035df69136f7288687df028ed92ec9701ab649c17517fd`; 27 source/104 evidence bindings. The previous seal remains manifest SHA-256 `f29b22dbc6f112a4ca939bfaf908e5102710dfc6bac194954efb254c3a22a18a`, validation `ec8f0f37158a9e672b4a5fe3b8c138016efe7078221a637c6adc39a6dcc1f96b`, source digest `7dd51abe690337cd83535a6e41ec860f1fce1ffcdd0c58d095936b60f588c1d1`. The strict executor validator passed before propagation and intentionally binds the pre-propagation spec bytes/exact 27-path dirty set; after records changed, the CPA instead revalidated every immutable source/evidence/freeze/prior-seal hash and the exact 27+15 publication allowlist without rewriting that historical seal.
- Verification: 47 deterministic-oracle, 23 Phase 4 browser, 27 corrupt-row, 20 fault, 90 inherited Assistant browser, 60 presentation, 21 protected-browser, ten restart, 33 repeated-browser and eleven Animator-smoke assertions plus thirteen protected technical oracles pass. TypeScript, focused lint, exact diff and focused production build pass. The full build retains only the byte-identical inherited `app/dev/ai-costs/lifetime/page.tsx` PageProps/searchParams failure.
- Live/cost record: the final authorized successful search used fixed Terra/Medium, one request/no retry, one hosted tool call, eight processed/three displayed sources, truthful Searching/Finalizing, accessible citations, reload persistence and unchanged project sentinel. Its application receipt is `$0.050754`. Two earlier authorized searches reached the hosted tool but failed before application receipts; their provider cost is unknown. The final corrupt-row correction made zero live/provider/search/paid calls. Chrome isolated IndexedDB is proven; physical devices and non-Chromium remain unproven.
- Boundary and consequence: CPA propagation changes only reviewed canonical control-plane/tree paths and changes no accepted runtime, fixture, technical-test, proof, manifest or evidence byte. Nothing is staged, committed, pushed, published or deployed; PID 31449 and port 58040 remain active for review. GIT-092 requires separate publication authority for the exact 27 technical plus 15 reviewed record/tree paths, proof preservation, server shutdown and D-0054 cleanup. Phase 5 is entry-ready only after GIT-092 closes but remains **Unauthorized; Not started** and still requires a separate transcription provider/model/privacy/cost decision. No further live/provider/search call is authorized.

### D-0126 — Publish, activate, preserve, and clean up SPEC-0012 Phase 4

- Date: 2026-09-23
- Status: Fully closed; Verified/published/integrated/synchronized/proof-preserved/runtime-activated/cleaned up
- Authority: Arthur explicitly accepted Phase 4 and authorized control-plane recording, commit, GitHub push, canonical-main integration, and cleanup, then rejected the first real-main result when current questions still used the old local-only provider and directed the Project Manager to finish the integration.
- Publication: exact 42-path GIT-092 commit `23816164592743a59d4cc3b83ffd0ac39844d654`, parent `fa2ef6c526d04de9c77b356b53b4768890c46e1f`, message `Complete SPEC-0012 Phase 4 hosted search`; 27 accepted technical plus 15 reviewed record/tree paths. Canonical `main`, local `origin/main`, and live GitHub `main` matched at clean `0/0` after a normal fast-forward and push.
- Preservation/cleanup: 823 files/33,608,214 bytes are preserved at `output/recovery/GIT-092-spec0012-phase4-2381616/phase-4`; source/destination checksums match and `SHA256SUMS.txt` hashes to `cd2ef1927802f9aac3d9b6e8b4bd8d6c87309458e5526378529e91f73ddc7d42`. PID 31449 stopped, port 58040 closed, and only the obsolete Phase 4 review worktree and merged local branch were removed.
- Runtime activation correction: the canonical dev server had remained alive across the fast-forward. Its UI hot-reloaded, but the route's process-global `diamondGuidanceAssistantV1` singleton still held the pre-Phase-4 local-only provider. The first exact YouTube Shorts question therefore answered that the Assistant had no web access even though GIT-092 was present. No source byte was wrong or changed. Restarting only the canonical dev server cleared the stale singleton; the identical question then returned a hosted-search answer with two official YouTube Help citations. Arthur later requested another stop/start to cancel a request stuck on Finalizing for about five minutes; the old job then returned `404`, the new server returned `/assistant` HTTP 200 and listened on port 3000 under PID 38389. Neither restart changed code.
- Boundary: no accepted Phase 4 source/proof byte changed after GIT-092. The activation verification made one additional user-directed live provider/search request; its exact application receipt was not extracted into this terminal record. Phase 5 is now entry-ready but remains **Unauthorized; Not started**. Phase 6, public beta, deployment, paused SPEC-0008 and every animation/project mutation owner remain unchanged.

### D-0127 — Accept and authorize publication of the SPEC-0012 Phase 4 terminal-lifecycle correction

- Date: 2026-09-23
- Status: Accepted directly by Arthur; technical verification and Control Plane Architect propagation complete; GIT-093 publication/integration explicitly authorized and pending
- Authority and identity: Arthur accepted the corrected review app and explicitly authorized control-plane propagation, commit, canonical-main integration and GitHub push. Exclusive ownership transferred after the executor stopped. The accepted worktree starts from exact clean synchronized GIT-092 terminal base `21c5b3d70bf3ae5444a310913dbccec9a63895d9`, empty index and exactly 14 technical dirty paths. Immutable `output/spec-0012/phase-4-final-correction/proof-manifest.json` SHA-256 is `a078d42e1b1ac131787c1f8b48aeb333c2263b119b3e2b2b9172a8b213908137`; source digest is `6593a497cc93dd57fbde384071bdb6f84d8e555b4d7e8c0dec8bcd05f73712ce`; it binds 14 sources and five evidence files.
- Accepted outcome: `DiamondAssistantJobService` alone enters **Finalizing answer** after Terra has returned and the complete result has passed validation, holds it for about three seconds, and always retains a terminal slot. Repeated provider lifecycle events are deduplicated, overlapping or excessive activity fails safely, the full job deadline is 55 seconds and hosted-search work is bounded to 30 seconds. A provider returning more candidate source URLs than the app retains no longer produces the user-visible `search source limit was exceeded` failure: all candidates are canonicalized and validated, then at most eight sources are processed and six are displayed/persisted. Ordinary query/action safety limits remain. Search labels name the actual selected public topic. Answer presentation strips leaked Markdown/source syntax and raw URLs while keeping verified sources in the dedicated clickable Sources panel.
- Verification: 57 deterministic assertions, 23 terminal/finalization assertions, 23 browser assertions and four protected regression suites pass, together with TypeScript, focused lint and diff checks. Two authorized live fixed-Terra requests used no retry: local greeting completed in 8.611 seconds with zero tools; the YouTube current-information answer completed in 18.850 seconds with one hosted search, eight processed/two displayed official sources and 2.949 seconds of visible Finalizing. Reasoning changed, citations survived reload, the project sentinel stayed identical and browser/page errors were zero. The accepted timing is below the hard 55-second limit; the 18.850-second search is honestly above Arthur's preferred typical 10–15-second target.
- Retained boundaries: this correction removes no per-request token/cost, query/action, source-retention, concurrency, identity, cancellation, storage, privacy or mutation safety boundary. It adds no dictation, project/workspace access, animation mutation, provider/model fallback, arbitrary fetch/scrape/download, deployment or SPEC-0008 resumption. The AI Animator and every non-Assistant product surface remain unchanged.
- Publication boundary: GIT-093 may stage only the accepted 14 technical paths plus reviewed canonical record/tree paths. After normal fast-forward/push, exact proof preservation, canonical-server restart, review-server shutdown and D-0054 cleanup, Phase 5 remains entry-ready but **Unauthorized; Not started** until Arthur separately authorizes it.

### D-0128 — Publish, activate, preserve and clean up the SPEC-0012 Phase 4 terminal correction

- Date: 2026-09-23
- Authority/status: Arthur explicitly accepted the correction and authorized control-plane propagation, commit, canonical-main integration and GitHub push. GIT-093 is complete.
- Publication: exact 29-path commit `99128d70f4320dcfc757d81c7bc146baf3467f86`, parent `21c5b3d70bf3ae5444a310913dbccec9a63895d9`, message `Finalize SPEC-0012 Phase 4 assistant responses`, contains exactly 14 accepted technical paths plus 15 reviewed record/tree paths. Canonical `main` fast-forwarded normally; local `main`, local `origin/main`, and live GitHub `main` matched at clean `0/0` before this terminal records-only successor.
- Proof/activation: nine files/551,351 bytes are preserved under `output/recovery/GIT-093-spec0012-phase4-terminal-99128d7/` at framed aggregate SHA-256 `b0b34be5cad8d1af0639c2a6f166fa8b33d478ad978251e21645a2c283f3cef2`. Canonical Diamond Animator restarted from integrated `main` on port 3000; Home and `/assistant` returned HTTP 200. No additional live Terra/search request was made during publication.
- Cleanup/boundary: the exact review server on port 58050 stopped, the obsolete Phase 4 correction worktree was removed/unregistered, and merged local branch `codex/spec0012-phase4-terminal-correction` was deleted. Canonical main, active PM worktree, remote history, preserved proof, every non-Assistant product system and paused SPEC-0008 remain unchanged. Phase 5 is entry-ready but **Unauthorized; Not started** until Arthur separately authorizes it and resolves its transcription provider/model/privacy/cost gate.

### D-0129 — Accept and authorize publication of the final SPEC-0012 Phase 4 citation/reveal correction

- Date: 2026-09-23
- Status: Accepted by Arthur; technical verification and control-plane propagation complete; GIT-094 publication explicitly authorized and pending.
- Identity/evidence: exact clean base `741ee803f9e0bd72f7c64174a77e52a6ac3a7b17`, empty index, exact 14-path allowlist, sealed manifest SHA-256 `f611cfa77811685f9ddc208df3d314f1710a8bfb9ceac3fcedf058818780ba9e`, independent receipt SHA-256 `171c22eb1f7c00cf38cb9b71f12f08bf66a3fc838fbad48ae9b98e226ba6fc2b`, and source digest `aa6c1d7954a97d9cb9fab1502d2ba7639f10246c98be03bbfc968ecb9605da54`.
- Accepted outcome: a search answer with no native inline annotations may recover citations only from canonical provider-consulted URL metadata and presents neutral host labels; invalid/forged/private/missing metadata still fails closed. Every newly completed local or searched answer uses the already accepted fast typewriter and blue-gradient reveal even when storage notification precedes the initiating render; saved/reloaded replies do not replay.
- Proof/boundary: 25 completions, 208 reveal assertions, 100% reveal rate, zero duplicate answers, zero historical replay, six protected browser suites, accepted V1 oracle/browser/regression proof, TypeScript/focused lint/focused build/diff PASS and zero real/paid deterministic calls. Provider/model/search policy/reasoning/job lifecycle/storage schema/request limits/dictation and every non-Assistant product system are unchanged beyond the accepted citation recovery and reveal race fix. Phase 5 remains Unauthorized/Not started.

### D-0130 — Publish, integrate and activate the final SPEC-0012 Phase 4 correction

- Date: 2026-09-23
- Status: Fully closed; Verified/published/integrated/synchronized/runtime-activated.
- Publication: exact 29-path GIT-094 product commit `7053851babec9247c6733e016145fd0d0b8a6e46`, parent `741ee803f9e0bd72f7c64174a77e52a6ac3a7b17`, message `Finalize SPEC-0012 Phase 4 citation and reply reveal`; 14 accepted technical plus 15 reviewed control-plane/tree paths. Canonical `main`, `origin/main`, and GitHub `main` synchronized normally with no merge/rebase/force.
- Activation/proof: canonical Diamond Animator started from integrated `main` on port 3000. One chat accepted two consecutive live local-guidance messages; both completed, the reasoning control remained enabled, and there were zero browser errors, disconnect banners, citation-verification warnings, or blocked drafts. Port 58060 closed.
- Preservation caveat/boundary: task archival removed the completed executor checkout before CPA publication. The exact accepted 14 source bytes were recovered from the accepted change record and preserved V1 archive, and their aggregate digest reverified exactly as `aa6c1d7954a97d9cb9fab1502d2ba7639f10246c98be03bbfc968ecb9605da54` before commit. GIT-094 is the durable exact-source anchor; the V1 archive remains preserved. No Phase 5, dictation, deployment, provider/model/reasoning/lifecycle/storage or non-Assistant product change is included.

### D-0131 — Accept SPEC-0012 Phase 5 dictation and its private transcription boundary

- Date: 2026-09-24
- Authority/status: Arthur reported that he made a message with Diamond Animator's microphone dictation, called the review copy a pass, and explicitly authorized control-plane recording followed by commit/GitHub integration. The accepted implementation is technically Verified in the dedicated review worktree. This decision records acceptance and CPA propagation; it is not a publication claim.
- Identity: detached canonical-main base/HEAD `3676cfabba28554c4e5106a7cfb8822ac7c8c456`, empty index, exact 16 technical dirty paths, immutable `output/spec-0012/phase-5/proof-manifest.json` SHA-256 `3e37e42b2a402bbb50a9f6bc33fd6cc537f527791aee85664d0e23bd24a666ed`, source digest `e56ec8b7ff7eee3c0998d8f65b1ac95f9fa8f74160366d857f4e5bc6235e51d0`, 16 source/355 evidence bindings and 32 independent mutation rejections. The original executor lifecycle flags remain historical and are not rewritten.
- Accepted user outcome: permission follows an explicit microphone click; sampled input drives the waveform and elapsed display; Cancel discards; Stop submits one separate bounded transcription; success appends editable text; only explicit Send starts ordinary guidance. Typing remains available. Audio is not deliberately persisted or logged. Physical microphone success is Arthur's user-reported observation, distinct from deterministic Chrome proof; no live-call count, provider accuracy, latency or paid cost is asserted from it.
- Phase-specific service decision: `gpt-transcribe` at the official OpenAI audio-transcription endpoint, browser-requested 16 kHz mono signed 16-bit PCM WAV, 120 seconds/20 MiB maximum, 30-second permission/1.5-second flush/45-second transcription deadlines, 6,000-character/12,000-byte transcript and 64,000-byte response bounds, two active requests and 500 ten-minute cancellation identities. The 2026-09-24 duration price is estimated at `$0.0045`/minute and no more than `$0.009` at 120 seconds. There is no model fallback, automatic retry, automatic guidance Send, undocumented token parameter, raw-audio application persistence or public route exposure. The ignored existing server credential is used only by the private review server.
- Model-identity interpretation: the requested model is pinned to the one official endpoint; a returned model echo, if provided, must match. The documented JSON may omit an echo, so absence is recorded as `null`. Fixed-request evidence is proven; independent returned-model attestation is **not** claimed. This decision applies only to transcription and does not relax guidance's fixed-Terra returned-model check or §10's other receipt/safety rules.
- Proof/boundary: 33 deterministic oracle, 71 Chrome synthetic-audio, four real-route and five inherited-regression groups passed; five accessible layout profiles had zero serious/critical Axe findings; TypeScript, focused lint/build and diff passed; automated real/paid calls were zero; project/animation ownership, guidance search/reasoning/lifecycle/reveal, AI Animator, sessions, Home, editor, recovery and Export remain unchanged. The inherited full-build dev-cost type failure and full-lint 5-error/81-warning baseline remain untouched. Non-Chromium/physical-device matrix, provider-side memory erasure, and quantified live-transcription quality/cost/latency remain unproven.
- Next boundary: GIT-095 is the separate publication/integration/proof-preservation/D-0054 cleanup task under Arthur's existing publication authorization. Stop if canonical `main` advanced or any accepted/reviewed path differs; no force, pull, merge, rebase or scope expansion. Phase 6, public beta, deployment and paused SPEC-0008 remain unauthorized.

### D-0132 — Use proportionate model effort for future Project Manager work

- Date: 2026-09-24
- Authority/status: Arthur's explicit cost-control preference for this and future Project Managers; accepted as a working preference.
- Decision: default to `gpt-6-sol` with reasoning effort proportionate to routine implementation, control-plane and publication work. Use `gpt-6-astra` at Extra High when the task genuinely warrants difficult, consequential judgment, particularly AI cost, money, research or substantive recommendations. Reserve Ultra for exceptional cases in those areas, not ordinary tasks.
- Boundary: this preference does not override an explicit owner instruction or an applicable phase-specific safety/proof requirement that genuinely needs stronger reasoning. If such an exception is necessary, make its reason and scope clear. It changes no Phase 5 implementation byte, provider request, Git state or Phase 6 authorization.

### D-0133 — Accept SPEC-0012 Phase 6 local/private-beta hardening and authorize closeout publication

- Date: 2026-09-24
- Authority/status: Arthur called the dedicated Phase 6 review copy a pass and explicitly authorized control-plane propagation, commit, GitHub push, canonical-main integration and SPEC-0012 closeout. The stopped executor transferred exclusive ownership. This decision records acceptance and CPA propagation; publication is GIT-096 and remains pending at this record stage.
- Identity: exact synchronized GIT-095 base/HEAD `6de47a9dc1d780b63ed355fa5148f87cc5f0b1f2`, empty index, exactly 20 technical dirty paths, immutable `output/spec-0012/phase-6/proof-manifest.json` SHA-256 `a0e3eea7e973eb183ff274cfd9ff868d3e4b7712e60e431c9db1c67057702229`, 20 source and 80 evidence bindings, and twelve rejected manifest mutation classes.
- Accepted outcome: offline submission saves exactly one question, makes zero provider request, publishes no fake answer and ends clearly; no retry is automatic. One visible explicit Retry reuses the original turn/context with a fresh job identity, retains bounded prior-attempt metadata and can publish exactly one answer after reconnection. Provider connection errors receive truthful offline terminal copy, and help text distinguishes local chat storage from internet-required answers.
- Limit decision: D-0127 already removed the temporary user-visible source-overflow failure by bounding excess valid provider candidates. Fresh audit finds no cumulative temporary search/review budget. Permanent per-request token/cost/time/source, session/storage, transcription, concurrency, identity, cancellation and ten-attempt explicit-retry limits remain because removing them would weaken accepted safety and exceed this phase.
- Proof: offline search/local-guidance/reconnection/reload flows, 90 inherited stress assertions, 10 restart/interruption assertions, 21 protected-product browser assertions, 11 AI Animator assertions, 14 inherited oracles, five accessibility layouts with zero serious/critical Axe findings, 50-session/6,000-message performance, TypeScript/focused lint/build/diff and manifest validation pass. Automated real/provider/paid calls are zero. The inherited full-build dev-cost type failure and five-error/81-warning full-lint baseline remain in untouched files.
- Boundary: target is local/private beta only. No public-beta authentication/ownership/rate-abuse/budget/deployment policy is claimed. Physical devices and non-Chromium are unproven. No AI Animator, project, Home, My/Open Projects, Tutorials, Export, editor/timeline/canvas/tools, recovery, persistence, finalizer, paused SPEC-0008, dependency, credential or deployment byte changed.
- Next boundary: GIT-096 must publish only the exact accepted implementation plus reviewed records/tree, preserve proof, synchronize cleanly, stop the exact review server and complete D-0054 cleanup. A terminal records-only successor must then record the actual product commit and mark SPEC-0012 fully closed without changing runtime.

### D-0134 — Publish, activate, preserve and close SPEC-0012 Phase 6

- Date: 2026-09-24
- Authority/status: Arthur explicitly accepted Phase 6 and authorized control-plane recording, commit, GitHub push, safe canonical-main integration and full SPEC-0012 closure. GIT-096 and its terminal records closeout are complete.
- Publication: exact 34-path product commit `20612c364686b5b855e85f0b12c28a61e84b1061`, parent GIT-095 `6de47a9dc1d780b63ed355fa5148f87cc5f0b1f2`, message `Complete SPEC-0012 Phase 6 beta hardening`: 20 accepted technical plus 14 reviewed control-plane/tree paths. Canonical `main`, local `origin/main` and live GitHub `main` matched at clean `0/0` after a normal fast-forward and push.
- Activation/proof: canonical Diamond Animator restarted from integrated main on port 3000; Home and `/assistant` returned HTTP 200 without another provider call. The 82-file/4,888-KiB proof bundle is preserved at `output/recovery/GIT-096-spec0012-phase6-20612c3/phase-6`; checksum-mode comparison is empty and its manifest remains SHA-256 `a0e3eea7e973eb183ff274cfd9ff868d3e4b7712e60e431c9db1c67057702229`.
- Cleanup: Phase 6 review PID 88555/process group 88554 stopped and port 58160 closed. After synchronization, D-0054 removed only `/Users/arthurcarlin/.codex/worktrees/1d01/stick-animation-app` and merged local branch `codex/spec0012-phase6-closeout`; canonical main, the active Project Manager worktree, remote history and recovery evidence remain.
- Boundary/consequence: no post-acceptance runtime/proof byte changed. No temporary cumulative search/review limit existed to remove; permanent safety bounds remain. Public beta, deployment, production-readiness, physical-device/all-browser claims and paused SPEC-0008 work are excluded. SPEC-0012 is fully closed with no Phase 7. The next new feature requires a separately planned and authorized SPEC-0013.

### D-0135 — Accept SPEC-0013 Phase 1 and authorize exact publication

- Date: 2026-09-24
- Authority/status: Arthur accepts the corrected SPEC-0013 review copy and authorizes canonical control-plane propagation, staging/commit of only the accepted implementation plus reviewed records, fast-forward integration into clean canonical `main`, normal GitHub push, proof preservation and D-0054 cleanup.
- Identity: exact clean base `8ddc534691a154d2f3ab25cceb61d91690446abb`, empty index, exactly seven technical paths, immutable `output/spec-0013/phase-1/proof-manifest.json` SHA-256 `bf8e51c68fe67015a97a6428c39a73bffc2c7a92b08e2f326554a1cf35e2db63` and fresh strict `VALID` recheck.
- Accepted outcome: remove the inert AI Project Finalizer Home card and its hover identity; leave Export as the sole Tools/final Home card; retain the existing Home scroll owner, all scrollbar CSS and 60px bottom space; correct only the now-stale Assistant Finalizer fact/catalog identity.
- Proof: 12 oracle and 28 real-browser checks pass across desktop, medium and compact profiles; Finalizer is absent from visible/semantic order, Export remains functional, scroll geometry and fade behavior pass, saved Assistant schema validates, focused TypeScript/lint pass and automated provider calls are zero. The untouched dev AI-cost generated-type build failure remains inherited.
- Connectivity/secret boundary: Arthur's review followed live Workspace Terra and AI Assistant replies using ignored local credential linkage. No credential or `.env.local` byte enters Git. Port 58080 is stopped/closed.
- Unchanged systems: no Finalizer feature, Export implementation, workspace/project/recovery, AI Animator, Assistant request/provider/search/dictation/retry/persistence, dependency, environment, deployment or paused SPEC-0008 behavior changed.
- Next boundary: exact GIT-097 publication, synchronization, proof preservation and D-0054 cleanup. SPEC-0014 remains undefined and unauthorized until that closeout completes.

### D-0136 — Publish, preserve and close SPEC-0013 Phase 1

- Date: 2026-09-24
- Publication: GIT-097 is exact 21-path product/control-plane commit `893daa82f555381d775af8fb73b14a551bd5fb45`, parent `8ddc534691a154d2f3ab25cceb61d91690446abb`, message `Complete SPEC-0013 Phase 1 Home cleanup`: seven accepted technical plus fourteen reviewed control-plane/tree paths.
- Synchronization: clean canonical `main` fast-forwarded normally and pushed; canonical/local-origin/live-GitHub `main` matched GIT-097 at clean `0/0` before this terminal records-only successor.
- Proof: thirteen files/262,442 bytes are preserved at `output/recovery/GIT-097-spec0013-phase1-893daa8/phase-1`. Source/destination inventories match at aggregate SHA-256 `cbe9142a42612f73f8e9cdec11452859085f98f4026dd7f21a3df48acc117d4b`. The immutable technical manifest remains SHA-256 `bf8e51c68fe67015a97a6428c39a73bffc2c7a92b08e2f326554a1cf35e2db63`; Control Plane Architect closeout manifest SHA-256 is `9346eb3eb1f1b7997e8339d5db2df0d6a1cd737e9e8be429b351fc8aaa938e6c`.
- Cleanup: port 58080 remained closed. After proof preservation and synchronization, D-0054 removed only `/Users/arthurcarlin/.codex/worktrees/spec0013-phase1/stick-animation-app` and merged local branch `codex/spec0013-phase1-publication`. Canonical main, the active Project Manager worktree, remote history and recovery material remain.
- Boundary/consequence: no post-acceptance runtime, fixture, technical-test or proof byte changed. No credential was committed. SPEC-0013 is fully closed with no Phase 2. SPEC-0014 is ready for a fresh owner-defined planning task but remains undefined and unauthorized.

### D-0137 — Approve the four-phase SPEC-0014 notification-center specification without authorizing implementation

- Date: 2026-09-24
- Authority/status: Arthur explicitly authorizes a four-phase specification-only control-plane task. [`SPEC-0014 — Durable In-App Notification Center and Background Completion`](specs/0014-in-app-notification-center.md) is approved as the intended staged contract; all four implementation phases remain **Unauthorized; Not started**.
- Evidence: the docs-only trace began clean at canonical `main` `acc3204d6c5aa08f1d0d714dcd5a64e8746dcb8d`. Live Chromium and code prove the Home bell is a component-local preview; Assistant/Terra terminalization and Export execution are screen-owned; no durable center/read owner or app-wide offline incident exists.
- Decision: Phase 1 defines the real center UI/store/event contract. Phase 2 owns exact Assistant/Terra completion and failure after in-app navigation. Phase 3 owns exact validated Export completion/failure after in-app navigation. Phase 4 closes offline incident dedupe, concurrency, persistence, reload/interruption truth, navigation, accessibility, regressions and inactive future contracts.
- Active truth: export success uses exact body **“Your animation is done exporting.”** and links the truthful exact project/export result; Assistant and Terra events resolve the exact conversation or project/workspace; AI and Export failure/timeout are included; one real browser-declared offline incident uses exact body **“There is no internet, so no AI will be able to be called.”** without per-call spam.
- Dormant truth: AI Animator finished animating, low AI usage at 10% with Buy/Refill, and update available are contract-only/inactive until their later authoritative systems exist. Current Terra chat, dashboard placeholders and hard-coded version checks may not emit them.
- Owner gate: before Phase 1, Arthur must create **Project Manager Version 5** and the current Project Manager must explicitly transition to **AI Animator/Dad support**. The planning package must also be separately published/synchronized and Arthur must separately authorize Phase 1 from that exact SHA.
- Boundary: this decision changes canonical planning records only. It authorizes no runtime/fixture/test/proof mutation, Spec Executor, provider/paid action, billing/dashboard/AI Animator/updater/OS push/email work, staging, commit, merge, push, publication or deployment.

### D-0138 — Accept corrected SPEC-0014 Phase 1 two-bell foundation and authorize closeout

- Date: 2026-09-25
- Authority/status: Arthur explicitly called the corrected review copy acceptable/a pass and requested canonical control-plane propagation followed by commit, safe `main` integration and GitHub push. This decision records acceptance and sequential Control Plane Architect ownership; actual Git publication is a separately verified operation and is not claimed here.
- Identity: accepted correction branch `codex/spec0014-phase1-notification-correction` at exact clean canonical base/HEAD `a5ca805b220357b5e8128bb6b76ea408e30fa790`; stopped Spec Executor; empty index; exactly 19 accepted runtime/fixture/technical-test paths; immutable `output/spec0014/phase1/manifest.json` SHA-256 `b1c082c2508b3039fdeacdd65f99ded4a9d49ed7cec99eb9aaff8352be143669` with 19 source and 111 evidence bindings. Fresh independent validator returns VALID and rejects 18 mutations. The earlier rejected review bytes were preserved separately and are not reused.
- Accepted outcome: exactly two visible bell locations, Home and AI Assistant. Home contains all eligible notification kinds; Assistant shows only its own reply completion/failure. Both use one durable record/read store, with Assistant-scoped rows/count/dot/ring/announcements/Mark-all. Every other surface has zero bells. The blue dot has no visible numeral; accessible text gives the exact count. No production producer, origin registration, target handler, polling loop or injected fixture is connected in Phase 1.
- Proof: 35 contract, 29 storage and 41 UI assertions; exact Home `1`/Assistant `1`/other `0` inventory; shared-read, scope, reload/restart/two-tab, corruption/capacity, accessibility and protected Home/Assistant/search/retry/synthetic-dictation/Terra/export/editor checks pass. TypeScript, focused lint/build and diff pass; automated provider/external requests are zero. Full lint remains inherited five errors/81 warnings. Full Webpack production build fails on the identical untouched dev AI-cost `PageProps` route on both base and result; default Turbopack build failed in the offline environment. Do not call this production-build ready.
- Boundary: accepted technical and proof bytes are frozen. No new AI/search/transcription limit, disconnected/configured state, provider policy, dictation/chat/Terra/export/project/editor/recovery behavior, credits/billing, deployment or later SPEC-0014 phase is authorized. Native browser 200% zoom, native OS focus, physical microphone/devices, non-Chromium and live provider calls remain unproven.
- Next boundary: publish only the accepted 19 paths plus reviewed control-plane/tree paths from the unchanged base if canonical `main` and live remote have not advanced; normal commit/fast-forward/push only, no pull/rebase/force/scope expansion. Preserve ignored proof and complete D-0054 review-copy cleanup after clean synchronization. Phase 2 then becomes entry-ready, but remains Unauthorized/Not started until Arthur separately authorizes a fresh Plan-mode executor.

### D-0139 — Publish, preserve and close SPEC-0014 Phase 1

- Date: 2026-09-25
- Authority/status: Arthur accepted the corrected Phase 1 app and explicitly requested project-record propagation, commit, GitHub push and safe integration into canonical Diamond Animator `main`. GIT-099 publication, synchronization, proof preservation and D-0054 cleanup are complete. No cloud database was changed.
- Publication: exact 33-path product/control-plane commit `06365eacffe493ea3550de71b3baff0f57bf03b2`, parent `a5ca805b220357b5e8128bb6b76ea408e30fa790`, message `Complete SPEC-0014 Phase 1 notification foundation`: 19 accepted technical paths plus 13 canonical documents and generated project tree. Clean canonical `main` fast-forwarded normally; local `main`, local `origin/main` and live GitHub `main` matched the exact commit at `0/0` after a normal push. No force, pull, rebase, unrelated worktree mutation or deployment occurred.
- Proof: immutable accepted manifest SHA-256 `b1c082c2508b3039fdeacdd65f99ded4a9d49ed7cec99eb9aaff8352be143669` and all 19 implementation/111 evidence bindings revalidated without altering the technical files. All 116 files/10,754,616 bytes under the accepted `output/spec0014/phase1` proof tree, including the CPA packet, were preserved in the active PM worktree at `output/recovery/GIT-099-spec0014-phase1-06365ea/phase1`; source/destination checksums and framed inventory SHA-256 `4f237679d33070d715939c433ca95e0bd6d07168f318aca5fda704ffc3f30598` match.
- Cleanup: verified exact `next dev --webpack -p 58420` parent PID 87896/listener PID 87897 and review-worktree cwd; stopped that process group and verified port 58420 closed. After proof preservation and clean synchronization, removed only `/Users/arthurcarlin/.codex/worktrees/9e07/stick-animation-app` and its merged now-unused local branch `codex/spec0014-phase1-notification-correction`. Canonical main, the active PM worktree, remote history and recovery evidence remain.
- Boundary/consequence: Phase 1 contains only two scoped bells and a durable store/UI foundation; actual Assistant/Terra, Export and offline producers remain disconnected until Phases 2/3/4. Full production build readiness remains unproven because of the identical untouched dev AI-cost `PageProps` baseline failure; native zoom/focus, physical devices, non-Chromium and live provider/microphone remain unproven. Phase 2's preceding-phase entry gate is satisfied, but Phase 2 is **Unauthorized; Not started** until Arthur separately authorizes a fresh Plan-mode executor from synchronized canonical `main`. No AI/search limit, provider policy, dictation/chat/export/project/editor behavior, credits/billing or deployment changed.

### D-0140 — Accept corrected SPEC-0014 Phase 2 unread-only AI completion and authorize closeout

- Date: 2026-09-25
- Authority/status: Arthur called the corrected existing Phase 2 app copy a pass and explicitly directed control-plane recording, commit, GitHub push and safe integration into canonical Diamond Animator. This entry records acceptance and sequential exclusive Control Plane Architect propagation; it does not claim that publication has occurred. The original Phase 2 executor and separate same-copy correction executor have stopped.
- Identity: `/Users/arthurcarlin/.codex/worktrees/974d/stick-animation-app`, branch `codex/spec0014-phase2-ai-completion-review`, unchanged canonical-main base/HEAD `acd4031cade0e4e960223190a8c1645d007d61af`, empty index, exact 21 accepted runtime/fixture/technical-test/proof paths. Immutable `output/spec0014/phase2/manifest.json` SHA-256 is `77bc08b62c472d9ed0f75a2cd6b7d9b4b84d3f9b2e75d89645738567904ae8e7`, with 21 source/105 evidence bindings and 12 rejected material mutations. Accepted review server is `http://127.0.0.1:58440/`.
- Accepted current outcome: root Assistant and Terra observers watch jobs already submitted by the user, commit/reverify exact Assistant session/turn or project-scoped Terra ledger terminal first and publish one shared deterministic unread notification only when the exact origin was not visibly focused. Home and Assistant are still the only bell locations; Assistant is reply-only; both share one read state. The panels display unread items only. Watched exact-origin results create no visible notification. Assistant rows name the saved chat, Terra rows the captured project, and exact Home-to-chat, chat-to-other-chat or Home-to-project navigation focuses the originating turn/reply. Only confirmed exact arrival marks a clicked item read; blocked/unavailable targets stay unread. Durable read records remain for dedupe, not as visible history, and no next-day purge is added. This latest Arthur-accepted rule supersedes SPEC-0014's earlier visible-read-history and mark-before-navigation planning language.
- Proof: 42 deterministic oracle and 42 real-browser assertions, 16 protected static and four protected browser receipts, TypeScript/focused lint/focused build/diff PASS, exact two-bell inventory and zero automated provider/external/paid/live AI calls. Nine Assistant and seven Terra explicit proof submissions are local deterministic fixtures; there is no automatic resubmit. The accepted protected runtime outside the allowlist is byte-identical. Full Webpack production build remains blocked by the identical untouched dev AI-cost `PageProps` route on base/result; physical microphone/devices, native OS focus and non-Chromium remain unproven.
- Unchanged boundaries: no AI thinking/reply/search/dictation/provider/model/request-limit policy, animation/project mutation, Export Phase 3 producer, offline Phase 4 incident, future low-usage/updater/AI-animation event, new bell, OS/external notification, paid call or deployment. No accepted technical/proof byte is modified by this control-plane propagation.
- Next boundary: GIT-100 is a separate bounded publication operation authorized by Arthur's latest request after the architect stops with an empty index. It may stage only the accepted 21 technical paths and reviewed canonical record/tree paths if canonical `main` and remote still match the base; normal commit/fast-forward/push only. Then preserve proof and complete D-0054 review-copy cleanup. Phase 3 remains Unauthorized/Not started until Phase 2 is fully integrated/synchronized/proof-preserved/cleaned up and Arthur separately authorizes it.

### D-0141 — Reject SPEC-0014 Phase 3 Export notifications; keep accepted AI bells and rescope Phase 4

- Date: 2026-09-25
- Authority/status: Arthur visibly rejected the separate Phase 3 Export review copy and explicitly authorized this canonical-main control-plane record. The reason is product utility: a short ordinary export should not require racing away from the result screen to receive an alert. This is **owner rejection**, despite the executor's technical PASS. Phase 3 is abandoned/unaccepted/unpublished, not completed; its runtime, fixture, test and proof bytes must not be merged or reused as implementation.
- Identity/evidence: rejected dedicated review worktree `/Users/arthurcarlin/.codex/worktrees/bd83/stick-animation-app` began at canonical-main `c8fbc954c130acdcc9df429d7581c38b497bc33a`, has an empty index and exactly 15 dirty technical paths. Its ignored `output/spec0014/phase3/manifest.json` SHA-256 is `6e93cd7b73f8dca43c19fae2aebc7170889475f515e63909342ab7116817f868`. This hash identifies rejected historical evidence, not accepted or independently published proof. D-0054 requires verified recovery preservation, exact server closure and worktree cleanup before a new executor.
- D-0054 cleanup closeout: after the stopped executor and exact 15-path dirty set were rechecked, all 15 rejected technical files plus 22 ignored proof files were preserved under `/Users/arthurcarlin/.codex/worktrees/d658/stick-animation-app/output/recovery/D-0141-rejected-spec0014-phase3-c8fbc95/`. Checksum dry-run comparison found no source/destination difference; the 37-file backup inventory digest is SHA-256 `20c2b36371a3fca949a11cf9b0375a0e68e4562c9a37518516ad6ff62679c8bf`, and the preserved manifest still hashes to `6e93cd7b73f8dca43c19fae2aebc7170889475f515e63909342ab7116817f868`. Exact listener PID 31063/parent 31062 and worktree cwd were verified, the parent was stopped, port 58460 closed, and only the rejected detached `/bd83/` worktree was removed. No local branch existed to delete. Canonical main, the PM worktree, the accepted code and recovery evidence were preserved.
- Accepted outcome retained: published Phases 1–2, both existing bells, unread-only exact-source Assistant/Terra completion/failure notifications and shared read state remain intact. **Only Export notifications and background Export execution are removed from this notification plan.** Existing screen-owned Export behavior remains unchanged; no new bell, AI thinking/reply/search/dictation/provider/limit change, or app behavior change is authorized here.
- Future outcome: Phase 4 is the next candidate for one truthful browser-declared offline incident and reliability/regression closure of the accepted notifications. Future AI-animation completion, low-AI-usage warning and Diamond Animator update notice remain desired but inactive until authoritative sources and separate authorization exist; no invented quota, Version 2 release or updater is permitted. Phase 4 has no Export producer/coordinator requirement and does not depend on accepted Phase 3.
- Gate/boundary: this decision is docs-only and does not itself authorize Phase 4 implementation. Before a fresh Phase 4 Plan-mode Spec Executor, publish/synchronize this record, complete D-0054 rejected-copy cleanup, and obtain Arthur's separate Phase 4 authorization from the resulting canonical-main SHA. This task performs no runtime/fixture/test/proof mutation, staging, commit, merge, push, deployment, provider or paid operation.
- Subsequent Phase 4 authorization: Arthur explicitly authorized a new Phase 4 app copy to add a truthful no-internet alert to the existing Home bell while leaving the Assistant bell reply-only, preserving accepted Assistant/Terra notifications, and changing no AI thinking/search/dictation/provider/tool or Export behavior. This authorizes the Phase 4 product task only after D-0141 is published from a clean base and a fresh dedicated Spec Executor starts in Plan mode. It is not authorization to publish a future implementation or to add fabricated low-usage/updater notices. The D-0141 rejection decision itself did not implement Phase 4.

## Provisional Legacy Classifications

### D-0006 — Preserve the V1 motion-tween specification for reconciliation

- Date: 2026-08-09
- Status: provisionally promoted legacy intent; Arthur confirmation and current acceptance rerun pending
- Decision: `diamond-animator-docs/02_animation_engine/MOTION_TWEEN_SYSTEM.md` remains the best detailed legacy description of intended V1 drawing motion-tween behavior and must be read before related work. This audit does not retroactively claim owner approval, and the latest user instruction or an accepted superseding decision prevails.
- Why: it is the only existing subsystem document with detailed scope, data model, debug path, and acceptance suite.
- Consequence: motion-tween tasks must read it, rerun/reconcile its acceptance flow, and record any code/spec mismatch before changing either. A mismatch is evidence to resolve, not automatic proof that live code or the document wins.

## Pending Product Decisions

### P-0001 — Launch role of the Drawing Workspace

- Status: resolved by D-0056/D-0062 and completed through D-0069/D-0070 and GIT-061 `185f58752c4f8dd4f5bfea0befe48998e5cd0f04`.
- Resolution: Under D-0062, Drawing and Stick become typed content items in neutral layers and the same frame/cell inside one Animation Workspace rather than a separate New Project choice or separate saved-project pillar. Drawing-specific feature expansion still requires its own scope.

### P-0002 — Official product name

- Status: pending Arthur
- Question: “Diamond Animator” or “Diamond Animator Pro”?
- Needed before: brand/UI/documentation normalization.

### P-0003 — Definition of professional-grade

- Status: pending product spec
- Question: which measurable gates cover visual clarity, motion continuity, timing, editability, fidelity, export, latency, and failure rate?
- Needed before: accepting AI or manual animation milestones.

### P-0004 — AI transaction semantics

- Status: D-0092 remains the recorded planning resolution for the paused SPEC-0008 V1 slice; no current dispatch authority; broader post-V1 policy pending
- Question: which commands preview, auto-apply, request confirmation, or require reversible transactions?
- Needed before: enabling broad Other/actions or stick-workspace AI control.
- Narrow SPEC-0001 resolution: D-0009 accepts Preview → explicit Apply and one atomic reversible transaction for the single approved Stick action only; the product-wide question remains pending.
- SPEC-0008 historical planning resolution: D-0092 specifies that a clear current create-animation or edit-animation message authorizes one bounded mutating job, with internal validation, one atomic global-history commit, pre-commit cancellation and Undo/Redo correction. D-0093 pauses every unimplemented phase, so this recorded contract is non-dispatchable until explicit resumption and reconciliation.

### P-0005 — Cost and latency policy

- Status: pending Arthur/product economics
- Question: target cost, latency, token, retry, search, and model-escalation ceilings; mapping to credits.
- Needed before: production AI defaults and paid regression gates.
- SPEC-0004 status: D-0033 retired the former SPEC-0001 Phase 7 Policy Gate; D-0034 sets the intended Terra/no-auto-switch and later release direction without accepting mutable provider facts or paid behavior. Cost, latency, token, retry, credits, and paid-proof values are Phase 6/8 gates and must be freshly approved rather than inferred.
- D-0038 future direction: prefer compact key-pose/timing plans, local in-betweens, free complete-match recipes first, at most one later bounded pre-commit repair inside the isolated candidate, measured actual/estimated cost, and monthly budget/credit limits. These are planning constraints, not accepted prices, caps, or paid-request authority.
- D-0085 adds bounded Phase 1 token/request/concurrency/latency guards but authorizes no paid call. Same-day Terra access/rate/pricing review and Arthur-approved request/spend ceilings are mandatory before any live paid smoke; video-provider and public-beta per-user/monthly/credit caps remain mandatory owner decisions at the Phase 2/6 entry gates.

### P-0006 — First usable manual drawing-based feature set

- Status: resolved and fully closed by corrected drawing-only SPEC-0007 under D-0074; GIT-062 closes Phase 1, GIT-063 closes Phase 2, D-0079/D-0080/GIT-064 close Phase 3, D-0081/D-0082/GIT-065 close Phase 4, and D-0083/D-0084/GIT-066 close Phase 5.
- Resolution: SPEC-0006 Phase 5 integrated the current tools. D-0074 replaces the former seven-phase structured-rig outcome with five phases: Drawing Engine/No-Loss; Draw Rig as ordinary raster paint; Safe Legacy Rig Retirement/Migration; bounded still Assets; and final command-registry/bug-burn proof. Active rig/Creator authoring ends after safe migration rather than expanding. Export, animated symbol timelines, motion/video/tracking, and AI expansion remain outside SPEC-0007.

### P-0007 — Data/search/privacy policy

- Status: pending product/legal/security decision
- Question: what workspace/project data can be sent to model/search providers, retained remotely, and logged locally?
- Needed before: public deployment.
- Narrow completed-SPEC-0001 resolution: D-0009's session-only transcript, minimized provider projection, search/tools-off, and zero application-persistent Stick AI logs remain historical rules for the completed bounded slice. D-0033 retired OD-13's former Phase 7 gate; D-0034 requires a fresh Phase 6/8 retention, data-minimization, logging, and privacy decision before Terra or production work.
- D-0085 specifies Phase 1 data minimization/log redaction and requires approved provider retention/data-use plus application retention/deletion before real-user enablement. Exact Phase 2 video-provider terms and the full Phase 6 public-beta record remain open named gates.

### P-0008 — AI-first drawing-based vertical-slice sequencing

- Status: Phase 1 resolved/completed; post-Phase-1 sequence paused and reopened for architecture reconciliation by D-0093
- Resolution: the published six-phase order and D-0091/D-0092 refinements remain historical design intent: Terra brain/jobs, bounded internet research and provider-neutral reference video, deterministic slicing, editable reconstruction, registered-command edits, and beta/export closeout. D-0093 prevents dispatch of Phases 2–6 while the reference-video path and the unproven reusable-motion alternative are reconsidered.
- Boundary: neither the historical sequence nor the research alternative creates implementation, provider, paid-call, asset-purchase or programming authority. Explicit resumption and a reconciled spec are required.

### P-0009 — Possible unified Animation Workspace

- Status: resolved by D-0056/D-0062 and completed through D-0069/D-0070 and GIT-061 `185f58752c4f8dd4f5bfea0befe48998e5cd0f04`.
- Resolution: one project/timeline/stage/save/history/AI surface replaces the separate Drawing/Stick choice using ordered typed Drawing/Stick items in neutral same-cell layers (D-0062), lossless non-destructive legacy adoption, and one canonical writable root. See corrected SPEC-0006 for seven numbered phases, two internal Phase 4 gates and final visible proof.
- Current boundary: published Phase 7 completes current tools/library, all-source adoption/recovery and ordinary duplicate-owner retirement on the neutral V2 root. Future capability expansion belongs to separately authorized SPEC-0007/0008.
